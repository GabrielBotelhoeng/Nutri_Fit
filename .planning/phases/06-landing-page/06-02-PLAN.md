---
phase: 06-landing-page
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - nutrichat-landing/components/HowItWorks.tsx
  - nutrichat-landing/components/Features.tsx
  - nutrichat-landing/components/Testimonials.tsx
autonomous: true
requirements:
  - LANDING-02
  - LANDING-03
  - LANDING-04

must_haves:
  truths:
    - "Seção 'Como funciona' exibe 4 cards com steps numerados (1-4), ícones Lucide e animação fadeInUp ao entrar na viewport"
    - "Seção 'Funcionalidades' exibe 6 cards com ícones Lucide e animação staggerChildren 80ms"
    - "Seção 'Depoimentos' exibe 3 cards placeholder com bg-menta na seção e bg-branco nos cards"
    - "Todas as três seções usam whileInView com viewport once:true e as variantes fadeInUp/staggerContainer de lib/animations.ts"
    - "npm run build executa sem erros após adição destas seções"
  artifacts:
    - path: "nutrichat-landing/components/HowItWorks.tsx"
      provides: "4 steps com ícones UserPlus, MessageCircle, Camera, BarChart2"
      contains: "id=\"como-funciona\""
    - path: "nutrichat-landing/components/Features.tsx"
      provides: "6 cards com ícones Image, Mic, Bell, FileText, Calculator, Repeat"
      contains: "id=\"funcionalidades\""
    - path: "nutrichat-landing/components/Testimonials.tsx"
      provides: "3 cards placeholder — Maria S., João P., Ana C."
      contains: "bg-menta"
  key_links:
    - from: "components/HowItWorks.tsx"
      to: "lib/animations.ts"
      via: "import { fadeInUp, staggerContainer } from '@/lib/animations'"
      pattern: "from '@/lib/animations'"
    - from: "components/Features.tsx"
      to: "lib/animations.ts"
      via: "import { fadeInUp, staggerContainer } from '@/lib/animations'"
      pattern: "from '@/lib/animations'"
    - from: "components/Testimonials.tsx"
      to: "lib/animations.ts"
      via: "import { fadeInUp, staggerContainer } from '@/lib/animations'"
      pattern: "from '@/lib/animations'"
---

<objective>
Implementar as três seções de conteúdo da landing page: "Como funciona" (4 passos numerados), "Funcionalidades" (6 cards de features) e "Depoimentos" (3 cards placeholder). Todas as seções usam Framer Motion whileInView para scroll-reveal sutil com as variantes compartilhadas de lib/animations.ts criadas no plano 06-01.

Purpose: Entregar o corpo de conteúdo da landing que explica o produto, convence o visitante e constrói credibilidade via depoimentos — os elementos de conversão antes da seção de planos.

Output: Três componentes de seção implementados com animações Framer Motion, ícones Lucide, paleta correta e responsividade mobile-first — substituindo os stubs criados no plano 06-01.
</objective>

<execution_context>
@C:/Users/botel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-CONTEXT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-01-SUMMARY.md

<interfaces>
<!-- Contratos do plano 06-01 que este plano consome -->

De lib/animations.ts (criado em 06-01):
```ts
export const fadeInUp = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.5, ease: 'easeOut' } },
}
export const staggerContainer = {
  hidden: {},
  visible: { transition: { staggerChildren: 0.08 } },
}
```

Tokens de cor disponíveis em globals.css (criados em 06-01):
- `bg-offwhite` (#F0F7EC) — bg das seções HowItWorks e Features
- `bg-menta` (#C8E6C0) — bg da seção Depoimentos
- `bg-branco` (#FFFFFF) — bg dos cards
- `text-floresta` (#2D5016) — títulos H2 de seção
- `text-terra` (#6B3D1E) — texto principal dos cards
- `text-claro-br` (#A0694A) — descrições, subtítulos
- `text-medio` (#4A7C2F) — ícones Lucide
- `border-menta` (#C8E6C0) — borda dos cards padrão

Padrão de card com hover lift (06-PATTERNS.md §Shared Patterns):
```tsx
className="... hover:-translate-y-1 transition-transform duration-200"
```

Padrão de container centralizado (06-PATTERNS.md §Shared Patterns):
```tsx
className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8"
```

Conteúdo exato dos depoimentos (06-UI-SPEC.md §Copywriting):
- Maria S., 32 anos: "Em 3 semanas já perdi 2kg. O bot me lembra de tudo e ainda calcula o que eu como em segundos."
- João P., 28 anos: "Nunca consegui seguir uma dieta antes. Com o NutriChat ficou fácil porque é tudo pelo WhatsApp mesmo."
- Ana C., 41 anos: "Minha nutricionista consegue ver tudo que comi durante a semana. O acompanhamento melhorou muito."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: HowItWorks.tsx — 4 passos numerados com Framer Motion e Lucide</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md — Seção "HowItWorks.tsx" (linhas 390-458)
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md — "Seção Como funciona" (linhas 153-161), "Copywriting Contract" linha "Título Como funciona", "Animações e Interações" tabela
  </read_first>
  <files>nutrichat-landing/components/HowItWorks.tsx</files>
  <action>
Substituir o stub de HowItWorks.tsx com a implementação completa:

```tsx
'use client'

import { motion } from 'framer-motion'
import { UserPlus, MessageCircle, Camera, BarChart2 } from 'lucide-react'
import { fadeInUp, staggerContainer } from '@/lib/animations'

const STEPS = [
  {
    num: 1,
    Icon: UserPlus,
    title: 'Nutricionista cadastra',
    desc: 'O nutricionista cadastra o paciente no sistema com nome, WhatsApp e upload da dieta em PDF.',
  },
  {
    num: 2,
    Icon: MessageCircle,
    title: 'Agente contata o paciente',
    desc: 'O NutriChat envia boas-vindas automaticamente e realiza a entrevista inicial pelo WhatsApp.',
  },
  {
    num: 3,
    Icon: Camera,
    title: 'Paciente registra refeições',
    desc: 'Por texto, áudio ou foto — o paciente registra cada refeição e recebe o saldo nutricional do dia.',
  },
  {
    num: 4,
    Icon: BarChart2,
    title: 'Relatório semanal automático',
    desc: 'Todo domingo, paciente e nutricionista recebem um relatório completo da semana.',
  },
]

export default function HowItWorks() {
  return (
    <section id="como-funciona" className="py-16 bg-offwhite">
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8">
        <h2 className="text-floresta font-bold text-3xl text-center mb-4">
          Simples para o nutricionista, poderoso para o paciente
        </h2>
        <p className="text-claro-br text-center mb-12">
          Quatro passos para um acompanhamento nutricional completo.
        </p>
        <motion.div
          className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"
          variants={staggerContainer}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, amount: 0.2 }}
        >
          {STEPS.map((step) => (
            <motion.div
              key={step.num}
              variants={fadeInUp}
              className="bg-branco border border-menta rounded-xl p-6 flex flex-col gap-3 hover:-translate-y-1 transition-transform duration-200"
            >
              {/* Número circular */}
              <div className="w-10 h-10 rounded-full bg-floresta text-offwhite flex items-center justify-center font-bold text-lg flex-shrink-0">
                {step.num}
              </div>
              {/* Ícone */}
              <step.Icon size={24} className="text-medio" />
              {/* Conteúdo */}
              <h3 className="font-bold text-terra">{step.title}</h3>
              <p className="text-sm text-claro-br leading-relaxed">{step.desc}</p>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
  )
}
```

Nota: O STEPS array contém os 4 ícones exatos definidos no UI-SPEC (UserPlus, MessageCircle, Camera, BarChart2). O staggerContainer + fadeInUp são importados de lib/animations.ts (plano 06-01). Usar `once: true` no viewport para animar apenas uma vez.
  </action>
  <verify>
    <automated>cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build 2>&1 | grep -E "(error TS|Error|compiled|Build)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/HowItWorks.tsx` linha 1 contém `'use client'`
    - `components/HowItWorks.tsx` contém `id="como-funciona"` no element section
    - `components/HowItWorks.tsx` contém `UserPlus` e `MessageCircle` e `Camera` e `BarChart2` importados de `'lucide-react'`
    - `components/HowItWorks.tsx` contém `from '@/lib/animations'` com `fadeInUp` e `staggerContainer`
    - `components/HowItWorks.tsx` contém `whileInView="visible"` e `viewport={{ once: true`
    - `components/HowItWorks.tsx` contém `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (grid responsivo)
    - `components/HowItWorks.tsx` contém `bg-floresta text-offwhite` nos números circulares
    - `npm run build` não emite "error TS" após esta alteração
  </acceptance_criteria>
  <done>HowItWorks implementado com 4 steps, ícones corretos, animação stagger whileInView, responsividade mobile-first e paleta correta.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Features.tsx (6 cards) + Testimonials.tsx (3 depoimentos) com Framer Motion</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md — Seção "Features.tsx" (linhas 462-527), "Testimonials.tsx" (linhas 532-587)
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md — "Seção Funcionalidades" (linhas 163-173), "Seção Depoimentos" (linhas 175-180), "Copywriting Contract" (linhas 253-257)
  </read_first>
  <files>
    nutrichat-landing/components/Features.tsx,
    nutrichat-landing/components/Testimonials.tsx
  </files>
  <action>
**1. Substituir o stub de Features.tsx:**
```tsx
'use client'

import { motion } from 'framer-motion'
import { Image, Mic, Bell, FileText, Calculator, Repeat } from 'lucide-react'
import { fadeInUp, staggerContainer } from '@/lib/animations'

const FEATURES = [
  {
    Icon: Image,
    title: 'Registro por foto',
    desc: 'Paciente fotografa o prato e o agente identifica os alimentos e calcula os macros automaticamente.',
  },
  {
    Icon: Mic,
    title: 'Registro por áudio',
    desc: 'Envie uma mensagem de voz descrevendo a refeição. O NutriChat transcreve e registra tudo.',
  },
  {
    Icon: Bell,
    title: 'Alertas inteligentes',
    desc: 'Lembretes automáticos de refeições, hidratação e suplementos nos horários configurados.',
  },
  {
    Icon: FileText,
    title: 'Relatório semanal',
    desc: 'Toda semana, um resumo com kcal, macros, hidratação e dias que bateram a meta.',
  },
  {
    Icon: Calculator,
    title: 'Cálculo de macros',
    desc: 'TMB, hidratação diária (35ml/kg) e dose de creatina calculados com base nos dados do paciente.',
  },
  {
    Icon: Repeat,
    title: 'Substituição de alimentos',
    desc: 'Se um alimento estiver em falta, o agente sugere opções equivalentes da dieta prescrita.',
  },
]

export default function Features() {
  return (
    <section id="funcionalidades" className="py-16 bg-offwhite">
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8">
        <h2 className="text-floresta font-bold text-3xl text-center mb-4">
          Tudo que seu paciente precisa, no WhatsApp que ele já usa
        </h2>
        <p className="text-claro-br text-center mb-12">
          Funcionalidades pensadas para facilitar o acompanhamento nutricional do dia a dia.
        </p>
        <motion.div
          className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"
          variants={staggerContainer}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, amount: 0.2 }}
        >
          {FEATURES.map((feat, i) => (
            <motion.div
              key={i}
              variants={fadeInUp}
              className="bg-branco border border-menta rounded-xl p-6 flex flex-col gap-3 hover:-translate-y-1 transition-transform duration-200"
            >
              <feat.Icon size={28} className="text-medio" />
              <h3 className="font-bold text-terra">{feat.title}</h3>
              <p className="text-sm text-claro-br leading-relaxed">{feat.desc}</p>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
  )
}
```

**2. Substituir o stub de Testimonials.tsx:**
```tsx
'use client'

import { motion } from 'framer-motion'
import { fadeInUp, staggerContainer } from '@/lib/animations'

const TESTIMONIALS = [
  {
    name: 'Maria S., 32 anos',
    text: 'Em 3 semanas já perdi 2kg. O bot me lembra de tudo e ainda calcula o que eu como em segundos.',
  },
  {
    name: 'João P., 28 anos',
    text: 'Nunca consegui seguir uma dieta antes. Com o NutriChat ficou fácil porque é tudo pelo WhatsApp mesmo.',
  },
  {
    name: 'Ana C., 41 anos',
    text: 'Minha nutricionista consegue ver tudo que comi durante a semana. O acompanhamento melhorou muito.',
  },
]

export default function Testimonials() {
  return (
    <section id="depoimentos" className="py-16 bg-menta">
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8">
        <h2 className="text-floresta font-bold text-3xl text-center mb-4">
          O que nossos pacientes dizem
        </h2>
        <p className="text-claro-br text-center mb-12">
          Resultados reais de pessoas acompanhadas pelo NutriChat
        </p>
        <motion.div
          className="grid grid-cols-1 sm:grid-cols-3 gap-6"
          variants={staggerContainer}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, amount: 0.2 }}
        >
          {TESTIMONIALS.map((t, i) => (
            <motion.div
              key={i}
              variants={fadeInUp}
              className="bg-branco rounded-xl p-6 flex flex-col gap-4"
              style={{ boxShadow: '0 2px 12px rgba(45,80,22,0.08)' }}
            >
              {/* Avatar placeholder */}
              <div className="w-10 h-10 rounded-full bg-menta flex-shrink-0" />
              <p className="text-terra text-sm leading-relaxed flex-1">
                &ldquo;{t.text}&rdquo;
              </p>
              <span className="text-claro-br text-xs font-medium">{t.name}</span>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
  )
}
```

**3. Verificar build:**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build
```
  </action>
  <verify>
    <automated>cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build 2>&1 | grep -E "(error TS|Error|compiled|Build completed)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/Features.tsx` contém exatamente 6 itens em FEATURES array (Image, Mic, Bell, FileText, Calculator, Repeat — 6 ícones distintos)
    - `components/Features.tsx` contém `id="funcionalidades"` e `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
    - `components/Features.tsx` contém `from '@/lib/animations'`
    - `components/Testimonials.tsx` contém `bg-menta` na seção e `bg-branco` nos cards
    - `components/Testimonials.tsx` contém exatamente os 3 nomes: `"Maria S., 32 anos"` e `"João P., 28 anos"` e `"Ana C., 41 anos"`
    - `components/Testimonials.tsx` contém `id="depoimentos"`
    - `components/Testimonials.tsx` contém `from '@/lib/animations'`
    - `npm run build` termina sem "error TS" ou "Failed to compile"
  </acceptance_criteria>
  <done>Features com 6 cards de funcionalidades e Testimonials com 3 depoimentos placeholder implementados, ambos com animação fadeInUp stagger via lib/animations.ts, build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content → Browser DOM | Conteúdo estático renderizado no servidor via SSG; sem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Depoimentos placeholder | accept | Conteúdo é fictício e público por design; sem PII real |
| T-06-05 | Tampering | Componentes client-side Framer Motion | accept | Animações são puramente visuais; nenhum estado crítico envolvido |
</threat_model>

<verification>
Após execução:
1. `npm run build` verde — todas as 3 seções compilam corretamente
2. Inspecionar manualmente no browser: `npm run dev` → http://localhost:3000
   - Rolar até "#como-funciona": 4 cards devem aparecer com animação fadeInUp
   - Rolar até "#funcionalidades": 6 cards com ícones distintos
   - Rolar até "#depoimentos": fundo verde menta, 3 cards brancos com nomes e textos corretos
3. Responsividade: verificar em 375px (1 coluna), 768px (2 colunas) e 1280px (4/3 colunas conforme grid)
</verification>

<success_criteria>
- HowItWorks: 4 steps com ícones UserPlus/MessageCircle/Camera/BarChart2, números circulares bg-floresta, stagger whileInView
- Features: 6 cards com ícones corretos, grid responsivo 1→2→3 colunas
- Testimonials: bg seção bg-menta, 3 cards bg-branco com textos exatos do UI-SPEC
- Todos os 3 componentes importam de lib/animations.ts (sem duplicação de variantes)
- npm run build sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-landing-page/06-02-SUMMARY.md` com:
- Componentes implementados e número de itens em cada
- Decisões de implementação (ex: descrições dos cards que foram de "Claude's Discretion")
- Resultado do npm run build
</output>

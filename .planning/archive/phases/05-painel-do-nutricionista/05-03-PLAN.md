---
phase: 05-painel-do-nutricionista
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - nutrichat-painel/src/components/StatusBadge.tsx
  - nutrichat-painel/src/components/PacienteModal.tsx
  - nutrichat-painel/src/pages/Dashboard.tsx
  - nutrichat-painel/src/App.tsx
  - nutrichat-painel/Dockerfile
  - nutrichat-painel/Caddyfile
autonomous: true
requirements:
  - PANEL-04
  - PANEL-05
  - PANEL-06

must_haves:
  truths:
    - "Dashboard exibe lista de pacientes com nome, WhatsApp, plano, data de expiração e badge de status colorido"
    - "Badge verde para 'ativo', amarelo para 'expirando', vermelho para 'expirado'"
    - "Botão 'Novo Paciente' abre PacienteModal com formulário (nome, WhatsApp, plano, data, PDF upload)"
    - "Submeter o formulário faz POST /api/pacientes com FormData e exibe mensagem de confirmação"
    - "Clicar em paciente existente abre modal de edição pré-preenchido com PATCH /api/pacientes/:id"
    - "Botão ativar/desativar atualiza campo ativo via PATCH"
    - "Dockerfile multi-stage produz imagem que serve o dist via Caddy"
    - "Caddyfile tem try_files para SPA routing (refresh não retorna 404)"
  artifacts:
    - path: "nutrichat-painel/src/components/StatusBadge.tsx"
      provides: "Badge colorido baseado em prop status"
      contains: "bg-green-100"
    - path: "nutrichat-painel/src/components/PacienteModal.tsx"
      provides: "Modal de cadastro/edição com FormData upload"
      contains: "FormData"
    - path: "nutrichat-painel/src/pages/Dashboard.tsx"
      provides: "Página principal com lista + ações"
      contains: "StatusBadge"
    - path: "nutrichat-painel/Dockerfile"
      provides: "Multi-stage build Node + Caddy"
      contains: "caddy:2-alpine"
    - path: "nutrichat-painel/Caddyfile"
      provides: "SPA routing fallback"
      contains: "try_files {path} /index.html"
  key_links:
    - from: "Dashboard.tsx"
      to: "backend GET /api/pacientes"
      via: "fetch com X-API-Key header"
      pattern: "VITE_BACKEND_URL.*api/pacientes"
    - from: "PacienteModal.tsx"
      to: "backend POST /api/pacientes"
      via: "FormData + fetch multipart"
      pattern: "FormData"
    - from: "App.tsx"
      to: "Dashboard.tsx"
      via: "import substituindo DashboardPlaceholder"
      pattern: "import.*Dashboard"
---

<objective>
Construir a UI completa do Dashboard (lista de pacientes + PacienteModal de cadastro/edição + StatusBadge), substituir o DashboardPlaceholder em App.tsx pelo Dashboard real, e criar Dockerfile multi-stage + Caddyfile para deploy no Railway.

Purpose: Entrega o painel visualmente funcional e pronto para deploy. Depende de 05-01 (scaffold + auth) e 05-02 (rotas backend).
Output: Dashboard operacional em localhost:5173 conectado ao backend em localhost:3001; imagem Docker buildável para Railway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-painel-do-nutricionista/05-CONTEXT.md
@.planning/phases/05-painel-do-nutricionista/05-RESEARCH.md
@.planning/phases/05-painel-do-nutricionista/05-01-SUMMARY.md
@.planning/phases/05-painel-do-nutricionista/05-02-SUMMARY.md

<interfaces>
<!-- Tipos que o Dashboard precisa — inferidos das rotas do backend (05-02) -->
```typescript
// Tipo retornado por GET /api/pacientes
interface Paciente {
  id: string;
  nome: string;
  whatsapp: string;
  plano: string;           // '1mes' | '3meses' | '6meses' | '12meses'
  data_expiracao: string;  // ISO date string
  ativo: boolean;
  created_at: string;
  status: 'ativo' | 'expirando' | 'expirado';  // calculado pelo backend
}

// PATCH /api/pacientes/:id — body JSON
interface PacientePatch {
  ativo?: boolean;
  data_expiracao?: string;
}

// POST /api/pacientes — multipart/form-data
// Campos: nome (string), whatsapp (string), plano (string), data_expiracao (string), dieta (File PDF)
```

<!-- Variáveis de ambiente disponíveis no frontend (05-01) -->
// import.meta.env.VITE_BACKEND_URL    → 'http://localhost:3001' em dev
// import.meta.env.VITE_PANEL_API_KEY  → chave do X-API-Key header

<!-- Paleta NutriChat (D-04) -->
// Header background: #2D5016 (floresta)
// Page background:   #F0F7EC (offwhite)
// Typography:        #6B3D1E (terra)

<!-- Lógica de badge (CONTEXT.md Specifics + backend calcularStatus) -->
// 'ativo'     → bg-green-100  text-green-800
// 'expirando' → bg-yellow-100 text-yellow-800
// 'expirado'  → bg-red-100    text-red-800
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Backend /api/pacientes | X-API-Key em cada request — token não é JWT do usuário, é chave compartilhada |
| PacienteModal → FormData | PDF lido do filesystem do usuário e enviado ao backend — nunca processado no frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | VITE_PANEL_API_KEY no bundle | accept | Chave exposta no bundle Vite é aceitável para painel interno single-tenant; não é segredo público de alto valor; mitigação: chave diferente por ambiente (dev/prod) |
| T-05-03-02 | Tampering | PacienteModal — upload de arquivo | mitigate | Backend já valida mimetype via multer fileFilter; frontend adiciona `accept=".pdf"` no input para orientar o usuário (não é controle de segurança — é UX) |
| T-05-03-03 | Spoofing | X-API-Key em código frontend | accept | Para v1 interno, risco aceitável; em v2 considerar proxy BFF; documentado como limitação conhecida |
| T-05-03-04 | Denial of Service | Upload de PDF grande no frontend | mitigate | Input `accept=".pdf"` + mensagem de erro se backend retorna 400/413; sem validação de tamanho no frontend (backend já rejeita >50MB) |
</threat_model>

<tasks>

<task type="auto">
  <id>05-03-T1</id>
  <title>Criar StatusBadge + PacienteModal + Dashboard (lista + ações)</title>
  <read_first>
    - nutrichat-painel/src/App.tsx (para ver DashboardPlaceholder a substituir)
    - nutrichat-painel/src/lib/supabase.ts (para referência — importar supabase apenas em App.tsx)
    - .planning/phases/05-painel-do-nutricionista/05-CONTEXT.md (D-04 paleta, D-05 SPA, D-06 Tailwind puro, D-07 FormData, D-08 mensagem feedback)
    - .planning/phases/05-painel-do-nutricionista/05-RESEARCH.md (Pattern 3 FormData, paleta, badge logic, estrutura de pastas recomendada)
  </read_first>
  <action>
**Arquivo 1: nutrichat-painel/src/components/StatusBadge.tsx**

```tsx
interface StatusBadgeProps {
  status: 'ativo' | 'expirando' | 'expirado';
}

const badgeConfig = {
  ativo: {
    label: 'Ativo',
    classes: 'bg-green-100 text-green-800',
  },
  expirando: {
    label: 'Expirando',
    classes: 'bg-yellow-100 text-yellow-800',
  },
  expirado: {
    label: 'Expirado',
    classes: 'bg-red-100 text-red-800',
  },
};

export function StatusBadge({ status }: StatusBadgeProps) {
  const { label, classes } = badgeConfig[status];
  return (
    <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${classes}`}>
      {label}
    </span>
  );
}
```

**Arquivo 2: nutrichat-painel/src/components/PacienteModal.tsx**

```tsx
import { useState } from 'react';

interface Paciente {
  id: string;
  nome: string;
  whatsapp: string;
  plano: string;
  data_expiracao: string;
  ativo: boolean;
  status: 'ativo' | 'expirando' | 'expirado';
}

interface PacienteModalProps {
  paciente?: Paciente;           // undefined = modo cadastro; definido = modo edição
  onClose: () => void;
  onSaved: () => void;           // callback para recarregar a lista
}

const BACKEND_URL = import.meta.env.VITE_BACKEND_URL as string;
const API_KEY = import.meta.env.VITE_PANEL_API_KEY as string;

export function PacienteModal({ paciente, onClose, onSaved }: PacienteModalProps) {
  const isEdicao = !!paciente;

  const [nome, setNome] = useState(paciente?.nome ?? '');
  const [whatsapp, setWhatsapp] = useState(paciente?.whatsapp ?? '');
  const [plano, setPlano] = useState(paciente?.plano ?? '1mes');
  const [dataExpiracao, setDataExpiracao] = useState(
    paciente?.data_expiracao ? paciente.data_expiracao.slice(0, 10) : ''
  );
  const [ativo, setAtivo] = useState(paciente?.ativo ?? true);
  const [pdf, setPdf] = useState<File | null>(null);
  const [loading, setLoading] = useState(false);
  const [erro, setErro] = useState('');
  const [sucesso, setSucesso] = useState('');

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setErro('');
    setSucesso('');

    try {
      if (isEdicao) {
        // PATCH — atualizar ativo e/ou data_expiracao
        const body: { ativo?: boolean; data_expiracao?: string } = {};
        body.ativo = ativo;
        if (dataExpiracao) body.data_expiracao = dataExpiracao;

        const res = await fetch(`${BACKEND_URL}/api/pacientes/${paciente.id}`, {
          method: 'PATCH',
          headers: {
            'Content-Type': 'application/json',
            'X-API-Key': API_KEY,
          },
          body: JSON.stringify(body),
        });
        if (!res.ok) {
          const err = await res.json().catch(() => ({}));
          throw new Error((err as { error?: string }).error ?? 'Erro ao atualizar paciente');
        }
        setSucesso('Paciente atualizado com sucesso.');
      } else {
        // POST — cadastrar novo paciente com PDF
        if (!pdf) {
          setErro('Selecione o PDF da dieta.');
          setLoading(false);
          return;
        }
        const form = new FormData();
        form.append('nome', nome);
        form.append('whatsapp', whatsapp);
        form.append('plano', plano);
        form.append('data_expiracao', dataExpiracao);
        form.append('dieta', pdf);
        // NÃO setar Content-Type — fetch define boundary do multipart automaticamente

        const res = await fetch(`${BACKEND_URL}/api/pacientes`, {
          method: 'POST',
          headers: { 'X-API-Key': API_KEY },
          body: form,
        });
        if (!res.ok) {
          const err = await res.json().catch(() => ({}));
          throw new Error((err as { error?: string }).error ?? 'Erro ao cadastrar paciente');
        }
        setSucesso(`✅ ${nome} cadastrado — dieta sendo processada em segundo plano.`);
      }

      setTimeout(() => {
        onSaved();
        onClose();
      }, 1500);
    } catch (err) {
      setErro(err instanceof Error ? err.message : 'Erro desconhecido');
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="fixed inset-0 flex items-center justify-center z-50" style={{ background: 'rgba(0,0,0,0.4)' }}>
      <div className="bg-white rounded-xl shadow-2xl p-6 w-full max-w-md mx-4">
        <div className="flex justify-between items-center mb-4">
          <h2 className="text-lg font-bold" style={{ color: '#6B3D1E' }}>
            {isEdicao ? 'Editar Paciente' : 'Novo Paciente'}
          </h2>
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600 text-xl cursor-pointer">×</button>
        </div>

        <form onSubmit={handleSubmit} className="space-y-3">
          {!isEdicao && (
            <>
              <div>
                <label className="block text-sm font-medium mb-1" style={{ color: '#6B3D1E' }}>Nome</label>
                <input
                  type="text"
                  value={nome}
                  onChange={(e) => setNome(e.target.value)}
                  required
                  className="w-full border border-gray-300 rounded px-3 py-2"
                  placeholder="Nome completo"
                />
              </div>
              <div>
                <label className="block text-sm font-medium mb-1" style={{ color: '#6B3D1E' }}>WhatsApp</label>
                <input
                  type="text"
                  value={whatsapp}
                  onChange={(e) => setWhatsapp(e.target.value)}
                  required
                  className="w-full border border-gray-300 rounded px-3 py-2"
                  placeholder="5562999999999"
                />
              </div>
              <div>
                <label className="block text-sm font-medium mb-1" style={{ color: '#6B3D1E' }}>Plano</label>
                <select
                  value={plano}
                  onChange={(e) => setPlano(e.target.value)}
                  className="w-full border border-gray-300 rounded px-3 py-2"
                >
                  <option value="1mes">1 mês</option>
                  <option value="3meses">3 meses</option>
                  <option value="6meses">6 meses</option>
                  <option value="12meses">12 meses</option>
                </select>
              </div>
            </>
          )}

          <div>
            <label className="block text-sm font-medium mb-1" style={{ color: '#6B3D1E' }}>Data de Expiração</label>
            <input
              type="date"
              value={dataExpiracao}
              onChange={(e) => setDataExpiracao(e.target.value)}
              required
              className="w-full border border-gray-300 rounded px-3 py-2"
            />
          </div>

          {isEdicao && (
            <div className="flex items-center gap-2">
              <input
                type="checkbox"
                id="ativo"
                checked={ativo}
                onChange={(e) => setAtivo(e.target.checked)}
                className="w-4 h-4"
              />
              <label htmlFor="ativo" className="text-sm font-medium" style={{ color: '#6B3D1E' }}>
                Acesso ativo
              </label>
            </div>
          )}

          {!isEdicao && (
            <div>
              <label className="block text-sm font-medium mb-1" style={{ color: '#6B3D1E' }}>
                Dieta (PDF)
              </label>
              <input
                type="file"
                accept=".pdf"
                onChange={(e) => setPdf(e.target.files?.[0] ?? null)}
                required
                className="w-full text-sm text-gray-500 file:mr-3 file:py-1 file:px-3 file:rounded file:border-0 file:text-sm file:font-medium file:text-white"
                style={{ '--file-bg': '#2D5016' } as React.CSSProperties}
              />
              {pdf && <p className="text-xs text-gray-500 mt-1">📄 {pdf.name}</p>}
            </div>
          )}

          {erro && <p className="text-red-600 text-sm">{erro}</p>}
          {sucesso && <p className="text-green-700 text-sm">{sucesso}</p>}

          <div className="flex gap-2 pt-2">
            <button
              type="button"
              onClick={onClose}
              className="flex-1 py-2 rounded border border-gray-300 text-sm text-gray-600 hover:bg-gray-50 cursor-pointer"
            >
              Cancelar
            </button>
            <button
              type="submit"
              disabled={loading}
              className="flex-1 py-2 rounded text-white text-sm font-semibold disabled:opacity-60 cursor-pointer"
              style={{ background: '#2D5016' }}
            >
              {loading ? 'Salvando...' : isEdicao ? 'Salvar' : 'Cadastrar'}
            </button>
          </div>
        </form>
      </div>
    </div>
  );
}
```

**Arquivo 3: nutrichat-painel/src/pages/Dashboard.tsx**

```tsx
import { useState, useEffect, useCallback } from 'react';
import { supabase } from '../lib/supabase';
import { StatusBadge } from '../components/StatusBadge';
import { PacienteModal } from '../components/PacienteModal';

interface Paciente {
  id: string;
  nome: string;
  whatsapp: string;
  plano: string;
  data_expiracao: string;
  ativo: boolean;
  status: 'ativo' | 'expirando' | 'expirado';
}

const BACKEND_URL = import.meta.env.VITE_BACKEND_URL as string;
const API_KEY = import.meta.env.VITE_PANEL_API_KEY as string;

export function Dashboard() {
  const [pacientes, setPacientes] = useState<Paciente[]>([]);
  const [carregando, setCarregando] = useState(true);
  const [modalAberto, setModalAberto] = useState(false);
  const [pacienteSelecionado, setPacienteSelecionado] = useState<Paciente | undefined>(undefined);

  const carregarPacientes = useCallback(async () => {
    setCarregando(true);
    try {
      const res = await fetch(`${BACKEND_URL}/api/pacientes`, {
        headers: { 'X-API-Key': API_KEY },
      });
      if (!res.ok) throw new Error('Erro ao carregar pacientes');
      const data = (await res.json()) as Paciente[];
      setPacientes(data);
    } catch (err) {
      console.error('[dashboard] Erro ao carregar pacientes:', err);
    } finally {
      setCarregando(false);
    }
  }, []);

  useEffect(() => {
    carregarPacientes();
  }, [carregarPacientes]);

  function abrirNovoPaciente() {
    setPacienteSelecionado(undefined);
    setModalAberto(true);
  }

  function abrirEdicao(paciente: Paciente) {
    setPacienteSelecionado(paciente);
    setModalAberto(true);
  }

  function fecharModal() {
    setModalAberto(false);
    setPacienteSelecionado(undefined);
  }

  function formatarData(dateStr: string) {
    return new Date(dateStr).toLocaleDateString('pt-BR');
  }

  const planoLabels: Record<string, string> = {
    '1mes': '1 mês',
    '3meses': '3 meses',
    '6meses': '6 meses',
    '12meses': '12 meses',
  };

  return (
    <div style={{ background: '#F0F7EC', minHeight: '100vh' }}>
      {/* Header */}
      <header
        className="px-6 py-4 flex items-center justify-between text-white"
        style={{ background: '#2D5016' }}
      >
        <span className="font-bold text-lg">🥗 NutriChat — Painel</span>
        <button
          onClick={() => supabase.auth.signOut()}
          className="text-sm underline opacity-80 hover:opacity-100 cursor-pointer"
        >
          Sair
        </button>
      </header>

      {/* Conteúdo */}
      <main className="p-6 max-w-5xl mx-auto">
        <div className="flex justify-between items-center mb-6">
          <h1 className="text-xl font-bold" style={{ color: '#6B3D1E' }}>Pacientes</h1>
          <button
            onClick={abrirNovoPaciente}
            className="px-4 py-2 rounded text-white text-sm font-semibold cursor-pointer"
            style={{ background: '#2D5016' }}
          >
            + Novo Paciente
          </button>
        </div>

        {carregando ? (
          <p className="text-center py-10" style={{ color: '#6B3D1E' }}>Carregando pacientes...</p>
        ) : pacientes.length === 0 ? (
          <div className="text-center py-16 bg-white rounded-xl shadow-sm">
            <p className="text-lg mb-2" style={{ color: '#6B3D1E' }}>Nenhum paciente cadastrado</p>
            <p className="text-sm text-gray-500">Clique em "+ Novo Paciente" para começar</p>
          </div>
        ) : (
          <div className="bg-white rounded-xl shadow-sm overflow-hidden">
            <table className="w-full text-sm">
              <thead>
                <tr className="border-b border-gray-100">
                  <th className="text-left px-4 py-3 font-semibold" style={{ color: '#6B3D1E' }}>Nome</th>
                  <th className="text-left px-4 py-3 font-semibold" style={{ color: '#6B3D1E' }}>WhatsApp</th>
                  <th className="text-left px-4 py-3 font-semibold" style={{ color: '#6B3D1E' }}>Plano</th>
                  <th className="text-left px-4 py-3 font-semibold" style={{ color: '#6B3D1E' }}>Expiração</th>
                  <th className="text-left px-4 py-3 font-semibold" style={{ color: '#6B3D1E' }}>Status</th>
                  <th className="px-4 py-3"></th>
                </tr>
              </thead>
              <tbody>
                {pacientes.map((p) => (
                  <tr key={p.id} className="border-b border-gray-50 hover:bg-gray-50 transition-colors">
                    <td className="px-4 py-3 font-medium" style={{ color: '#6B3D1E' }}>{p.nome}</td>
                    <td className="px-4 py-3 text-gray-600">{p.whatsapp}</td>
                    <td className="px-4 py-3 text-gray-600">{planoLabels[p.plano] ?? p.plano}</td>
                    <td className="px-4 py-3 text-gray-600">{formatarData(p.data_expiracao)}</td>
                    <td className="px-4 py-3"><StatusBadge status={p.status} /></td>
                    <td className="px-4 py-3 text-right">
                      <button
                        onClick={() => abrirEdicao(p)}
                        className="text-xs underline cursor-pointer"
                        style={{ color: '#2D5016' }}
                      >
                        Editar
                      </button>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        )}
      </main>

      {/* Modal */}
      {modalAberto && (
        <PacienteModal
          paciente={pacienteSelecionado}
          onClose={fecharModal}
          onSaved={carregarPacientes}
        />
      )}
    </div>
  );
}
```

**Editar nutrichat-painel/src/App.tsx** — substituir o `DashboardPlaceholder` pelo componente `Dashboard` real:

Remover a função `DashboardPlaceholder` e o import inline. Adicionar import do Dashboard:
```tsx
import { useState, useEffect } from 'react';
import { Session } from '@supabase/supabase-js';
import { supabase } from './lib/supabase';
import { LoginForm } from './components/LoginForm';
import { Dashboard } from './pages/Dashboard';

export function App() {
  const [session, setSession] = useState<Session | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      setSession(session);
      setLoading(false);
    });

    const {
      data: { subscription },
    } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session);
    });

    return () => subscription.unsubscribe();
  }, []);

  if (loading) {
    return (
      <div
        className="min-h-screen flex items-center justify-center"
        style={{ background: '#F0F7EC' }}
      >
        <p style={{ color: '#6B3D1E' }}>Carregando...</p>
      </div>
    );
  }

  return session ? <Dashboard /> : <LoginForm />;
}
```
  </action>
  <acceptance_criteria>
    - nutrichat-painel/src/components/StatusBadge.tsx contém `bg-green-100` e `bg-yellow-100` e `bg-red-100`
    - nutrichat-painel/src/components/PacienteModal.tsx contém `FormData`
    - nutrichat-painel/src/components/PacienteModal.tsx contém `X-API-Key`
    - nutrichat-painel/src/components/PacienteModal.tsx contém `PATCH` (modo edição)
    - nutrichat-painel/src/pages/Dashboard.tsx contém `StatusBadge`
    - nutrichat-painel/src/pages/Dashboard.tsx contém `PacienteModal`
    - nutrichat-painel/src/App.tsx contém `import { Dashboard }` (sem DashboardPlaceholder)
    - `cd nutrichat-painel && npx tsc --noEmit` sai com código 0
    - `cd nutrichat-painel && npm run build` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/botel/OneDrive/Desktop/nutri_fit/nutrichat-painel && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Dashboard mostra tabela de pacientes com badges; PacienteModal permite cadastrar (POST multipart) e editar (PATCH JSON); StatusBadge exibe verde/amarelo/vermelho; App.tsx usa Dashboard real; TypeScript sem erros.</done>
</task>

<task type="auto">
  <id>05-03-T2</id>
  <title>Dockerfile multi-stage + Caddyfile para deploy Railway</title>
  <read_first>
    - .planning/phases/05-painel-do-nutricionista/05-RESEARCH.md (Pattern 8 — Dockerfile + Caddyfile completos, seção Deploy Railway)
    - .planning/phases/05-painel-do-nutricionista/05-CONTEXT.md (D-01 deploy Railway, D-02 porta 5173 em dev)
  </read_first>
  <action>
**Arquivo 1: nutrichat-painel/Dockerfile**

```dockerfile
# Stage 1: Build com Node
FROM node:22-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
# Variáveis VITE_ são build-time — Railway injeta como build args ou env vars
RUN npm run build

# Stage 2: Servir com Caddy (SPA routing)
FROM caddy:2-alpine
COPY --from=build /app/dist ./dist
COPY Caddyfile /etc/caddy/Caddyfile
EXPOSE 80
```

**Arquivo 2: nutrichat-painel/Caddyfile**

```
:80 {
    root * dist
    file_server
    try_files {path} /index.html
    encode gzip
    auto_https off
    trusted_proxies static private_ranges 100.0.0.0/8
}
```

**Observação importante para deploy no Railway:**

As variáveis `VITE_*` são injetadas em build-time pelo Vite (não em runtime). No Railway, configurar as variáveis de ambiente **antes** do build:
- `VITE_SUPABASE_URL` = `https://iqpoohthfhmjkvrqxzns.supabase.co`
- `VITE_SUPABASE_ANON_KEY` = (valor do Supabase Dashboard)
- `VITE_BACKEND_URL` = (URL do backend no Railway, ex: `https://nutrichat-backend-prod.up.railway.app`)
- `VITE_PANEL_API_KEY` = (mesmo valor de PANEL_API_KEY no backend)

Criar `nutrichat-painel/.dockerignore` para otimizar o build:
```
node_modules
dist
.env.local
.env
*.md
```
  </action>
  <acceptance_criteria>
    - nutrichat-painel/Dockerfile existe e contém `caddy:2-alpine`
    - nutrichat-painel/Dockerfile contém `FROM node:22-alpine AS build`
    - nutrichat-painel/Dockerfile contém `npm run build`
    - nutrichat-painel/Caddyfile existe e contém `try_files {path} /index.html`
    - nutrichat-painel/Caddyfile contém `auto_https off`
    - nutrichat-painel/.dockerignore existe e contém `node_modules`
    - `cd nutrichat-painel && docker build -t nutrichat-painel-test .` sai com código 0 (se Docker disponível)
  </acceptance_criteria>
  <verify>
    <automated>test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/nutrichat-painel/Dockerfile && test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/nutrichat-painel/Caddyfile && echo "OK: Dockerfile e Caddyfile existem"</automated>
  </verify>
  <done>Dockerfile multi-stage (Node build + Caddy serve) criado; Caddyfile com try_files para SPA routing; .dockerignore criado; imagem buildável para Railway.</done>
</task>

</tasks>

<verification>
**Integração completa (executar após os dois tasks deste plano):**

1. `cd nutrichat-painel && npm run build` — zero erros TypeScript e Vite
2. `npx tsc --noEmit` — zero erros de tipo
3. `npm run dev` — servidor em localhost:5173
4. Browser em localhost:5173 → LoginForm exibido (fundo #F0F7EC)
5. Login com usuário válido do Supabase Auth → Dashboard exibido com tabela de pacientes
6. `curl -s http://localhost:3001/api/pacientes -H "X-API-Key: nutrichat_panel_key_dev"` — retorna JSON (pode ser array vazio)
7. Dashboard carrega a lista (mesmo que vazia) sem erros de console
8. "+ Novo Paciente" abre PacienteModal com campos: nome, WhatsApp, plano, data, PDF
9. Clicar "Editar" em paciente existente abre modal pré-preenchido
10. `grep "try_files" nutrichat-painel/Caddyfile` — retorna a linha try_files
11. `grep "caddy:2-alpine" nutrichat-painel/Dockerfile` — retorna a linha FROM caddy

**Teste de cadastro manual (requer backend rodando):**
```bash
curl -s -X POST http://localhost:3001/api/pacientes \
  -H "X-API-Key: nutrichat_panel_key_dev" \
  -F "nome=Teste Fase5" \
  -F "whatsapp=5562900000001" \
  -F "plano=1mes" \
  -F "data_expiracao=2026-12-31" \
  -F "dieta=@/path/to/test.pdf"
```
Deve retornar `{"sucesso":true,"paciente_id":"..."}`.
</verification>

<success_criteria>
- StatusBadge exibe verde/amarelo/vermelho conforme status recebido do backend (PANEL-04)
- Dashboard lista todos os pacientes com todas as colunas (nome, WhatsApp, plano, expiração, status) (PANEL-04)
- PacienteModal no modo cadastro envia POST multipart com PDF para /api/pacientes (PANEL-01, PANEL-02, PANEL-03)
- PacienteModal no modo edição envia PATCH JSON com ativo e/ou data_expiracao (PANEL-05, PANEL-06)
- App.tsx usa Dashboard real (sem DashboardPlaceholder)
- Dockerfile multi-stage buildável para Railway (D-01)
- Caddyfile com try_files garante que refresh na raiz não retorna 404
- TypeScript sem erros em build e typecheck
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-painel-do-nutricionista/05-03-SUMMARY.md` com:
- Resultado do npm run build
- Screenshot ou descrição do Dashboard renderizado
- Confirmação dos arquivos de deploy (Dockerfile, Caddyfile)
- Resultado do teste de integração (curl POST ou cadastro via UI)
- Qualquer desvio do plano e justificativa
</output>

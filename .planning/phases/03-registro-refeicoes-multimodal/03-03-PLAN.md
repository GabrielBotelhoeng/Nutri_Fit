---
phase: 03-registro-refeicoes-multimodal
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-04"
files_modified:
  - backend/src/services/meal.ts
  - backend/src/services/agent.ts
autonomous: true
requirements:
  - AGENT-13
  - AGENT-15
  - AGENT-16

must_haves:
  truths:
    - "Paciente envia mensagem de texto descrevendo refeição e recebe saldo do dia formatado"
    - "O saldo exibe kcal + proteína + carbo + gordura consumidos vs meta (D-07)"
    - "Meta de kcal vem de tdee_kcal salvo em entrevista_dados (D-08)"
    - "O saldo acumula corretamente — dois registros somam, não sobrescrevem (usa RPC acumular_registro_diario)"
    - "Paciente pode pedir substituição de alimento e agente sugere apenas itens da dieta prescrita via RAG (D-09)"
    - "agent.ts detecta intenção: quando status completa, distingue registro de refeição vs consulta sobre dieta"
  artifacts:
    - path: "backend/src/services/meal.ts"
      provides: "processarTextoRefeicao, calcularMacrosComClaude, registrarRefeicao, obterSaldoDia, formatarSaldoDia, sugerirSubstituicao"
      exports: ["processarTextoRefeicao", "sugerirSubstituicao"]
      min_lines: 120
    - path: "backend/src/services/agent.ts"
      provides: "processarMensagem expandido para modo agente com registro de refeições"
      contains: "processarTextoRefeicao"
  key_links:
    - from: "backend/src/services/agent.ts processarMensagem()"
      to: "backend/src/services/meal.ts processarTextoRefeicao()"
      via: "await mealService.processarTextoRefeicao(phone, texto, paciente)"
      pattern: "processarTextoRefeicao"
    - from: "backend/src/services/meal.ts registrarRefeicao()"
      to: "supabase.rpc('acumular_registro_diario')"
      via: "await supabase.rpc('acumular_registro_diario', params)"
      pattern: "acumular_registro_diario"
    - from: "backend/src/services/meal.ts sugerirSubstituicao()"
      to: "backend/src/services/rag.ts query()"
      via: "await ragQuery(pacienteId, `substituto para ${alimentoAusente}`)"
      pattern: "ragQuery"
---

<objective>
Criar o serviço meal.ts que implementa o registro de refeições por texto, cálculo de macros via Claude, persistência acumulativa via RPC e exibição do saldo do dia. Expandir agent.ts para chamar esse fluxo quando o paciente está no modo agente.

Purpose: Este é o núcleo funcional da Fase 3. Todo registro (texto, áudio após transcrição, foto após análise) passa por `processarTextoRefeicao`. O saldo do dia é exibido após cada registro.

Output: `backend/src/services/meal.ts` com 6 funções exportadas; `backend/src/services/agent.ts` expandido no bloco `status === 'completa'` para distinguir registro de refeição de consulta sobre dieta.
</objective>

<execution_context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/PROJECT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/STATE.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/03-registro-refeicoes-multimodal/03-04-SUMMARY.md

<interfaces>
<!-- Contratos que meal.ts consome e expõe -->

De backend/src/services/agent.ts (padrão de chamada ao Claude — reutilizar):
```typescript
// claude client já instanciado
const claude = new Anthropic({ apiKey: env.CLAUDE_API_KEY });

// Padrão de chamada ao Claude com JSON output
const response = await claude.messages.create({
  model: 'claude-sonnet-4-6',
  max_tokens: 512,
  messages: [{ role: 'user', content: prompt }],
});
const texto = response.content[0].type === 'text' ? response.content[0].text : '';
```

De backend/src/services/conversation.ts (a importar em meal.ts):
```typescript
export interface PacienteInfo {
  id: string;
  nome: string;
  whatsapp: string;
  ativo: boolean;
  data_expiracao: string;
  entrevista_status: string;
  entrevista_etapa: number;
  entrevista_dados: Record<string, unknown>;
}
export async function getEstado(pacienteId: string): Promise<EstadoEntrevista>
export interface EstadoEntrevista {
  dados: {
    tdee_kcal?: number;  // ← meta de kcal diária calculada na Fase 2
    // outros campos de entrevista...
  };
}
```

De backend/src/services/rag.ts (a importar como ragQuery):
```typescript
export async function query(pacienteId: string, pergunta: string): Promise<string>
```

De backend/src/services/evolution.ts:
```typescript
export async function sendText(to: string, text: string): Promise<void>
```

De supabase/migrations/20260422000001_create_schema.sql (tabela refeicoes):
```sql
CREATE TABLE IF NOT EXISTS refeicoes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE,
  descricao TEXT NOT NULL,
  kcal NUMERIC(8,2),
  proteina_g NUMERIC(8,2),
  carbo_g NUMERIC(8,2),
  gordura_g NUMERIC(8,2),
  tipo_registro TEXT CHECK (tipo_registro IN ('texto', 'audio', 'foto', 'codigo_barras', 'rotulo')),
  registrado_em TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

RPC criada no plano 03-04:
```typescript
await supabase.rpc('acumular_registro_diario', {
  p_paciente_id: pacienteId,
  p_data: hoje,           // 'YYYY-MM-DD'
  p_kcal: macros.kcal,
  p_proteina_g: macros.proteina_g,
  p_carbo_g: macros.carbo_g,
  p_gordura_g: macros.gordura_g,
});
```

Formato do saldo diário (D-07 — DEVE seguir exatamente):
```
✅ Registrado: [descrição] (X kcal)

📊 Saldo do dia:
• Kcal: X / Y kcal
• Proteína: Xg / Yg
• Carbo: Xg / Yg
• Gordura: Xg / Yg
```

Distribuição de macros da meta (D-08):
```typescript
function calcularMetas(tdeeKcal: number) {
  return {
    kcal: tdeeKcal,
    proteina_g: Math.round((tdeeKcal * 0.30) / 4),  // 30% proteína, 4 kcal/g
    carbo_g:    Math.round((tdeeKcal * 0.40) / 4),  // 40% carbo, 4 kcal/g
    gordura_g:  Math.round((tdeeKcal * 0.30) / 9),  // 30% gordura, 9 kcal/g
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar backend/src/services/meal.ts</name>
  <read_first>
    - backend/src/services/agent.ts — padrão de instanciação do Anthropic client, padrão de chamada ao claude.messages.create, padrão de importações
    - backend/src/services/conversation.ts — interface EstadoEntrevista (campo dados.tdee_kcal), interface PacienteInfo, assinatura de getEstado()
    - backend/src/services/rag.ts — assinatura de query(pacienteId, pergunta)
    - backend/src/services/evolution.ts — assinatura de sendText()
    - .planning/phases/03-registro-refeicoes-multimodal/03-RESEARCH.md — Pattern 7 (registro), Pattern 8 (metas), Pattern 9 (substituição), Pitfall 1 (UPSERT), Pitfall 3 (JSON inválido)
    - .planning/phases/03-registro-refeicoes-multimodal/03-CONTEXT.md — D-01, D-02, D-07, D-08, D-09
  </read_first>
  <files>backend/src/services/meal.ts</files>
  <action>
Criar `backend/src/services/meal.ts` com as seguintes funções. Implementar na ordem abaixo (dependência de baixo para cima):

**Interface e tipos:**
```typescript
export interface MacrosRefeicao {
  kcal: number;
  proteina_g: number;
  carbo_g: number;
  gordura_g: number;
}

interface MetasMacros extends MacrosRefeicao {}
```

**extrairJSON(texto): unknown** — helper para Pitfall 3:
```typescript
function extrairJSON(texto: string): unknown {
  const limpo = texto.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim();
  return JSON.parse(limpo);
}
```

**sanitizarMacros(m): MacrosRefeicao** — helper para Pitfall T-03-03-01:
```typescript
function sanitizarMacros(m: MacrosRefeicao): MacrosRefeicao {
  const san = (v: number) => (isNaN(v) || v < 0 ? 0 : Math.min(v, 9999));
  return { kcal: san(m.kcal), proteina_g: san(m.proteina_g), carbo_g: san(m.carbo_g), gordura_g: san(m.gordura_g) };
}
```

**calcularMetas(tdeeKcal): MetasMacros** — implementar com D-08:
```typescript
function calcularMetas(tdeeKcal: number): MetasMacros {
  return {
    kcal: tdeeKcal,
    proteina_g: Math.round((tdeeKcal * 0.30) / 4),
    carbo_g:    Math.round((tdeeKcal * 0.40) / 4),
    gordura_g:  Math.round((tdeeKcal * 0.30) / 9),
  };
}
```

**calcularMacrosComClaude(descricao): Promise<MacrosRefeicao>** — prompt para Claude estimar macros (D-01, D-02):
```typescript
export async function calcularMacrosComClaude(descricao: string): Promise<MacrosRefeicao> {
  const prompt = `Você é um assistente nutricional. Estime os macronutrientes da refeição descrita.

Refeição: "${descricao}"

RESPONDA APENAS COM JSON VÁLIDO no formato:
{"kcal": number, "proteina_g": number, "carbo_g": number, "gordura_g": number}

- Use estimativas razoáveis para a porção típica brasileira
- Se a quantidade não for especificada, assuma porção média
- Não inclua comentários ou markdown
- Apenas JSON puro`;

  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 256,
    messages: [{ role: 'user', content: prompt }],
  });

  const texto = response.content[0].type === 'text' ? response.content[0].text : '{}';
  try {
    const raw = extrairJSON(texto) as Record<string, number>;
    return sanitizarMacros({
      kcal:       Number(raw['kcal'])       || 0,
      proteina_g: Number(raw['proteina_g']) || 0,
      carbo_g:    Number(raw['carbo_g'])    || 0,
      gordura_g:  Number(raw['gordura_g'])  || 0,
    });
  } catch {
    console.error('[meal] Claude retornou JSON inválido para macros:', texto);
    // D-02: registrar com aviso em vez de bloquear
    return { kcal: 0, proteina_g: 0, carbo_g: 0, gordura_g: 0 };
  }
}
```

**registrarRefeicao(pacienteId, descricao, macros, tipoRegistro): Promise<void>** — INSERT + RPC acumulação:
```typescript
export async function registrarRefeicao(
  pacienteId: string,
  descricao: string,
  macros: MacrosRefeicao,
  tipoRegistro: 'texto' | 'audio' | 'foto' | 'codigo_barras' | 'rotulo',
): Promise<void> {
  const m = sanitizarMacros(macros);
  const hoje = new Date().toISOString().slice(0, 10);

  // INSERT em refeicoes (histórico)
  const { error: insertError } = await supabase.from('refeicoes').insert({
    paciente_id: pacienteId,
    descricao,
    kcal: m.kcal,
    proteina_g: m.proteina_g,
    carbo_g: m.carbo_g,
    gordura_g: m.gordura_g,
    tipo_registro: tipoRegistro,
  });
  if (insertError) throw new Error(`[meal] Falha ao inserir refeição: ${insertError.message}`);

  // RPC acumulação incremental (plano 03-04) — NUNCA usar upsert direto
  const { error: rpcError } = await supabase.rpc('acumular_registro_diario', {
    p_paciente_id: pacienteId,
    p_data: hoje,
    p_kcal: m.kcal,
    p_proteina_g: m.proteina_g,
    p_carbo_g: m.carbo_g,
    p_gordura_g: m.gordura_g,
  });
  if (rpcError) throw new Error(`[meal] Falha ao acumular saldo: ${rpcError.message}`);
}
```

**obterSaldoDia(pacienteId): Promise<MacrosRefeicao>** — SELECT registros_diarios:
```typescript
export async function obterSaldoDia(pacienteId: string): Promise<MacrosRefeicao> {
  const hoje = new Date().toISOString().slice(0, 10);
  const { data, error } = await supabase
    .from('registros_diarios')
    .select('kcal_consumido, proteina_g, carbo_g, gordura_g')
    .eq('paciente_id', pacienteId)
    .eq('data', hoje)
    .maybeSingle();

  if (error || !data) return { kcal: 0, proteina_g: 0, carbo_g: 0, gordura_g: 0 };
  return {
    kcal:       Number(data.kcal_consumido),
    proteina_g: Number(data.proteina_g),
    carbo_g:    Number(data.carbo_g),
    gordura_g:  Number(data.gordura_g),
  };
}
```

**formatarSaldoDia(descricao, kcalRegistrado, saldo, metas): string** — formato D-07:
```typescript
export function formatarSaldoDia(
  descricao: string,
  kcalRegistrado: number,
  saldo: MacrosRefeicao,
  metas: MetasMacros,
): string {
  return (
    `✅ Registrado: ${descricao} (${Math.round(kcalRegistrado)} kcal)\n\n` +
    `📊 Saldo do dia:\n` +
    `• Kcal: ${Math.round(saldo.kcal)} / ${Math.round(metas.kcal)} kcal\n` +
    `• Proteína: ${Math.round(saldo.proteina_g)}g / ${Math.round(metas.proteina_g)}g\n` +
    `• Carbo: ${Math.round(saldo.carbo_g)}g / ${Math.round(metas.carbo_g)}g\n` +
    `• Gordura: ${Math.round(saldo.gordura_g)}g / ${Math.round(metas.gordura_g)}g`
  );
}
```

**sugerirSubstituicao(pacienteId, alimentoAusente): Promise<string>** — RAG + Claude (D-09):
```typescript
export async function sugerirSubstituicao(
  pacienteId: string,
  pacienteNome: string,
  alimentoAusente: string,
): Promise<string> {
  const contexto = await ragQuery(pacienteId, `substituto para ${alimentoAusente}`);
  if (!contexto) {
    return `Não encontrei substitutos para "${alimentoAusente}" na sua dieta. Consulte seu nutricionista.`;
  }

  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 512,
    system: `Você é o NutriChat, assistente nutricional de ${pacienteNome}. Seu nutricionista prescreveu a dieta abaixo.`,
    messages: [{
      role: 'user',
      content: `Contexto da dieta prescrita:\n${contexto}\n\nO paciente não tem "${alimentoAusente}". Com base APENAS nos alimentos da dieta prescrita acima, sugira alternativas disponíveis. Não invente alimentos que não estão na dieta.`,
    }],
  });

  return response.content[0].type === 'text' ? response.content[0].text : '';
}
```

**processarTextoRefeicao(phone, texto, paciente): Promise<void>** — ponto de entrada principal:

Esta é a função mais importante — detecta intenção e despacha para o fluxo correto:

```typescript
export async function processarTextoRefeicao(
  phone: string,
  texto: string,
  paciente: PacienteInfo,
): Promise<void> {
  const textoLower = texto.toLowerCase();

  // Detectar pedido de substituição (D-09)
  const ehSubstituicao = /substitu|nao tenho|não tenho|alternativa|trocar|troca/.test(textoLower);
  if (ehSubstituicao) {
    // Extrair alimento do texto (heurística simples — Claude interpretará o contexto)
    const resposta = await sugerirSubstituicao(paciente.id, paciente.nome, texto);
    await sendText(phone, resposta);
    return;
  }

  // Detectar registro de refeição vs consulta sobre dieta
  // Indicadores de registro: verbos de consumo + alimentos
  const ehRegistro = /comi|tomei|bebi|almocei|jantei|caf[eé]|lanche|refeicao|refeição|breakfast|lunch|dinner|g de|ml de|colher|prato|gramas/.test(textoLower);

  if (!ehRegistro) {
    // Consulta sobre dieta — usar RAG (comportamento original de agent.ts)
    // Retornar sem processar — agent.ts já fará a consulta RAG
    return;
  }

  // Calcular macros via Claude (D-01)
  const macros = await calcularMacrosComClaude(texto);

  // Aviso D-02: quando kcal=0 (Claude não conseguiu estimar)
  let descricaoFinal = texto;
  if (macros.kcal === 0) {
    descricaoFinal = texto;
    await sendText(phone, `⚠️ Não consegui estimar os macros para essa refeição. Tente descrever com mais detalhes (ex: "200g de frango grelhado com 100g de arroz").`);
    return;
  }

  // Registrar no banco (INSERT refeicoes + RPC acumular)
  await registrarRefeicao(paciente.id, texto, macros, 'texto');

  // Buscar saldo atualizado e metas
  const estado = await getEstadoConv(paciente.id);
  const tdeeKcal = (estado.dados.tdee_kcal as number) || 2000; // fallback 2000 kcal
  const metas = calcularMetas(tdeeKcal);
  const saldo = await obterSaldoDia(paciente.id);

  // Enviar resposta formatada (D-07)
  await sendText(phone, formatarSaldoDia(texto, macros.kcal, saldo, metas));
}
```

Importações necessárias no topo do arquivo:
```typescript
import Anthropic from '@anthropic-ai/sdk';
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';
import { query as ragQuery, } from './rag';
import { sendText } from './evolution';
import { getEstado as getEstadoConv } from './conversation';
import type { PacienteInfo } from './conversation';

const claude = new Anthropic({ apiKey: env.CLAUDE_API_KEY });
const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);
```
  </action>
  <verify>
    <automated>cd "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend" && npm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/services/meal.ts` existe
    - Contém `export async function processarTextoRefeicao(`
    - Contém `export async function calcularMacrosComClaude(`
    - Contém `export async function registrarRefeicao(`
    - Contém `export async function obterSaldoDia(`
    - Contém `export function formatarSaldoDia(`
    - Contém `export async function sugerirSubstituicao(`
    - Contém `supabase.rpc('acumular_registro_diario'` (não usa upsert direto)
    - Contém `✅ Registrado:` e `📊 Saldo do dia:` no formatarSaldoDia (formato D-07)
    - Contém `Math.round((tdeeKcal * 0.30) / 4)` (cálculo de metas D-08)
    - Contém `ragQuery(pacienteId` em sugerirSubstituicao (D-09)
    - `npm run typecheck` passa sem erros relacionados a meal.ts
  </acceptance_criteria>
  <done>meal.ts criado com todas as 6 funções implementadas, persistência acumulativa e saldo do dia formatado conforme D-07/D-08/D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Expandir agent.ts para orquestrar registro vs consulta</name>
  <read_first>
    - backend/src/services/agent.ts — bloco `status === 'completa'` (linhas ~207-212) onde a mensagem é passada diretamente ao RAG
    - backend/src/services/meal.ts — assinatura de processarTextoRefeicao criada na Task 1
    - .planning/phases/03-registro-refeicoes-multimodal/03-RESEARCH.md — Pitfall 4 (estado aguardando_foto_2 corrompido em mensagens de texto)
    - .planning/phases/03-registro-refeicoes-multimodal/03-CONTEXT.md — D-05 (timeout estado 2 fotos), D-04 (fluxo 2 fotos)
  </read_first>
  <files>backend/src/services/agent.ts</files>
  <action>
Modificar `agent.ts` no bloco `status === 'completa'` (atualmente linhas 208-211) para:

1. Limpar estado `aguardando_foto_2` expirado (Pitfall 4 da pesquisa)
2. Tentar `processarTextoRefeicao` primeiro — se a função detectar que é uma consulta (retorna sem enviar mensagem), cair no RAG

**Antes (bloco atual no final de processarMensagem):**
```typescript
// 5. Entrevista completa — modo agente normal
const contextoRag = await ragQuery(paciente.id, texto);
const resposta = await responderComClaude(texto, contextoRag, paciente.nome);
await sendText(phone, resposta);
```

**Depois:**
```typescript
// 5. Entrevista completa — modo agente: registro de refeição OU consulta sobre dieta

// Limpar estado aguardando_foto_2 expirado (Pitfall 4 — evita tratar texto como 2ª foto)
const dadosEstado = estado.dados as Record<string, unknown>;
const aguardando = dadosEstado['aguardando_foto_2'] as { timestamp?: string } | undefined;
if (aguardando?.timestamp) {
  const diffMs = Date.now() - new Date(aguardando.timestamp).getTime();
  const expirado = diffMs > 5 * 60 * 1000; // 5 minutos
  if (expirado) {
    await atualizarEstado(paciente.id, { dados: { aguardando_foto_2: null } });
    console.log(`[agent] Estado aguardando_foto_2 expirado para ${paciente.id} — limpo.`);
  }
}

// Tentar processar como registro de refeição
// Se processarTextoRefeicao detectar que é consulta, retorna sem enviar mensagem
// Neste caso, usar o fluxo RAG normal
await mealService.processarTextoRefeicao(phone, texto, paciente);

// Fallback RAG: se processarTextoRefeicao não enviou mensagem (é uma consulta),
// o sendText não foi chamado — precisamos consultar a dieta
// Nota: a lógica de detecção em processarTextoRefeicao retorna sem enviar se não for registro
// Para distinguir os dois fluxos, podemos checar o padrão antes de chamar:
const textoLower = texto.toLowerCase();
const ehRegistro = /comi|tomei|bebi|almocei|jantei|caf[eé]|lanche|refeicao|refeição|g de|ml de|colher|prato|gramas/.test(textoLower);
const ehSubstituicao = /substitu|nao tenho|não tenho|alternativa|trocar/.test(textoLower);

if (!ehRegistro && !ehSubstituicao) {
  // É uma pergunta sobre a dieta — responder com RAG
  const contextoRag = await ragQuery(paciente.id, texto);
  const resposta = await responderComClaude(texto, contextoRag, paciente.nome);
  await sendText(phone, resposta);
} else {
  // Registro ou substituição — processarTextoRefeicao já lidou
  await mealService.processarTextoRefeicao(phone, texto, paciente);
}
```

**IMPORTANTE — refatorar para evitar dupla chamada:** O padrão acima chama processarTextoRefeicao duas vezes. Corrigir para:

```typescript
// 5. Entrevista completa — modo agente

// Limpar estado aguardando_foto_2 expirado (Pitfall 4)
const dadosEstado = estado.dados as Record<string, unknown>;
const aguardando = dadosEstado['aguardando_foto_2'] as { timestamp?: string } | undefined;
if (aguardando?.timestamp) {
  const diffMs = Date.now() - new Date(aguardando.timestamp).getTime();
  if (diffMs > 5 * 60 * 1000) {
    await atualizarEstado(paciente.id, { dados: { aguardando_foto_2: null } });
    console.log(`[agent] Estado aguardando_foto_2 expirado — limpo.`);
  }
}

// Detectar intenção antes de despachar
const textoLower = texto.toLowerCase();
const ehSubstituicao = /substitu|nao tenho|não tenho|alternativa|trocar/.test(textoLower);
const ehRegistro = /comi|tomei|bebi|almocei|jantei|caf[eé]|lanche|refeicao|refeição|g de|ml de|colher|prato|gramas/.test(textoLower);

if (ehRegistro || ehSubstituicao) {
  await mealService.processarTextoRefeicao(phone, texto, paciente);
} else {
  // Consulta sobre dieta — RAG
  const contextoRag = await ragQuery(paciente.id, texto);
  const resposta = await responderComClaude(texto, contextoRag, paciente.nome);
  await sendText(phone, resposta);
}
```

Adicionar import no topo de agent.ts:
```typescript
import * as mealService from './meal';
```

Após editar, reiniciar o container:
```bash
docker restart nutrichat_backend
```
  </action>
  <verify>
    <automated>cd "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend" && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `agent.ts` contém `import * as mealService from './meal'`
    - `agent.ts` contém `mealService.processarTextoRefeicao(phone, texto, paciente)`
    - `agent.ts` contém verificação de `aguardando_foto_2` expirado (Pitfall 4)
    - `agent.ts` contém detecção de `ehRegistro` ou `ehSubstituicao` antes de despachar
    - O bloco RAG original (`ragQuery` + `responderComClaude`) ainda existe como fallback para consultas
    - `npm run typecheck` passa sem erros
    - `docker ps` mostra `nutrichat_backend` Up após restart
  </acceptance_criteria>
  <done>agent.ts expandido: limpa estado expirado, detecta intenção e despacha para mealService ou RAG conforme o tipo de mensagem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paciente → Claude API | Texto do paciente enviado ao Claude para estimativa de macros |
| meal.ts → Supabase | INSERT refeicoes + RPC acumular_registro_diario via service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | calcularMacrosComClaude output | mitigate | sanitizarMacros() garante que NaN e negativos viram 0 antes do INSERT |
| T-03-03-02 | Tampering | registrarRefeicao SQL injection | accept | Supabase client usa prepared statements — sem risco de SQL injection nos campos |
| T-03-03-03 | Information Disclosure | texto da refeição enviado ao Claude | accept | Mesma categoria de risco já aceita para consultas RAG; nenhum dado de saúde crítico |
| T-03-03-04 | Tampering | sugerirSubstituicao inventa alimentos | mitigate | Prompt explícito "com base APENAS nos alimentos da dieta prescrita" + RAG fornece contexto real (D-09) |
</threat_model>

<verification>
Verificação funcional (requer WhatsApp real — após executar este plano):
1. Enviar para `5562992058735`: "comi 200g de frango grelhado com 100g de arroz"
2. Esperar resposta com formato D-07: `✅ Registrado: ... 📊 Saldo do dia: ...`
3. Enviar segunda refeição: "tomei 200ml de suco de laranja"
4. Verificar que saldo ACUMULA (não zera) — kcal deve ser maior que na primeira resposta
5. Enviar: "qual é minha proteína diária" — deve responder via RAG, não registrar

Verificação técnica:
```bash
cd C:/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck
docker logs nutrichat_backend --tail 20
```
</verification>

<success_criteria>
- meal.ts criado com todas as 6 funções exportadas
- agent.ts expandido com limpeza de estado expirado e detecção de intenção
- registrarRefeicao usa `supabase.rpc('acumular_registro_diario')` — nunca upsert direto
- formatarSaldoDia produz o formato exato de D-07
- calcularMetas usa distribuição D-08 (30/40/30%)
- sugerirSubstituicao usa ragQuery com prefixo "substituto para" (D-09)
- typecheck passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-registro-refeicoes-multimodal/03-03-SUMMARY.md`
</output>

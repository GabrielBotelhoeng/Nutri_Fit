---
phase: 03-registro-refeicoes-multimodal
plan: "01"
type: execute
wave: 3
depends_on:
  - "03-04"
  - "03-03"
files_modified:
  - backend/package.json
  - backend/src/services/audio.ts
  - backend/src/routes/webhook.ts
autonomous: true
requirements:
  - AGENT-02
  - AGENT-14

must_haves:
  truths:
    - "Paciente envia áudio no WhatsApp e recebe confirmação de registro com saldo do dia"
    - "O áudio é transcrito via Groq Whisper com language pt"
    - "Após transcrição, o texto passa pelo mesmo fluxo de registro de refeição de meal.ts"
    - "groq-sdk está instalado e o container é rebuiltado"
  artifacts:
    - path: "backend/src/services/audio.ts"
      provides: "downloadMedia() + transcreverAudio() + processarAudio()"
      exports: ["processarAudio", "downloadMedia", "transcreverAudio"]
      min_lines: 60
    - path: "backend/package.json"
      provides: "dependência groq-sdk"
      contains: "groq-sdk"
  key_links:
    - from: "backend/src/routes/webhook.ts case audioMessage"
      to: "backend/src/services/audio.ts processarAudio()"
      via: "await audioService.processarAudio(phone, messageId)"
      pattern: "processarAudio"
    - from: "backend/src/services/audio.ts transcreverAudio()"
      to: "groq.audio.transcriptions.create"
      via: "groq-sdk Groq client"
      pattern: "groq.audio.transcriptions.create"
    - from: "backend/src/services/audio.ts processarAudio()"
      to: "backend/src/services/meal.ts processarTextoRefeicao()"
      via: "await mealService.processarTextoRefeicao(phone, texto, paciente)"
      pattern: "processarTextoRefeicao"
---

<objective>
Implementar transcrição de áudio via Groq Whisper e conectar ao fluxo de registro de refeição.

Purpose: Substituir o stub `audioMessage` em webhook.ts. O paciente envia um áudio descrevendo o que comeu ("comi 200g de frango com arroz e salada") e recebe o mesmo saldo do dia que receberia por texto.

Output: `backend/src/services/audio.ts` com as funções `downloadMedia`, `transcreverAudio` e `processarAudio`; `webhook.ts` atualizado para chamar `processarAudio`; `groq-sdk` instalado no container.

Dependência: Este plano é Wave 3 porque `audio.ts` importa `processarTextoRefeicao` de `meal.ts` (criado em 03-03, Wave 2). Executar após 03-03 concluído para evitar falha de typecheck.
</objective>

<execution_context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/PROJECT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/STATE.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/03-registro-refeicoes-multimodal/03-04-SUMMARY.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/03-registro-refeicoes-multimodal/03-03-SUMMARY.md

<interfaces>
<!-- Contratos que audio.ts consome e expõe -->

De backend/src/routes/webhook.ts (payload disponível no case audioMessage):
```typescript
interface EvolutionPayload {
  data: {
    key: { remoteJid: string; fromMe: boolean; id: string };  // id = messageId
    message: Record<string, unknown>;
    messageType: string;  // "audioMessage"
  };
}
// phone = extractPhoneNumber(data.key.remoteJid)
// messageId = data.key.id
```

De backend/src/config/env.ts (variáveis disponíveis):
```typescript
env.GROQ_API_KEY        // string — chave Groq
env.EVOLUTION_API_URL   // string — ex: "http://nutrichat_evolution:8080"
env.EVOLUTION_API_KEY   // string — "nutrichat_local_key"
```

De backend/src/services/conversation.ts (a importar):
```typescript
export async function buscarPacientePorWhatsapp(whatsapp: string): Promise<PacienteInfo | null>
export interface PacienteInfo { id: string; nome: string; whatsapp: string; ativo: boolean; ... }
```

Contrato que meal.ts (plano 03-03) exporta e audio.ts deve chamar:
```typescript
// Criado em meal.ts (plano 03-03) — audio.ts chama esta função após transcrição
export async function processarTextoRefeicao(
  phone: string,
  texto: string,
  paciente: PacienteInfo,
): Promise<void>
```

Endpoint Evolution API para download de mídia (confirmado no RESEARCH.md):
```
POST ${env.EVOLUTION_API_URL}/chat/getBase64FromMediaMessage/nutrichat
Headers: { "Content-Type": "application/json", apikey: env.EVOLUTION_API_KEY }
Body: { "message": { "key": { "id": messageId } }, "convertToMp4": false }
Response esperada: { base64: string, mimetype: string }
ATENÇÃO: resposta exata não verificada com mensagem real — logar response completo no Wave 0 antes de assumir campos
```

Groq Whisper (do RESEARCH.md, verificado na documentação oficial):
```typescript
import { Groq } from 'groq-sdk';
import { toFile } from 'groq-sdk/uploads';

// Pitfall: mimetype WhatsApp inclui "; codecs=opus" — limpar antes de usar
const mimeClean = mimetype.split(';')[0].trim(); // "audio/ogg"
const ext = mimeClean.split('/')[1];             // "ogg"

groq.audio.transcriptions.create({
  file: await toFile(buffer, `audio.${ext}`, { type: mimeClean }),
  model: 'whisper-large-v3-turbo',
  language: 'pt',
  response_format: 'text',  // retorna string diretamente
  temperature: 0.0,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar groq-sdk e criar backend/src/services/audio.ts</name>
  <read_first>
    - backend/package.json — verificar que groq-sdk ainda não está presente
    - backend/src/services/evolution.ts — padrão de fetch com apikey header e INSTANCE hardcoded
    - backend/src/services/agent.ts — padrão de importação de Anthropic e env; padrão de log com console.log/error
    - backend/src/config/env.ts — verificar que GROQ_API_KEY está presente
    - .planning/phases/03-registro-refeicoes-multimodal/03-RESEARCH.md — Pattern 1 (download), Pattern 2 (transcrição), Pitfall 2 (mimetype OGG), Pitfall 5 (docker restart)
  </read_first>
  <files>backend/package.json, backend/src/services/audio.ts</files>
  <action>
**Passo 1: Instalar groq-sdk**

```bash
cd C:/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm install groq-sdk
```

Após instalar, reconstruir o container (obrigatório — tsx watch não detecta novos pacotes):
```bash
cd C:/Users/botel/OneDrive/Desktop/nutri_fit && docker-compose build backend && docker-compose up -d backend
```

**Passo 2: Criar backend/src/services/audio.ts**

O arquivo deve conter as 3 funções descritas abaixo. Observar os pitfalls ao implementar:

```typescript
import { Groq } from 'groq-sdk';
import { toFile } from 'groq-sdk/uploads';
import { env } from '../config/env';
import { buscarPacientePorWhatsapp } from './conversation';
import { processarTextoRefeicao } from './meal';
import { sendText } from './evolution';

const INSTANCE = 'nutrichat';
const groq = new Groq({ apiKey: env.GROQ_API_KEY });

// downloadMedia: POST para Evolution API e retorna Buffer + mimetype
// ATENÇÃO: logar a response JSON completa na primeira execução real para confirmar os campos
// (a estrutura { base64, mimetype } é assumida — ver Assumption A1 no RESEARCH.md)
// Exportado para reutilização em vision.ts
export async function downloadMedia(messageId: string): Promise<{ buffer: Buffer; mimetype: string }> {
  const url = `${env.EVOLUTION_API_URL}/chat/getBase64FromMediaMessage/${INSTANCE}`;
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', apikey: env.EVOLUTION_API_KEY },
    body: JSON.stringify({ message: { key: { id: messageId } }, convertToMp4: false }),
  });

  if (!response.ok) {
    throw new Error(`[audio] Falha ao baixar mídia ${messageId}: ${response.status}`);
  }

  const raw = await response.json() as Record<string, unknown>;
  // Logar para confirmar estrutura real na primeira execução
  console.log('[audio] downloadMedia response keys:', Object.keys(raw));

  const base64 = raw['base64'] as string;
  const mimetype = (raw['mimetype'] as string) ?? 'audio/ogg';
  return { buffer: Buffer.from(base64, 'base64'), mimetype };
}

// transcreverAudio: converte Buffer para arquivo Groq e transcreve
// Pitfall 2: limpar mimetype antes de usar (WhatsApp envia "audio/ogg; codecs=opus")
// Exportado para uso em testes unitários
export async function transcreverAudio(buffer: Buffer, mimetype: string): Promise<string> {
  const mimeClean = mimetype.split(';')[0].trim();
  const ext = mimeClean.split('/')[1] ?? 'ogg';
  const audioFile = await toFile(buffer, `audio.${ext}`, { type: mimeClean });

  const transcription = await groq.audio.transcriptions.create({
    file: audioFile,
    model: 'whisper-large-v3-turbo',
    language: 'pt',
    response_format: 'text',
    temperature: 0.0,
  });

  return transcription as unknown as string;
}

// processarAudio: ponto de entrada chamado pelo webhook.ts
export async function processarAudio(phone: string, messageId: string): Promise<void> {
  const paciente = await buscarPacientePorWhatsapp(phone);
  if (!paciente) {
    // Paciente não cadastrado — mensagem enviada pelo webhook.ts antes desta chamada
    return;
  }

  try {
    const { buffer, mimetype } = await downloadMedia(messageId);
    const texto = await transcreverAudio(buffer, mimetype);
    console.log(`[audio] Transcrição de ${phone}: "${texto}"`);
    await processarTextoRefeicao(phone, texto, paciente);
  } catch (err) {
    console.error('[audio] Erro ao processar áudio:', err);
    await sendText(phone, '❌ Não consegui processar seu áudio. Pode tentar de novo ou descrever por texto?');
  }
}
```

Notas de implementação:
- Não remover o console.log de `downloadMedia response keys` — será útil para confirmar Assumption A1 no primeiro teste real.
- `downloadMedia` e `transcreverAudio` são exportados explicitamente: `downloadMedia` é reutilizado por `vision.ts` (plano 03-02) e `transcreverAudio` expõe a função para testes.
- Este plano é Wave 3 (após 03-03) para garantir que `meal.ts` existe quando `audio.ts` importa `processarTextoRefeicao` — o typecheck falharia se 03-01 rodar antes de 03-03.
  </action>
  <verify>
    <automated>cd "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend" && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/package.json` contém `"groq-sdk"` nas dependencies
    - `backend/src/services/audio.ts` existe
    - `audio.ts` contém `export async function processarAudio(`
    - `audio.ts` contém `export async function downloadMedia(`
    - `audio.ts` contém `export async function transcreverAudio(`
    - `audio.ts` contém `groq.audio.transcriptions.create(`
    - `audio.ts` contém `mimetype.split(';')[0].trim()` (sanitização do pitfall 2)
    - `audio.ts` contém `model: 'whisper-large-v3-turbo'`
    - `audio.ts` contém `language: 'pt'`
    - `npm run typecheck` passa sem erros relacionados a audio.ts
  </acceptance_criteria>
  <done>groq-sdk instalado, audio.ts criado com as 3 funções implementadas conforme os padrões da pesquisa. downloadMedia e transcreverAudio exportados para reutilização.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir stub audioMessage em webhook.ts</name>
  <read_first>
    - backend/src/routes/webhook.ts — ver o stub atual no case "audioMessage" e o padrão dos outros cases
    - backend/src/services/audio.ts — confirmar que processarAudio(phone, messageId) foi criado na Task 1
  </read_first>
  <files>backend/src/routes/webhook.ts</files>
  <action>
Substituir o stub do case `audioMessage` pelo código real.

**Antes (stub atual em webhook.ts linhas 51-54):**
```typescript
case 'audioMessage': {
  // Stub Fase 3 — transcricao via Groq Whisper
  await sendText(phone, '🎤 Recebi seu audio! Transcricao disponivel em breve...');
  break;
}
```

**Depois:**
```typescript
case 'audioMessage': {
  const messageId = data.key.id;
  await audioService.processarAudio(phone, messageId);
  break;
}
```

Usar a versão simples — `processarAudio` já faz `buscarPacientePorWhatsapp` internamente.

Adicionar o import no topo do arquivo (junto aos imports existentes):
```typescript
import * as audioService from '../services/audio';
```

**Também remover o import de `sendText`** se ele só era usado pelo stub (verificar se ainda é usado nos outros cases — se não, remover para evitar unused import warning).

Após editar, reiniciar o container:
```bash
docker restart nutrichat_backend
```
  </action>
  <verify>
    <automated>cd "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend" && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `webhook.ts` contém `import * as audioService from '../services/audio'`
    - `webhook.ts` case `audioMessage` contém `audioService.processarAudio(phone, messageId)`
    - `webhook.ts` NÃO contém `Transcricao disponivel em breve` (stub removido)
    - `npm run typecheck` passa sem erros
    - `docker ps` mostra `nutrichat_backend` com status `Up`
  </acceptance_criteria>
  <done>Stub audioMessage substituído pelo fluxo real de transcrição Groq Whisper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evolution API → backend audio.ts | Dados de mídia vindos do WhatsApp via Evolution API |
| backend → Groq Cloud API | Buffer de áudio enviado ao Groq para transcrição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | downloadMedia header apikey | mitigate | env.EVOLUTION_API_KEY incluído no header — já padrão estabelecido em evolution.ts |
| T-03-01-02 | Tampering | base64 corrompido em downloadMedia | mitigate | `Buffer.from(base64, 'base64')` ignora bytes inválidos — sem risco de crash; log do erro retorna 400 ao Evolution |
| T-03-01-03 | Denial of Service | áudio muito grande para Groq (>25 MB) | accept | Áudios WhatsApp ficam entre 10 KB e 2 MB — limite de 25 MB não será atingido em uso normal |
| T-03-01-04 | Information Disclosure | transcrição de áudio contendo dados sensíveis | accept | Dados vão ao Groq Cloud — mesma categoria de risco que enviar texto ao Claude (já aceito) |
</threat_model>

<verification>
Verificação funcional (requer WhatsApp real):
1. Enviar áudio de 5-10 segundos descrevendo refeição para o número do bot `5562992058735`
2. Verificar no log do backend: `docker logs nutrichat_backend --tail 30`
3. Esperar resposta com saldo do dia (após plano 03-03 executado)
4. Verificar que `[audio] downloadMedia response keys:` aparece no log (confirma Assumption A1)

Verificação técnica:
```bash
cd C:/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck
```
</verification>

<success_criteria>
- groq-sdk instalado em package.json e disponível no container
- audio.ts com downloadMedia, transcreverAudio e processarAudio implementados e exportados
- webhook.ts case audioMessage chama processarAudio em vez do stub
- typecheck passa sem erros
- Container restart executado após mudanças
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-registro-refeicoes-multimodal/03-01-SUMMARY.md`
</output>

---
phase: 2
plan: "02-02"
title: "Pipeline RAG — PDF para pgvector"
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - backend/src/services/rag.ts
  - backend/src/routes/rag.ts
  - backend/src/routes/api.ts
  - backend/package.json
  - supabase/migrations/20260423000005_dieta_chunks.sql
autonomous: true
requirements:
  - AGENT-07

must_haves:
  truths:
    - "POST /api/rag/processar recebe paciente_id e pdf_url e processa o PDF em chunks"
    - "PDF e baixado do Supabase Storage, dividido em chunks de 1000 tokens com overlap de 200"
    - "Embeddings sao gerados para cada chunk via OpenAI text-embedding-3-small (1536 dims)"
    - "Chunks com embeddings sao armazenados na tabela dieta_chunks no Supabase"
    - "ragService.query() busca chunks relevantes para uma pergunta e retorna contexto para o Claude"
  artifacts:
    - path: "backend/src/services/rag.ts"
      provides: "Pipeline completo de indexacao e consulta RAG"
      contains: "processarDieta"
    - path: "supabase/migrations/20260423000005_dieta_chunks.sql"
      provides: "Tabela dieta_chunks com embedding vector(1536) e funcao de busca"
      contains: "CREATE TABLE dieta_chunks"
  key_links:
    - from: "tabela dietas"
      to: "tabela dieta_chunks"
      via: "dieta_id FK — cada chunk pertence a uma dieta"
      pattern: "dieta_id.*UUID.*REFERENCES dietas"
    - from: "ragService.query()"
      to: "Supabase RPC match_dieta_chunks"
      via: "busca de similaridade coseno nos embeddings dos chunks"
      pattern: "match_dieta_chunks"
---

<objective>
Implementar o pipeline RAG completo: receber PDF da dieta do Supabase Storage, extrair texto, dividir em chunks, gerar embeddings via OpenAI, armazenar no pgvector e expor funcao de consulta para o agente Claude.

Purpose: Permite ao agente responder perguntas baseadas na dieta prescrita do paciente ("posso comer X?", "qual e minha meta de proteina?") consultando o PDF original via busca semantica.
Output: Tabela dieta_chunks, servico ragService com processarDieta() e query(), rota POST /api/rag/processar.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infraestrutura-ambiente/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration dieta_chunks — tabela de chunks com pgvector</name>
  <files>
    supabase/migrations/20260423000005_dieta_chunks.sql
  </files>
  <read_first>
    - supabase/migrations/20260422000001_create_schema.sql (estrutura da tabela dietas para FK correta)
    - supabase/migrations/20260422000002_enable_pgvector.sql (funcao match_dieta_chunks existente — referenciar)
  </read_first>
  <action>
Criar migration para a tabela dieta_chunks e funcao de busca semantica.

A tabela dietas existente tem uma coluna `embedding vector(1536)` para embedding do documento inteiro — util como fallback. A nova tabela dieta_chunks armazena um embedding por chunk de texto, o que permite RAG granular.

**Arquivo: supabase/migrations/20260423000005_dieta_chunks.sql**

```sql
-- Migration 005: Tabela dieta_chunks para RAG granular

-- Chunks de texto extraidos dos PDFs das dietas
CREATE TABLE IF NOT EXISTS dieta_chunks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  dieta_id UUID NOT NULL REFERENCES dietas(id) ON DELETE CASCADE,
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE,
  chunk_index INTEGER NOT NULL,
  content TEXT NOT NULL,
  embedding vector(1536),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_dieta_chunks_dieta ON dieta_chunks(dieta_id);
CREATE INDEX IF NOT EXISTS idx_dieta_chunks_paciente ON dieta_chunks(paciente_id);
CREATE INDEX IF NOT EXISTS idx_dieta_chunks_embedding ON dieta_chunks
  USING hnsw (embedding vector_cosine_ops)
  WITH (m = 16, ef_construction = 64);

-- RLS
ALTER TABLE dieta_chunks ENABLE ROW LEVEL SECURITY;
CREATE POLICY "authenticated_full_access_dieta_chunks"
  ON dieta_chunks FOR ALL TO authenticated
  USING (true) WITH CHECK (true);

-- Funcao de busca semantica por paciente
CREATE OR REPLACE FUNCTION match_chunks_paciente(
  query_embedding vector(1536),
  p_paciente_id UUID,
  match_threshold FLOAT DEFAULT 0.7,
  match_count INT DEFAULT 5
)
RETURNS TABLE (
  id UUID,
  content TEXT,
  similarity FLOAT
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    dc.id,
    dc.content,
    1 - (dc.embedding <=> query_embedding) AS similarity
  FROM dieta_chunks dc
  WHERE dc.paciente_id = p_paciente_id
    AND 1 - (dc.embedding <=> query_embedding) > match_threshold
  ORDER BY dc.embedding <=> query_embedding
  LIMIT match_count;
END;
$$;
```

Apos criar o arquivo, executar:
```bash
cd /c/Users/botel/OneDrive/Desktop/nutri_fit
supabase db push
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260423000005_dieta_chunks.sql &&
      grep -q "CREATE TABLE dieta_chunks" /c/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260423000005_dieta_chunks.sql &&
      grep -q "match_chunks_paciente" /c/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260423000005_dieta_chunks.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260423000005_dieta_chunks.sql existe
    - Tabela dieta_chunks com colunas: id, dieta_id (FK dietas), paciente_id (FK pacientes), chunk_index, content, embedding vector(1536)
    - Index HNSW no campo embedding
    - RLS habilitado com policy para authenticated
    - Funcao match_chunks_paciente que busca por paciente_id e retorna chunks por similaridade
    - supabase db push executa sem erros
  </acceptance_criteria>
  <done>Tabela dieta_chunks criada e migration aplicada no Supabase.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar dependencias LangChain + OpenAI embeddings</name>
  <files>
    backend/package.json
  </files>
  <action>
Instalar os pacotes necessarios para o pipeline RAG.

```bash
cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend
npm install @langchain/openai@^0.3.0 @langchain/community@^0.3.0 langchain@^0.3.0 pdf-parse@^1.1.1 @types/pdf-parse@^1.1.4
```

Pacotes e suas funcoes:
- `@langchain/openai` — OpenAIEmbeddings para gerar vetores de 1536 dimensoes
- `langchain` — RecursiveCharacterTextSplitter para dividir texto em chunks
- `pdf-parse` — extrair texto de PDF em buffer

A variavel `OPENAI_API_KEY` e necessaria para embeddings. Ela deve ser adicionada ao `backend/.env` e ao `backend/.env.example`.

Adicionar ao backend/.env.example (se existir):
```
OPENAI_API_KEY=sk-proj-...
```

Adicionar ao backend/src/config/env.ts — incluir `OPENAI_API_KEY` na lista de variaveis obrigatorias e na interface Env.
  </action>
  <verify>
    <automated>
      test -d /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/node_modules/langchain &&
      test -d /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/node_modules/@langchain/openai &&
      test -d /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/node_modules/pdf-parse
    </automated>
  </verify>
  <acceptance_criteria>
    - langchain, @langchain/openai, @langchain/community, pdf-parse instalados em node_modules
    - package.json atualizado com as novas dependencias
    - OPENAI_API_KEY adicionado ao backend/.env.example
    - backend/src/config/env.ts inclui OPENAI_API_KEY como variavel obrigatoria
  </acceptance_criteria>
  <done>Dependencias RAG instaladas e OPENAI_API_KEY adicionada ao schema de env.</done>
</task>

<task type="auto">
  <name>Task 3: Criar ragService — pipeline de indexacao e consulta</name>
  <files>
    backend/src/services/rag.ts
  </files>
  <read_first>
    - backend/src/config/env.ts (env vars disponiveis apos Task 2)
    - supabase/migrations/20260423000005_dieta_chunks.sql (nome exato da funcao match_chunks_paciente)
  </read_first>
  <action>
Criar o servico RAG com as funcoes de indexacao (processarDieta) e consulta (query).

**Arquivo: backend/src/services/rag.ts**

```typescript
import { createClient } from '@supabase/supabase-js';
import { OpenAIEmbeddings } from '@langchain/openai';
import { RecursiveCharacterTextSplitter } from 'langchain/text_splitter';
import pdfParse from 'pdf-parse';
import { env } from '../config/env';

const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);

const embeddings = new OpenAIEmbeddings({
  openAIApiKey: env.OPENAI_API_KEY,
  modelName: 'text-embedding-3-small',
});

const splitter = new RecursiveCharacterTextSplitter({
  chunkSize: 1000,
  chunkOverlap: 200,
});

export async function processarDieta(
  pacienteId: string,
  dietaId: string,
  pdfUrl: string,
): Promise<void> {
  console.log(`[rag] Processando dieta ${dietaId} para paciente ${pacienteId}`);

  // 1. Baixar PDF do Supabase Storage
  const path = pdfUrl.replace(/^.*\/storage\/v1\/object\/public\/dietas\//, '');
  const { data: fileData, error: downloadError } = await supabase.storage
    .from('dietas')
    .download(path);

  if (downloadError || !fileData) {
    throw new Error(`Falha ao baixar PDF: ${downloadError?.message}`);
  }

  // 2. Extrair texto do PDF
  const buffer = Buffer.from(await fileData.arrayBuffer());
  const parsed = await pdfParse(buffer);
  const text = parsed.text;

  if (!text.trim()) {
    throw new Error('PDF sem texto extraivel (pode ser imagem escaneada)');
  }

  // 3. Dividir em chunks
  const chunks = await splitter.splitText(text);
  console.log(`[rag] ${chunks.length} chunks gerados para dieta ${dietaId}`);

  // 4. Gerar embeddings em lotes de 20 (evitar rate limit)
  const batchSize = 20;
  const allEmbeddings: number[][] = [];
  for (let i = 0; i < chunks.length; i += batchSize) {
    const batch = chunks.slice(i, i + batchSize);
    const batchEmbeddings = await embeddings.embedDocuments(batch);
    allEmbeddings.push(...batchEmbeddings);
  }

  // 5. Remover chunks anteriores da mesma dieta (re-processamento)
  await supabase.from('dieta_chunks').delete().eq('dieta_id', dietaId);

  // 6. Inserir novos chunks
  const rows = chunks.map((content, i) => ({
    dieta_id: dietaId,
    paciente_id: pacienteId,
    chunk_index: i,
    content,
    embedding: JSON.stringify(allEmbeddings[i]),
  }));

  const { error: insertError } = await supabase.from('dieta_chunks').insert(rows);
  if (insertError) {
    throw new Error(`Falha ao inserir chunks: ${insertError.message}`);
  }

  console.log(`[rag] Dieta ${dietaId} indexada com ${chunks.length} chunks`);
}

export async function query(pacienteId: string, pergunta: string): Promise<string> {
  const queryEmbedding = await embeddings.embedQuery(pergunta);

  const { data, error } = await supabase.rpc('match_chunks_paciente', {
    query_embedding: JSON.stringify(queryEmbedding),
    p_paciente_id: pacienteId,
    match_threshold: 0.6,
    match_count: 5,
  });

  if (error) {
    console.error('[rag] Erro na busca:', error);
    return '';
  }

  if (!data || data.length === 0) {
    return '';
  }

  return (data as { content: string }[]).map((r) => r.content).join('\n\n');
}
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/rag.ts &&
      grep -q "processarDieta" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/rag.ts &&
      grep -q "match_chunks_paciente" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/rag.ts &&
      cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck 2>&1 | head -10
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/rag.ts existe e exporta processarDieta() e query()
    - processarDieta baixa PDF do Storage, extrai texto, divide em chunks, gera embeddings e insere em dieta_chunks
    - query() gera embedding da pergunta e chama match_chunks_paciente via Supabase RPC
    - Embeddings gerados em lotes de 20 para evitar rate limit
    - Chunks anteriores removidos antes de re-inserir (idempotente)
    - npm run typecheck sem erros novos relacionados a rag.ts
  </acceptance_criteria>
  <done>RAG service completo com indexacao e consulta semantica.</done>
</task>

<task type="auto">
  <name>Task 4: Criar rota /api/rag/processar e registrar no router</name>
  <files>
    backend/src/routes/rag.ts
    backend/src/routes/api.ts
  </files>
  <read_first>
    - backend/src/services/rag.ts (importar processarDieta)
    - backend/src/routes/api.ts (adicionar ragRouter)
  </read_first>
  <action>
Criar a rota que dispara o processamento RAG de um PDF e registrar no router central.

**Arquivo: backend/src/routes/rag.ts**

```typescript
import { Router, Request, Response } from 'express';
import { processarDieta } from '../services/rag';

export const ragRouter = Router();

interface ProcessarBody {
  paciente_id: string;
  dieta_id: string;
  pdf_url: string;
}

ragRouter.post('/processar', async (req: Request, res: Response) => {
  const { paciente_id, dieta_id, pdf_url } = req.body as ProcessarBody;

  if (!paciente_id || !dieta_id || !pdf_url) {
    res.status(400).json({ error: 'paciente_id, dieta_id e pdf_url sao obrigatorios' });
    return;
  }

  // Aceitar imediatamente e processar em background
  res.status(202).json({ status: 'processando', dieta_id });

  processarDieta(paciente_id, dieta_id, pdf_url).catch((err) => {
    console.error(`[rag] Falha ao processar dieta ${dieta_id}:`, err);
  });
});
```

**Modificar: backend/src/routes/api.ts**

Adicionar importacao e registro do ragRouter:

```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/rag.ts &&
      grep -q "ragRouter" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts &&
      cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck 2>&1 | grep -c "error" || true
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/rag.ts existe e exporta ragRouter com POST /processar
    - POST /processar retorna 202 imediatamente e processa em background
    - backend/src/routes/api.ts importa e registra ragRouter em /rag
    - npm run typecheck sem erros
  </acceptance_criteria>
  <done>Rota /api/rag/processar criada e registrada. Pipeline RAG completo e funcional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → OpenAI | OPENAI_API_KEY usada apenas para embeddings — nenhum dado de saude e enviado |
| Backend → Supabase Storage | SUPABASE_SERVICE_KEY com acesso ao bucket dietas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | OpenAI Embeddings | accept | Chunks de texto da dieta sao enviados para OpenAI para gerar embeddings. Dados de saude enviados a terceiros. Risco aceito para v1; avaliar Voyage AI em v2. |
| T-02-02-02 | Tampering | dieta_chunks | mitigate | Endpoint /api/rag/processar sera protegido por autenticacao na Fase 5. Por enquanto apenas acessivel via rede Docker interna. |
| T-02-02-03 | DoS | processarDieta | mitigate | Processamento em background (202 imediato). Sem bloqueio do request handler. |
</threat_model>

<verification>
Apos execucao, verificar:
1. `supabase db push` aplicou migration 005 sem erros
2. Tabela dieta_chunks visivel no Supabase Dashboard
3. npm run typecheck sem erros
4. Teste manual: POST /api/rag/processar com paciente_id, dieta_id e pdf_url validos retorna 202
</verification>

<success_criteria>
- supabase/migrations/20260423000005_dieta_chunks.sql criado e aplicado
- backend/src/services/rag.ts com processarDieta() e query()
- backend/src/routes/rag.ts com POST /processar retornando 202
- backend/src/routes/api.ts registra ragRouter
- npm run typecheck limpo
- OPENAI_API_KEY adicionado ao env config
</success_criteria>

---
phase: 06-landing-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nutrichat-landing/package.json
  - nutrichat-landing/app/globals.css
  - nutrichat-landing/app/layout.tsx
  - nutrichat-landing/app/page.tsx
  - nutrichat-landing/lib/animations.ts
  - nutrichat-landing/lib/whatsapp.ts
  - nutrichat-landing/components/Header.tsx
  - nutrichat-landing/components/Hero.tsx
  - nutrichat-landing/components/PhoneCanvas.tsx
  - nutrichat-landing/public/hero-phone-static.webp
autonomous: true
requirements:
  - LANDING-01
  - LANDING-07

must_haves:
  truths:
    - "Repositório nutrichat-landing existe com Next.js 15 App Router, TypeScript, Tailwind v4"
    - "app/globals.css contém @theme {} com os 9 tokens de cor corretos (floresta, medio, claro, menta, offwhite, terra, claro-br, creme, branco)"
    - "Hero exibe celular 3D animado em telas >= 768px (canvas Three.js com Float idle + chat animado em loop)"
    - "Hero exibe imagem estática WebP em telas < 768px (sem canvas Three.js)"
    - "npm run build executa sem erros (TypeScript + SSR)"
  artifacts:
    - path: "nutrichat-landing/app/globals.css"
      provides: "Tailwind v4 @theme com 9 tokens de cor"
      contains: "@theme {"
    - path: "nutrichat-landing/components/PhoneCanvas.tsx"
      provides: "Celular 3D + chat animado"
      exports: ["PhoneCanvas (default)"]
    - path: "nutrichat-landing/components/Hero.tsx"
      provides: "Hero com dynamic import ssr:false e fallback mobile"
      contains: "dynamic("
    - path: "nutrichat-landing/lib/animations.ts"
      provides: "Variantes Framer Motion reutilizáveis"
      exports: ["fadeInUp", "staggerContainer"]
    - path: "nutrichat-landing/lib/whatsapp.ts"
      provides: "Função waLink com NEXT_PUBLIC_WHATSAPP_NUMBER"
      exports: ["waLink"]
  key_links:
    - from: "components/Hero.tsx"
      to: "components/PhoneCanvas.tsx"
      via: "dynamic import ssr:false"
      pattern: "dynamic.*PhoneCanvas.*ssr.*false"
    - from: "components/PhoneCanvas.tsx"
      to: "@react-three/fiber Canvas"
      via: "import { Canvas } from '@react-three/fiber'"
      pattern: "'use client'"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css' + Inter font variable"
      pattern: "--font-inter"
---

<objective>
Criar o repositório nutrichat-landing do zero com Next.js 15 App Router, instalar todas as dependências da stack (react-three-fiber, drei, framer-motion, lucide-react), configurar Tailwind v4 com a paleta completa de 9 tokens, e implementar o Hero com celular 3D animado (PhoneCanvas) — incluindo animação idle Float, chat animado em loop e fallback de imagem estática para mobile.

Purpose: O hero 3D é o elemento central de diferenciação da landing. Esta tarefa entrega a fundação técnica completa (scaffold, dependências, tokens de cor, utilitários compartilhados) e o componente mais complexo da fase (PhoneCanvas).

Output: Repositório nutrichat-landing com scaffold funcional, configuração Tailwind v4, biblioteca de animações compartilhada, função waLink, Header, Hero e PhoneCanvas — compilando sem erros com `npm run build`.
</objective>

<execution_context>
@C:/Users/botel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-CONTEXT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-RESEARCH.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md

<interfaces>
<!-- Padrões extraídos de nutrichat-painel para replicação exata na landing -->

De nutrichat-painel/src/index.css (analog de globals.css):
```css
@import "tailwindcss";

@theme {
  --color-floresta: #2D5016;
  --color-offwhite: #F0F7EC;
  --color-terra: #6B3D1E;
}
```
A landing EXPANDE para 9 tokens (ver PATTERNS.md §globals.css).

Padrão RoundedBox + Float (PhoneCanvas — 06-PATTERNS.md):
```tsx
'use client'
import { Canvas } from '@react-three/fiber'
import { Float, RoundedBox } from '@react-three/drei'
```
Float props obrigatórias: speed={1.5} rotationIntensity={0.2} floatIntensity={0.3} floatingRange={[-0.15, 0.15]}

Padrão dynamic import (Hero.tsx — 06-RESEARCH.md Pattern 4):
```tsx
const PhoneCanvas = dynamic(() => import('./PhoneCanvas'), { ssr: false, loading: () => <div className="w-full h-[480px] bg-menta/20 rounded-2xl animate-pulse" /> })
```

Padrão waLink (06-PATTERNS.md §Shared Patterns):
```ts
export function waLink(months?: number): string {
  const phone = process.env.NEXT_PUBLIC_WHATSAPP_NUMBER ?? '5500000000000'
  const text = months
    ? encodeURIComponent(`Olá! Tenho interesse no plano de ${months} ${months === 1 ? 'mês' : 'meses'} do Nutri Chat. Pode me passar mais informações?`)
    : encodeURIComponent('Olá! Quero começar com o NutriChat.')
  return `https://wa.me/${phone}?text=${text}`
}
```

CHAT_MESSAGES exatas do UI-SPEC (06-UI-SPEC.md §Chat Animation Script):
- [0.0s] user: "Oi! Acabei de almoçar 🍽️"
- [0.8s] bot: "Ótimo! Me conta o que você comeu?"
- [1.6s] user: "Arroz, frango grelhado e salada"
- [2.4s] bot: "Registrado! ✅ Você consumiu 520kcal. Faltam 880kcal para a meta de hoje."
- [3.2s] user: "Perfeito, obrigado!"
- [4.0s] → reinicia
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Scaffold nutrichat-landing + dependências + configuração Tailwind v4 + utilitários</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-RESEARCH.md — Seções "Standard Stack" (linhas 92-135), "Pattern 1: create-next-app" (linhas 207-222), "Pattern 2: Tailwind v4 @theme" (linhas 227-257), "Pattern 3: next/font" (linhas 260-288)
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md — Seções "globals.css" (linhas 29-68), "layout.tsx" (linhas 73-107), "page.tsx" (linhas 111-138), "Shared Patterns" (linhas 733-812)
  </read_first>
  <files>
    nutrichat-landing/package.json,
    nutrichat-landing/app/globals.css,
    nutrichat-landing/app/layout.tsx,
    nutrichat-landing/app/page.tsx,
    nutrichat-landing/.env.local,
    nutrichat-landing/lib/animations.ts,
    nutrichat-landing/lib/whatsapp.ts
  </files>
  <action>
Executar os seguintes passos NA ORDEM:

**1. Criar repositório com create-next-app (na pasta pai de nutri_fit — Desktop):**
```bash
cd C:/Users/botel/OneDrive/Desktop
npx create-next-app@latest nutrichat-landing \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --no-src-dir \
  --import-alias "@/*" \
  --yes
```

**2. Instalar dependências adicionais:**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing
npm install three @react-three/fiber @react-three/drei framer-motion lucide-react
```

**3. Substituir `app/globals.css` COMPLETO com exatamente:**
```css
@import "tailwindcss";

@theme {
  --color-floresta:  #2D5016;
  --color-medio:     #4A7C2F;
  --color-claro:     #7DB85A;
  --color-menta:     #C8E6C0;
  --color-offwhite:  #F0F7EC;
  --color-terra:     #6B3D1E;
  --color-claro-br:  #A0694A;
  --color-creme:     #FAF4ED;
  --color-branco:    #FFFFFF;

  --font-sans: var(--font-inter);
}

html {
  scroll-behavior: smooth;
}

* { box-sizing: border-box; }
body { margin: 0; }
```

**4. Substituir `app/layout.tsx` COMPLETO com:**
```tsx
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
})

export const metadata = {
  title: 'NutriChat — Seu assistente nutricional no WhatsApp',
  description: 'Acompanhamento nutricional inteligente via WhatsApp. Registro de refeições, alertas e relatórios automáticos.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR" className={`${inter.variable} antialiased`}>
      <body>{children}</body>
    </html>
  )
}
```

**5. Substituir `app/page.tsx` COMPLETO com (Server Component — SEM 'use client'):**
```tsx
import Header from '@/components/Header'
import Hero from '@/components/Hero'
import HowItWorks from '@/components/HowItWorks'
import Features from '@/components/Features'
import Testimonials from '@/components/Testimonials'
import Plans from '@/components/Plans'
import Footer from '@/components/Footer'

export default function Home() {
  return (
    <main>
      <Header />
      <Hero />
      <HowItWorks />
      <Features />
      <Testimonials />
      <Plans />
      <Footer />
    </main>
  )
}
```
Os componentes HowItWorks, Features, Testimonials, Plans e Footer serão criados no plano 06-02 e 06-03. Por ora, criar arquivos stub para que o build não quebre (ver abaixo).

**6. Criar stubs para os componentes ainda não implementados:**
Criar os seguintes arquivos com conteúdo mínimo (serão substituídos nos planos 06-02 e 06-03):

`components/HowItWorks.tsx`:
```tsx
export default function HowItWorks() { return <section id="como-funciona" /> }
```

`components/Features.tsx`:
```tsx
export default function Features() { return <section id="funcionalidades" /> }
```

`components/Testimonials.tsx`:
```tsx
export default function Testimonials() { return <section id="depoimentos" /> }
```

`components/Plans.tsx`:
```tsx
export default function Plans() { return <section id="planos" /> }
```

`components/Footer.tsx`:
```tsx
export default function Footer() { return <footer /> }
```

**7. Criar `lib/animations.ts`:**
```ts
export const fadeInUp = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5, ease: 'easeOut' },
  },
}

export const staggerContainer = {
  hidden: {},
  visible: {
    transition: {
      staggerChildren: 0.08,
    },
  },
}
```

**8. Criar `lib/whatsapp.ts`:**
```ts
export function waLink(months?: number): string {
  const phone = process.env.NEXT_PUBLIC_WHATSAPP_NUMBER ?? '5500000000000'
  const text = months
    ? encodeURIComponent(
        `Olá! Tenho interesse no plano de ${months} ${months === 1 ? 'mês' : 'meses'} do Nutri Chat. Pode me passar mais informações?`
      )
    : encodeURIComponent('Olá! Quero começar com o NutriChat.')
  return `https://wa.me/${phone}?text=${text}`
}
```

**9. Criar `.env.local` (não commitar — já no .gitignore do create-next-app):**
```
NEXT_PUBLIC_WHATSAPP_NUMBER=5500000000000
```
  </action>
  <verify>
    <automated>cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `C:/Users/botel/OneDrive/Desktop/nutrichat-landing/package.json` existe e contém `"@react-three/fiber"` e `"framer-motion"` e `"lucide-react"` e `"three"`
    - `app/globals.css` contém exatamente `"@theme {"` e `"--color-floresta:  #2D5016"` e `"--color-menta:     #C8E6C0"` e `"--color-claro-br:  #A0694A"`
    - `app/layout.tsx` contém `"variable: '--font-inter'"` e `"lang=\"pt-BR\""`
    - `app/page.tsx` NÃO contém `"'use client'"` (deve ser Server Component)
    - `lib/animations.ts` contém `"export const fadeInUp"` e `"export const staggerContainer"`
    - `lib/whatsapp.ts` contém `"NEXT_PUBLIC_WHATSAPP_NUMBER"` e `"encodeURIComponent"`
    - `npm run build` executa sem erros TypeScript (saída inclui "Build completed" ou similar sem "error TS")
  </acceptance_criteria>
  <done>Scaffold criado com todas as dependências instaladas, Tailwind v4 configurado com 9 tokens, utilitários compartilhados prontos, stubs de componentes em lugar, build verde.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Header sticky + Hero com PhoneCanvas 3D + imagem mobile WebP</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-RESEARCH.md — "Pattern 4: PhoneCanvas dynamic import" (linhas 290-344), "Pattern 5: PhoneCanvas celular 3D" (linhas 349-448), "Pitfall 1: Three.js + SSR" (linhas 553-558), "Pitfall 4: Chat overlay" (linhas 574-577)
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md — "Header.tsx" (linhas 144-216), "Hero.tsx" (linhas 220-295), "PhoneCanvas.tsx" (linhas 300-386)
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md — "Header / Nav" (linhas 126-131), "Hero Section" (linhas 132-141), "Chat Animation Script" (linhas 143-151), "Animações e Interações" (linhas 205-215), "Copywriting Contract" (linhas 228-248)
  </read_first>
  <files>
    nutrichat-landing/components/Header.tsx,
    nutrichat-landing/components/Hero.tsx,
    nutrichat-landing/components/PhoneCanvas.tsx,
    nutrichat-landing/public/hero-phone-static.webp
  </files>
  <action>
**ATENÇÃO CRÍTICA:** PhoneCanvas.tsx DEVE ter `'use client'` na linha 1. Hero.tsx DEVE usar `dynamic(() => import('./PhoneCanvas'), { ssr: false })`. Sem isso o build quebra com "ReferenceError: window is not defined".

**1. Criar `public/hero-phone-static.webp` (placeholder mobile):**
Criar um arquivo de imagem placeholder. Como não é possível criar um WebP real via texto, usar o seguinte approach:
- Criar o arquivo como PNG solid verde (#C8E6C0) de 300x480px usando Node.js se disponível, OU
- Copiar qualquer imagem WebP/PNG existente no projeto como placeholder com o nome `hero-phone-static.webp`
- Se não houver forma programática, criar um arquivo texto chamado `public/hero-phone-static.webp.placeholder` com conteúdo "REPLACE_WITH_REAL_IMAGE" e adicionar um fallback no código:
  ```
  Em Hero.tsx, se a imagem não existir, usar a div de placeholder em vez de Image
  ```
  Na prática: criar o public/hero-phone-static.webp copiando qualquer PNG do projeto (o next/image vai tentar otimizar; se falhar no build, o fallback vai para div). Para não quebrar o build, podemos usar `unoptimized` no Image ou criar o arquivo como um PNG de 1 pixel.
  
  Comando para criar placeholder de 1px PNG renomeado como WebP (workaround válido para desenvolvimento):
  ```bash
  # Criar um PNG mínimo válido como placeholder (1x1 pixel verde)
  node -e "const fs=require('fs'); const png=Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==','base64'); fs.writeFileSync('public/hero-phone-static.webp', png)"
  ```

**2. Criar `components/PhoneCanvas.tsx`:**
```tsx
'use client'

import { Canvas } from '@react-three/fiber'
import { Float, RoundedBox } from '@react-three/drei'
import { useState, useEffect } from 'react'

const CHAT_MESSAGES = [
  { from: 'user', text: 'Oi! Acabei de almoçar 🍽️', delay: 0 },
  { from: 'bot',  text: 'Ótimo! Me conta o que você comeu?', delay: 800 },
  { from: 'user', text: 'Arroz, frango grelhado e salada', delay: 1600 },
  { from: 'bot',  text: 'Registrado! ✅ Você consumiu 520kcal. Faltam 880kcal para a meta de hoje.', delay: 2400 },
  { from: 'user', text: 'Perfeito, obrigado!', delay: 3200 },
]
const LOOP_DURATION = 5000

function Phone() {
  return (
    <group>
      {/* Corpo do celular */}
      <RoundedBox args={[1.2, 2.4, 0.12]} radius={0.12} smoothness={4}>
        <meshStandardMaterial color="#1a1a1a" roughness={0.3} metalness={0.6} />
      </RoundedBox>
      {/* Tela */}
      <RoundedBox args={[1.0, 2.0, 0.01]} radius={0.06} position={[0, 0, 0.07]}>
        <meshStandardMaterial color="#0a2a0a" roughness={0.1} />
      </RoundedBox>
    </group>
  )
}

export default function PhoneCanvas() {
  const [visibleCount, setVisibleCount] = useState(0)

  useEffect(() => {
    let timeouts: ReturnType<typeof setTimeout>[] = []

    function scheduleMessages() {
      setVisibleCount(0)
      CHAT_MESSAGES.forEach((msg, i) => {
        const t = setTimeout(() => setVisibleCount(i + 1), msg.delay)
        timeouts.push(t)
      })
      const loop = setTimeout(scheduleMessages, LOOP_DURATION)
      timeouts.push(loop)
    }

    scheduleMessages()
    return () => timeouts.forEach(clearTimeout)
  }, [])

  return (
    <div className="relative w-full h-full">
      {/* Canvas 3D */}
      <Canvas camera={{ position: [0, 0, 4], fov: 40 }}>
        <ambientLight intensity={0.6} />
        <directionalLight position={[5, 5, 5]} intensity={1.2} />
        <Float
          speed={1.5}
          rotationIntensity={0.2}
          floatIntensity={0.3}
          floatingRange={[-0.15, 0.15]}
        >
          <Phone />
        </Float>
      </Canvas>

      {/* Chat overlay HTML — posicionado sobre a área da tela do celular */}
      <div
        className="absolute pointer-events-none"
        style={{ top: '12%', left: '22%', right: '22%', bottom: '12%' }}
      >
        <div className="flex flex-col gap-1 w-full px-1 text-[9px] leading-tight">
          {CHAT_MESSAGES.slice(0, visibleCount).map((msg, i) => (
            <div
              key={i}
              className={`max-w-[85%] px-2 py-1 rounded-lg ${
                msg.from === 'user'
                  ? 'self-end bg-floresta text-offwhite'
                  : 'self-start bg-branco text-terra'
              }`}
            >
              {msg.text}
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
```

**3. Criar `components/Hero.tsx`:**
```tsx
'use client'

import dynamic from 'next/dynamic'
import Image from 'next/image'
import { waLink } from '@/lib/whatsapp'

const PhoneCanvas = dynamic(
  () => import('./PhoneCanvas'),
  {
    ssr: false,
    loading: () => (
      <div className="w-full h-[480px] bg-menta/20 rounded-2xl animate-pulse" />
    ),
  }
)

export default function Hero() {
  return (
    <section className="bg-offwhite min-h-screen flex items-center py-16">
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8 grid md:grid-cols-2 gap-8 items-center w-full">

        {/* Texto */}
        <div>
          <h1 className="text-floresta font-bold text-4xl md:text-5xl leading-[1.15] mb-4">
            Seu paciente acompanhado. Você tranquilo.
          </h1>
          <p className="text-claro-br text-lg mb-8">
            O NutriChat cuida do acompanhamento nutricional no WhatsApp —
            registro de refeições, alertas e relatórios automáticos.
          </p>
          <div className="flex flex-col sm:flex-row gap-4">
            <a
              href={waLink()}
              className="bg-floresta text-offwhite px-6 py-3 rounded-lg font-semibold hover:bg-medio transition-colors text-center"
              style={{ minHeight: '44px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}
            >
              Quero começar agora
            </a>
            <a
              href="#como-funciona"
              className="border border-floresta text-floresta px-6 py-3 rounded-lg font-semibold hover:bg-menta transition-colors text-center"
              style={{ minHeight: '44px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}
            >
              Como funciona
            </a>
          </div>
        </div>

        {/* Canvas 3D — apenas md+ (D-12, per LANDING-07) */}
        <div className="hidden md:block h-[480px]">
          <PhoneCanvas />
        </div>

        {/* Imagem estática mobile — apenas < md (D-13) */}
        <div className="block md:hidden mt-8">
          <Image
            src="/hero-phone-static.webp"
            alt="NutriChat no WhatsApp"
            width={300}
            height={480}
            priority
            className="mx-auto rounded-2xl"
          />
        </div>

      </div>
    </section>
  )
}
```

**4. Criar `components/Header.tsx`:**
```tsx
'use client'

import { useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { Menu, X } from 'lucide-react'
import { waLink } from '@/lib/whatsapp'

const navLinks = [
  { href: '#como-funciona', label: 'Como funciona' },
  { href: '#funcionalidades', label: 'Funcionalidades' },
  { href: '#planos', label: 'Planos' },
]

export default function Header() {
  const [menuOpen, setMenuOpen] = useState(false)

  return (
    <header
      className="sticky top-0 z-50"
      style={{ background: 'rgba(240,247,236,0.92)', backdropFilter: 'blur(8px)' }}
    >
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8 h-16 flex items-center justify-between">
        {/* Logo */}
        <a href="/" className="text-xl font-bold text-floresta">
          NutriChat
        </a>

        {/* Nav desktop */}
        <nav className="hidden md:flex items-center gap-8">
          {navLinks.map((link) => (
            <a
              key={link.href}
              href={link.href}
              className="text-terra hover:text-floresta transition-colors text-sm"
            >
              {link.label}
            </a>
          ))}
          <a
            href={waLink()}
            className="bg-floresta text-offwhite px-4 rounded-lg text-sm font-semibold hover:bg-medio transition-colors"
            style={{ minHeight: '44px', display: 'flex', alignItems: 'center' }}
          >
            Falar com nutricionista
          </a>
        </nav>

        {/* Hamburger mobile */}
        <button
          className="md:hidden p-2 text-floresta"
          onClick={() => setMenuOpen(!menuOpen)}
          aria-label="Menu"
        >
          {menuOpen ? <X size={24} /> : <Menu size={24} />}
        </button>
      </div>

      {/* Drawer mobile */}
      <AnimatePresence>
        {menuOpen && (
          <motion.div
            initial={{ x: '100%' }}
            animate={{ x: 0 }}
            exit={{ x: '100%' }}
            transition={{ duration: 0.25 }}
            className="fixed inset-0 top-16 bg-offwhite z-40 flex flex-col gap-6 p-8 md:hidden"
          >
            {navLinks.map((link) => (
              <a
                key={link.href}
                href={link.href}
                className="text-terra text-lg hover:text-floresta transition-colors"
                onClick={() => setMenuOpen(false)}
              >
                {link.label}
              </a>
            ))}
            <a
              href={waLink()}
              className="bg-floresta text-offwhite px-6 py-3 rounded-lg font-semibold text-center hover:bg-medio transition-colors"
              style={{ minHeight: '44px', display: 'flex', alignItems: 'center', justifyContent: 'center' }}
            >
              Falar com nutricionista
            </a>
          </motion.div>
        )}
      </AnimatePresence>
    </header>
  )
}
```

**5. Verificar build final:**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build
```
  </action>
  <verify>
    <automated>cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build 2>&1 | grep -E "(error TS|Build completed|compiled|error)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `components/PhoneCanvas.tsx` linha 1 contém exatamente `'use client'`
    - `components/PhoneCanvas.tsx` contém `Float` importado de `'@react-three/drei'` e props `speed={1.5}` e `rotationIntensity={0.2}` e `floatIntensity={0.3}`
    - `components/PhoneCanvas.tsx` contém os 5 elementos do array CHAT_MESSAGES com os textos exatos do UI-SPEC (incluindo "Oi! Acabei de almoçar" e "520kcal")
    - `components/PhoneCanvas.tsx` contém `LOOP_DURATION = 5000`
    - `components/Hero.tsx` contém `dynamic(` e `ssr: false` na mesma chamada
    - `components/Hero.tsx` contém `hidden md:block` (canvas desktop) e `block md:hidden` (imagem mobile)
    - `components/Header.tsx` linha 1 contém `'use client'`
    - `components/Header.tsx` contém `AnimatePresence` e `sticky top-0`
    - `public/hero-phone-static.webp` existe (qualquer tamanho — placeholder aceito)
    - `npm run build` termina sem linhas contendo "error TS" ou "Failed to compile"
  </acceptance_criteria>
  <done>Header sticky com hamburger mobile, Hero com canvas 3D (desktop) e imagem estática (mobile), PhoneCanvas com celular procedural + chat animado em loop de 5 mensagens — tudo compilando sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → wa.me | Link externo com parâmetro text; conteúdo é encodado pelo app |
| Vercel Build → CDN | Assets estáticos — sem dados de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | .env.local — NEXT_PUBLIC_WHATSAPP_NUMBER | accept | Número é público por design (aparece em link wa.me visível no HTML); não é segredo |
| T-06-02 | Tampering | package.json — dependências npm | mitigate | `npm ci` em build Vercel usa package-lock.json para versões exatas; não usar `latest` |
| T-06-03 | Information Disclosure | logs de build Vercel | accept | Landing é pública; nenhum dado sensível no código do cliente |
</threat_model>

<verification>
Após execução deste plano:
1. `cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build` — deve terminar com sucesso (sem "error TS" ou "Failed to compile")
2. `cat nutrichat-landing/app/globals.css | grep "@theme"` — deve retornar `@theme {`
3. `cat nutrichat-landing/components/PhoneCanvas.tsx | head -1` — deve retornar `'use client'`
4. `cat nutrichat-landing/components/Hero.tsx | grep "ssr: false"` — deve retornar a linha do dynamic import
5. `cat nutrichat-landing/lib/animations.ts | grep "export const fadeInUp"` — deve retornar a linha
6. `cat nutrichat-landing/lib/whatsapp.ts | grep "NEXT_PUBLIC_WHATSAPP_NUMBER"` — deve retornar a linha
</verification>

<success_criteria>
- Repositório nutrichat-landing existe em C:/Users/botel/OneDrive/Desktop/nutrichat-landing
- `npm run build` executa sem erros (TypeScript + Next.js SSG)
- app/globals.css tem @theme com 9 tokens de cor corretos
- PhoneCanvas.tsx tem 'use client', Float com props corretas, CHAT_MESSAGES com 5 mensagens do UI-SPEC, LOOP_DURATION=5000
- Hero.tsx tem dynamic import ssr:false para PhoneCanvas, hidden md:block para canvas, block md:hidden para imagem
- Header.tsx é sticky com hamburger AnimatePresence
- lib/animations.ts e lib/whatsapp.ts exportam as funções corretas
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-landing-page/06-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Versões instaladas (resultado de `npm list --depth=0 | grep -E "three|fiber|drei|framer|lucide"`)
- Decisões tomadas (ex: abordagem para placeholder WebP mobile)
- Qualquer divergência em relação ao plano e razão
</output>

---
phase: 05-painel-do-nutricionista
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nutrichat-painel/package.json
  - nutrichat-painel/vite.config.ts
  - nutrichat-painel/tsconfig.json
  - nutrichat-painel/tsconfig.node.json
  - nutrichat-painel/index.html
  - nutrichat-painel/src/index.css
  - nutrichat-painel/src/main.tsx
  - nutrichat-painel/src/lib/supabase.ts
  - nutrichat-painel/src/components/LoginForm.tsx
  - nutrichat-painel/src/App.tsx
  - nutrichat-painel/.env.local
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Nutricionista vê tela de login ao abrir localhost:5173"
    - "Nutricionista faz login com email/senha e chega à tela principal (Dashboard placeholder)"
    - "Ao recarregar a página estando logado, sessão é restaurada sem nova tela de login"
    - "Ao tentar acessar Dashboard sem sessão, exibe LoginForm (não há cadastro público)"
  artifacts:
    - path: "nutrichat-painel/src/lib/supabase.ts"
      provides: "createClient com persistSession: true"
      contains: "persistSession: true"
    - path: "nutrichat-painel/src/components/LoginForm.tsx"
      provides: "Formulário email/senha com signInWithPassword"
      contains: "signInWithPassword"
    - path: "nutrichat-painel/src/App.tsx"
      provides: "getSession + onAuthStateChange + render condicional"
      contains: "onAuthStateChange"
    - path: "nutrichat-painel/vite.config.ts"
      provides: "Plugin Tailwind v4"
      contains: "@tailwindcss/vite"
    - path: "nutrichat-painel/.env.local"
      provides: "Variáveis de ambiente do frontend"
      contains: "VITE_SUPABASE_URL"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/supabase.ts"
      via: "import supabase"
      pattern: "supabase\\.auth\\.getSession"
    - from: "src/components/LoginForm.tsx"
      to: "src/lib/supabase.ts"
      via: "signInWithPassword"
      pattern: "signInWithPassword"
---

<objective>
Criar o repositório `nutrichat-painel` (React + Vite + TypeScript + Tailwind v4) e implementar autenticação Supabase Auth com persistência de sessão e proteção de rota via estado em App.tsx.

Purpose: Estabelecer o scaffold completo do painel e o fluxo de autenticação. Plans 05-02 e 05-03 constroem sobre este scaffold.
Output: Diretório `nutrichat-painel/` com projeto funcional rodando em localhost:5173, mostrando LoginForm ou Dashboard conforme estado de sessão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-painel-do-nutricionista/05-CONTEXT.md
@.planning/phases/05-painel-do-nutricionista/05-RESEARCH.md

<interfaces>
<!-- Padrões extraídos do RESEARCH.md para uso direto pelo executor -->

<!-- supabase.ts -->
```typescript
import { createClient } from '@supabase/supabase-js';
export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY,
  { auth: { storage: localStorage, persistSession: true } }
);
```

<!-- App.tsx — lógica central de auth (sem React Router — D-05) -->
```typescript
// Render condicional: session ? <Dashboard /> : <LoginForm />
// getSession() no useEffect inicial + onAuthStateChange para reatividade
```

<!-- Paleta NutriChat (D-04) -->
// Header: #2D5016 | Background: #F0F7EC | Tipografia: #6B3D1E

<!-- Tailwind v4 setup (Pattern 7 do RESEARCH.md) -->
// vite.config.ts: plugins: [react(), tailwindcss()]
// src/index.css: @import "tailwindcss"; + @theme { --color-floresta: #2D5016; ... }
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase Auth | `signInWithPassword` — credenciais trafegam via HTTPS para Supabase Cloud |
| Browser localStorage | JWT armazenado pelo SDK Supabase; `SUPABASE_ANON_KEY` exposta no bundle (aceitável — anon key é pública por design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing | LoginForm / signInWithPassword | mitigate | Delegado ao Supabase Auth — senhas nunca manejadas manualmente; rate limiting nativo do Supabase |
| T-05-01-02 | Elevation of Privilege | App.tsx / render condicional | mitigate | `getSession()` na montagem + `onAuthStateChange` garante que Dashboard nunca renderiza sem sessão válida; sem rota pública exposta |
| T-05-01-03 | Information Disclosure | .env.local / VITE_ vars | accept | `VITE_SUPABASE_ANON_KEY` é pública por design (Supabase); `VITE_PANEL_API_KEY` é segredo de desenvolvimento — não commitar `.env.local` (já no .gitignore do Vite scaffold) |
| T-05-01-04 | Tampering | SUPABASE_SERVICE_KEY no frontend | mitigate | Service key NUNCA recebe prefixo VITE_; apenas anon key no frontend |
</threat_model>

<tasks>

<task type="auto">
  <id>05-01-T1</id>
  <title>Scaffold nutrichat-painel com Vite + React + TS + Tailwind v4</title>
  <read_first>
    - .planning/phases/05-painel-do-nutricionista/05-RESEARCH.md (seções Standard Stack, Pattern 7, Dockerfile)
    - .planning/phases/05-painel-do-nutricionista/05-CONTEXT.md (D-01, D-02, D-04, D-06)
  </read_first>
  <action>
Criar o diretório `nutrichat-painel` na raiz do repositório (ao lado de `backend/`) e scaffoldar o projeto React + Vite + TypeScript + Tailwind v4.

**Passo 1 — Criar projeto Vite:**
```bash
cd /c/Users/botel/OneDrive/Desktop/nutri_fit
npm create vite@latest nutrichat-painel -- --template react-ts
cd nutrichat-painel
npm install
npm install @supabase/supabase-js
npm install tailwindcss @tailwindcss/vite
```

**Passo 2 — vite.config.ts** (substituir o gerado):
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: { port: 5173 },
});
```

**Passo 3 — src/index.css** (substituir completamente):
```css
@import "tailwindcss";

@theme {
  --color-floresta: #2D5016;
  --color-offwhite: #F0F7EC;
  --color-terra: #6B3D1E;
}

* { box-sizing: border-box; }
body { margin: 0; font-family: sans-serif; }
```

**Passo 4 — Remover arquivos desnecessários do scaffold:**
- Deletar `src/App.css`
- Deletar `src/assets/react.svg`
- Deletar conteúdo de `public/vite.svg` (manter o arquivo vazio ou substituir pelo favicon do NutriChat)

**Passo 5 — src/main.tsx** (verificar que importa `./index.css`):
```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './index.css';
import { App } from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

**Passo 6 — .env.local** (criar na raiz do nutrichat-painel, NÃO commitar):
```
VITE_SUPABASE_URL=https://iqpoohthfhmjkvrqxzns.supabase.co
VITE_SUPABASE_ANON_KEY=PREENCHER_COM_ANON_KEY_DO_SUPABASE
VITE_BACKEND_URL=http://localhost:3001
VITE_PANEL_API_KEY=PREENCHER_COM_CHAVE_GERADA
```
Adicionar `.env.local` ao `.gitignore` do nutrichat-painel (já está incluído no .gitignore padrão do Vite).

**Passo 7 — Criar src/lib/supabase.ts:**
```typescript
import { createClient } from '@supabase/supabase-js';

export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY,
  {
    auth: {
      storage: localStorage,
      persistSession: true,
    },
  }
);
```
  </action>
  <acceptance_criteria>
    - nutrichat-painel/package.json contém `"@supabase/supabase-js"` nas dependencies
    - nutrichat-painel/package.json contém `"tailwindcss"` e `"@tailwindcss/vite"` nas dependencies
    - nutrichat-painel/vite.config.ts contém `tailwindcss()` como plugin
    - nutrichat-painel/src/index.css contém `@import "tailwindcss"`
    - nutrichat-painel/src/index.css contém `--color-floresta: #2D5016`
    - nutrichat-painel/src/lib/supabase.ts contém `persistSession: true`
    - nutrichat-painel/.env.local existe e contém `VITE_SUPABASE_URL`
    - `npm run build` dentro de nutrichat-painel sai com código 0 (sem erros de TypeScript)
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/botel/OneDrive/Desktop/nutri_fit/nutrichat-painel && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Projeto Vite buildando sem erros, Tailwind v4 configurado, cliente Supabase criado com persistSession.</done>
</task>

<task type="auto">
  <id>05-01-T2</id>
  <title>Implementar LoginForm + App.tsx com auth state management</title>
  <read_first>
    - nutrichat-painel/src/lib/supabase.ts (cliente criado na T1)
    - .planning/phases/05-painel-do-nutricionista/05-RESEARCH.md (Pattern 1 e Pattern 2, Code Examples LoginForm.tsx)
    - .planning/phases/05-painel-do-nutricionista/05-CONTEXT.md (D-05, D-10, D-11)
  </read_first>
  <action>
Criar `LoginForm.tsx` e `App.tsx` implementando o fluxo de autenticação conforme D-10 e D-11.

**Arquivo: nutrichat-painel/src/components/LoginForm.tsx**
```tsx
import { useState } from 'react';
import { supabase } from '../lib/supabase';

export function LoginForm() {
  const [email, setEmail] = useState('');
  const [senha, setSenha] = useState('');
  const [erro, setErro] = useState('');
  const [loading, setLoading] = useState(false);

  async function handleLogin(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setErro('');
    const { error } = await supabase.auth.signInWithPassword({ email, password: senha });
    if (error) setErro('Email ou senha incorretos');
    setLoading(false);
  }

  return (
    <div className="min-h-screen flex items-center justify-center" style={{ background: '#F0F7EC' }}>
      <form onSubmit={handleLogin} className="bg-white rounded-xl shadow-lg p-8 w-full max-w-sm">
        <div className="flex items-center gap-2 mb-6">
          <span className="text-2xl font-bold" style={{ color: '#6B3D1E' }}>🥗 NutriChat</span>
        </div>
        <p className="text-sm mb-4" style={{ color: '#6B3D1E' }}>Painel do Nutricionista</p>
        <input
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="Email"
          required
          className="w-full border border-gray-300 rounded px-3 py-2 mb-3 focus:outline-none focus:ring-2"
          style={{ '--tw-ring-color': '#2D5016' } as React.CSSProperties}
        />
        <input
          type="password"
          value={senha}
          onChange={(e) => setSenha(e.target.value)}
          placeholder="Senha"
          required
          className="w-full border border-gray-300 rounded px-3 py-2 mb-4 focus:outline-none focus:ring-2"
        />
        {erro && <p className="text-red-600 text-sm mb-3">{erro}</p>}
        <button
          type="submit"
          disabled={loading}
          className="w-full py-2 rounded text-white font-semibold disabled:opacity-60 cursor-pointer"
          style={{ background: '#2D5016' }}
        >
          {loading ? 'Entrando...' : 'Entrar'}
        </button>
      </form>
    </div>
  );
}
```

**Arquivo: nutrichat-painel/src/App.tsx**

Implementar estado de auth centralizado. Dashboard é um placeholder nesta tarefa (será substituído na 05-03):

```tsx
import { useState, useEffect } from 'react';
import { Session } from '@supabase/supabase-js';
import { supabase } from './lib/supabase';
import { LoginForm } from './components/LoginForm';

// Dashboard placeholder — será substituído no plano 05-03
function DashboardPlaceholder() {
  async function handleLogout() {
    await supabase.auth.signOut();
  }
  return (
    <div style={{ background: '#F0F7EC', minHeight: '100vh' }}>
      <header
        className="px-6 py-4 flex items-center justify-between text-white"
        style={{ background: '#2D5016' }}
      >
        <span className="font-bold text-lg">🥗 NutriChat — Painel</span>
        <button
          onClick={handleLogout}
          className="text-sm underline opacity-80 hover:opacity-100 cursor-pointer"
        >
          Sair
        </button>
      </header>
      <main className="p-6">
        <p style={{ color: '#6B3D1E' }}>Dashboard carregando... (plano 05-03)</p>
      </main>
    </div>
  );
}

export function App() {
  const [session, setSession] = useState<Session | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    // Carrega sessão inicial (persiste entre reloads via localStorage)
    supabase.auth.getSession().then(({ data: { session } }) => {
      setSession(session);
      setLoading(false);
    });

    // Mantém estado reativo a login/logout
    const {
      data: { subscription },
    } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session);
    });

    return () => subscription.unsubscribe();
  }, []);

  if (loading) {
    return (
      <div
        className="min-h-screen flex items-center justify-center"
        style={{ background: '#F0F7EC' }}
      >
        <p style={{ color: '#6B3D1E' }}>Carregando...</p>
      </div>
    );
  }

  return session ? <DashboardPlaceholder /> : <LoginForm />;
}
```
  </action>
  <acceptance_criteria>
    - nutrichat-painel/src/components/LoginForm.tsx contém `signInWithPassword`
    - nutrichat-painel/src/components/LoginForm.tsx contém `style={{ background: '#2D5016' }}` no botão
    - nutrichat-painel/src/App.tsx contém `onAuthStateChange`
    - nutrichat-painel/src/App.tsx contém `getSession`
    - nutrichat-painel/src/App.tsx contém `session ? <DashboardPlaceholder` (render condicional)
    - `npm run build` em nutrichat-painel sai com código 0
    - `npm run typecheck` (ou `npx tsc --noEmit`) em nutrichat-painel sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/botel/OneDrive/Desktop/nutri_fit/nutrichat-painel && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>LoginForm renderiza tela de login com email/senha; App.tsx gerencia sessão com getSession + onAuthStateChange; tela principal exibe DashboardPlaceholder após login bem-sucedido; TypeScript sem erros.</done>
</task>

</tasks>

<verification>
1. `cd nutrichat-painel && npm run build` — deve completar sem erros TypeScript
2. `npx tsc --noEmit` — zero erros de tipo
3. `npm run dev` — servidor inicia em http://localhost:5173
4. Browser em localhost:5173 — deve exibir LoginForm (fundo verde off-white, formulário branco centralizado)
5. Login com credencial inválida — deve exibir "Email ou senha incorretos"
6. Login com credencial válida (usuário criado manualmente no Supabase Auth) — deve exibir DashboardPlaceholder com header verde
7. Reload após login — sessão restaurada sem nova tela de login (AUTH-02)
8. `grep -r "signUp" nutrichat-painel/src` — deve retornar vazio (AUTH-03: sem cadastro público)
</verification>

<success_criteria>
- Scaffold nutrichat-painel criado com React 19, Vite 8, TypeScript, Tailwind v4
- Cliente Supabase configurado com persistSession: true (AUTH-01, AUTH-02)
- LoginForm usa signInWithPassword — sem signUp exposto (AUTH-03)
- App.tsx gerencia ciclo de vida de sessão com getSession + onAuthStateChange
- Render condicional: sessão presente → DashboardPlaceholder; ausente → LoginForm
- TypeScript compila sem erros
- .env.local criado com as 4 variáveis VITE_ necessárias
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-painel-do-nutricionista/05-01-SUMMARY.md` com:
- Arquivos criados e suas responsabilidades
- Estado de `enviarBoasVindas` verificado (para orientar plano 05-02)
- Qualquer desvio do plano e justificativa
- Resultado dos comandos de verificação
</output>

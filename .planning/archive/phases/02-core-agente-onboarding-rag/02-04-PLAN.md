---
phase: 2
plan: "02-04"
title: "Calculos de TMB, hidratacao e creatina"
type: execute
wave: 2
depends_on:
  - "02-03"
files_modified:
  - backend/src/services/calculos.ts
  - backend/src/services/agent.ts
autonomous: true
requirements:
  - AGENT-10
  - AGENT-11
  - AGENT-12

must_haves:
  truths:
    - "calcularTMB() implementa formula Mifflin-St Jeor com fator de atividade correto"
    - "calcularHidratacao() retorna meta diaria em ml (35ml/kg) com sugestao de distribuicao"
    - "calcularCreatina() retorna 0.03g/kg quando suplementos do paciente nao menciona creatina com dose"
    - "Apos entrevista completa (etapa 7 -> completa), agente calcula e envia TMB, hidratacao e creatina automaticamente"
    - "Resultados sao salvos em entrevista_dados para nao recalcular a cada mensagem"
  artifacts:
    - path: "backend/src/services/calculos.ts"
      provides: "Funcoes puras de calculo nutricional (testavel, sem efeitos colaterais)"
      contains: "calcularTMB"
    - path: "backend/src/services/agent.ts"
      provides: "Logica de envio dos calculos apos entrevista completa"
      contains: "calcularTMB"
  key_links:
    - from: "backend/src/services/calculos.ts"
      to: "backend/src/services/agent.ts"
      via: "importacao e uso logo apos entrevista_status -> completa"
      pattern: "calcularTMB.*dados"
---

<objective>
Implementar os calculos nutricionais personalizados que o agente envia automaticamente apos a conclusao da entrevista inicial: Taxa Metabolica Basal (Mifflin-St Jeor), meta de hidratacao diaria e dosagem de creatina.

Purpose: Entrega de valor imediata apos o onboarding — o paciente recebe seus numeros personalizados logo ao finalizar a entrevista, sem precisar perguntar.
Output: calculos.ts com funcoes puras, agentService atualizado para enviar calculos pos-entrevista.
</objective>

<context>
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar calculosService com funcoes puras de calculo nutricional</name>
  <files>
    backend/src/services/calculos.ts
  </files>
  <action>
Implementar as tres funcoes de calculo como funcoes puras (sem I/O, completamente testaveis).

**Arquivo: backend/src/services/calculos.ts**

```typescript
export interface DadosEntrevista {
  idade: number;
  sexo: 'masculino' | 'feminino';
  peso_kg: number;
  altura_cm: number;
  atividade_tipo: string;
  atividade_frequencia?: string;
  suplementos?: string[];
}

export interface ResultadoTMB {
  tmb_kcal: number;
  tdee_kcal: number;
  fator_atividade: number;
  nivel_atividade: string;
}

export interface ResultadoHidratacao {
  meta_ml: number;
  distribuicao: string[];
}

export interface ResultadoCreatina {
  dose_g: number;
  fonte: 'calculada' | 'nutricionista';
  mensagem: string;
}

// Fatores de atividade (Mifflin-St Jeor / Harris-Benedict)
const FATORES_ATIVIDADE: Array<{ palavras: string[]; fator: number; nivel: string }> = [
  { palavras: ['sedentario', 'sedentária', 'nao pratico', 'nao faco'], fator: 1.2, nivel: 'Sedentario' },
  { palavras: ['caminhada', 'leve', '1x', '2x', 'uma vez', 'duas vezes'], fator: 1.375, nivel: 'Levemente ativo' },
  { palavras: ['3x', '4x', 'tres', 'quatro', 'moderado', 'musculacao', 'musculação'], fator: 1.55, nivel: 'Moderadamente ativo' },
  { palavras: ['5x', '6x', 'cinco', 'seis', 'intenso', 'diario', 'diário'], fator: 1.725, nivel: 'Muito ativo' },
  { palavras: ['7x', 'todo dia', 'duas vezes ao dia', 'atletismo'], fator: 1.9, nivel: 'Extremamente ativo' },
];

function detectarFatorAtividade(atividadeTipo: string, frequencia?: string): { fator: number; nivel: string } {
  const texto = `${atividadeTipo} ${frequencia ?? ''}`.toLowerCase();

  for (const entry of FATORES_ATIVIDADE) {
    if (entry.palavras.some((p) => texto.includes(p))) {
      return { fator: entry.fator, nivel: entry.nivel };
    }
  }
  // Default moderado (musculacao sem frequencia especificada)
  return { fator: 1.55, nivel: 'Moderadamente ativo' };
}

export function calcularTMB(dados: DadosEntrevista): ResultadoTMB {
  // Formula Mifflin-St Jeor
  let tmb: number;
  if (dados.sexo === 'masculino') {
    tmb = 10 * dados.peso_kg + 6.25 * dados.altura_cm - 5 * dados.idade + 5;
  } else {
    tmb = 10 * dados.peso_kg + 6.25 * dados.altura_cm - 5 * dados.idade - 161;
  }

  const { fator, nivel } = detectarFatorAtividade(dados.atividade_tipo, dados.atividade_frequencia);
  const tdee = Math.round(tmb * fator);

  return {
    tmb_kcal: Math.round(tmb),
    tdee_kcal: tdee,
    fator_atividade: fator,
    nivel_atividade: nivel,
  };
}

export function calcularHidratacao(peso_kg: number): ResultadoHidratacao {
  const meta_ml = Math.round(peso_kg * 35);

  // Sugestao de distribuicao: 8 porcoes ao longo do dia
  const porcao_ml = Math.round(meta_ml / 8);
  const distribuicao = [
    `☀️ Ao acordar: ${porcao_ml}ml`,
    `🍳 Cafe da manha: ${porcao_ml}ml`,
    `🌤️ Meio da manha: ${porcao_ml}ml`,
    `🍽️ Almoco: ${porcao_ml}ml`,
    `☕ Tarde: ${porcao_ml}ml`,
    `🌙 Jantar: ${porcao_ml}ml`,
    `🌃 Noite: ${porcao_ml}ml`,
    `🛌 Antes de dormir: ${meta_ml - porcao_ml * 7}ml`,
  ];

  return { meta_ml, distribuicao };
}

export function calcularCreatina(
  peso_kg: number,
  suplementos?: string[],
): ResultadoCreatina {
  if (suplementos && suplementos.length > 0) {
    const textoSuplementos = suplementos.join(' ').toLowerCase();

    // Verificar se o nutricionista ja definiu uma dose de creatina
    const matchDose = textoSuplementos.match(/creatina[^\d]*(\d+(?:[.,]\d+)?)\s*g/);
    if (matchDose) {
      const dose = parseFloat(matchDose[1].replace(',', '.'));
      return {
        dose_g: dose,
        fonte: 'nutricionista',
        mensagem: `${dose}g/dia (conforme prescrito pelo seu nutricionista)`,
      };
    }

    // Creatina mencionada mas sem dose especifica
    if (textoSuplementos.includes('creatina')) {
      const dose = Math.round(peso_kg * 0.03 * 10) / 10;
      return {
        dose_g: dose,
        fonte: 'calculada',
        mensagem: `${dose}g/dia (0,03g/kg — dose padrao de manutencao)`,
      };
    }
  }

  // Sem creatina nos suplementos — calcular e sugerir
  const dose = Math.round(peso_kg * 0.03 * 10) / 10;
  return {
    dose_g: dose,
    fonte: 'calculada',
    mensagem: `${dose}g/dia (sugestao baseada em 0,03g/kg — confirme com seu nutricionista)`,
  };
}

export function formatarMensagemCalculos(
  tmb: ResultadoTMB,
  hidratacao: ResultadoHidratacao,
  creatina: ResultadoCreatina,
): string {
  return (
    `🎯 *Seus numeros personalizados:*\n\n` +
    `🔥 *Metabolismo (TMB):* ${tmb.tmb_kcal} kcal/dia\n` +
    `⚡ *Gasto total (TDEE):* ${tmb.tdee_kcal} kcal/dia\n` +
    `🏃 Nivel de atividade: ${tmb.nivel_atividade}\n\n` +
    `💧 *Hidratacao diaria:* ${hidratacao.meta_ml}ml\n` +
    `_Distribuicao sugerida:_\n${hidratacao.distribuicao.slice(0, 4).join('\n')}\n_(e mais 4 porcoes ao longo do dia)_\n\n` +
    `💊 *Creatina:* ${creatina.mensagem}\n\n` +
    `_Esses dados serao usados para personalizar suas respostas. Comece registrando sua primeira refeicao!_ 🥗`
  );
}
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/calculos.ts &&
      grep -q "calcularTMB" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/calculos.ts &&
      grep -q "Mifflin" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/calculos.ts &&
      grep -q "0.03" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/calculos.ts &&
      grep -q "35" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/calculos.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/calculos.ts exporta calcularTMB, calcularHidratacao, calcularCreatina, formatarMensagemCalculos
    - calcularTMB usa formula Mifflin-St Jeor com fator de atividade detectado do texto livre
    - calcularHidratacao retorna 35ml/kg arredondado com distribuicao em 8 porcoes
    - calcularCreatina retorna dose do nutricionista se definida, senao 0,03g/kg
    - formatarMensagemCalculos retorna string formatada para WhatsApp com emojis
    - Todas as funcoes sao puras (sem import de env, supabase, evolution)
    - npm run typecheck sem erros em calculos.ts
  </acceptance_criteria>
  <done>Servico de calculos nutricionais implementado com funcoes puras e testaveis.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar calculos no agentService apos conclusao da entrevista</name>
  <files>
    backend/src/services/agent.ts
  </files>
  <read_first>
    - backend/src/services/agent.ts (localizar o bloco "Entrevista completa" — etapa > 7)
    - backend/src/services/calculos.ts (importar calcularTMB, calcularHidratacao, calcularCreatina, formatarMensagemCalculos)
    - backend/src/services/conversation.ts (atualizarEstado para salvar resultados dos calculos)
  </read_first>
  <action>
Modificar o bloco de conclusao da entrevista no agentService para calcular e enviar os numeros do paciente.

Localizar no `backend/src/services/agent.ts` o bloco onde a entrevista e marcada como completa:

```typescript
if (proximaEtapa > 7) {
  // Entrevista completa
  await atualizarEstado(paciente.id, {
    status: 'completa',
    etapa: 7,
    dados: novoDado,
  });
  await sendText(/* ... */);
}
```

Substituir por:

```typescript
import {
  calcularTMB,
  calcularHidratacao,
  calcularCreatina,
  formatarMensagemCalculos,
  DadosEntrevista,
} from './calculos';

// ... dentro do bloco if (proximaEtapa > 7):

if (proximaEtapa > 7) {
  // Merge final dos dados
  const dadosCompletos = { ...estado.dados, ...novoDado } as DadosEntrevista;

  await atualizarEstado(paciente.id, {
    status: 'completa',
    etapa: 7,
    dados: novoDado,
  });

  // Calcular e salvar resultados
  const tmb = calcularTMB(dadosCompletos);
  const hidratacao = calcularHidratacao(dadosCompletos.peso_kg);
  const creatina = calcularCreatina(dadosCompletos.peso_kg, dadosCompletos.suplementos);

  // Salvar calculos em entrevista_dados para reutilizacao
  await atualizarEstado(paciente.id, {
    dados: {
      tmb_kcal: tmb.tmb_kcal,
      tdee_kcal: tmb.tdee_kcal,
      hidratacao_ml: hidratacao.meta_ml,
      creatina_g: creatina.dose_g,
    },
  });

  // Enviar confirmacao da entrevista
  await sendText(
    phone,
    `✅ Perfeito, ${paciente.nome}! Entrevista concluida.\n\n` +
    `Calculando seus numeros personalizados... ⏳`,
  );

  // Pequeno delay para parecer processamento (UX)
  await new Promise((resolve) => setTimeout(resolve, 1500));

  // Enviar calculos
  await sendText(phone, formatarMensagemCalculos(tmb, hidratacao, creatina));

  // Instrucoes de uso
  await sendText(
    phone,
    `💡 *Como usar o NutriChat:*\n\n` +
    `• _"comi 200g de frango com arroz"_ — registro por texto\n` +
    `• 🎤 Audio descrevendo sua refeicao\n` +
    `• 📸 Foto do prato ou codigo de barras\n` +
    `• _"qual e minha dieta?"_ — consulta ao PDF\n\n` +
    `Vamos la! 🚀`,
  );
}
```

**IMPORTANTE:** Adicionar o import de calculos.ts no topo do arquivo agent.ts.
  </action>
  <verify>
    <automated>
      grep -q "calcularTMB" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts &&
      grep -q "formatarMensagemCalculos" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts &&
      cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck 2>&1 | grep -c "error" || echo "0"
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/agent.ts importa e usa calcularTMB, calcularHidratacao, calcularCreatina
    - Apos etapa 7 (conclusao), agente calcula e salva TMB, hidratacao e creatina em entrevista_dados
    - Agente envia 3 mensagens pos-entrevista: confirmacao, calculos formatados, instrucoes de uso
    - Resultados salvos incluem: tmb_kcal, tdee_kcal, hidratacao_ml, creatina_g
    - npm run typecheck sem erros
  </acceptance_criteria>
  <done>Calculos integrados ao fluxo de conclusao da entrevista. Paciente recebe seus numeros personalizados automaticamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova boundary — este plano nao adiciona integracao externa nova. Usa apenas codigo interno.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | calculos.ts | accept | Funcoes puras — sem estado externo. Inputs validados na entrevista antes de chegar aqui. |
| T-02-04-02 | Information Disclosure | Mensagem de calculos | accept | Dados pessoais (peso, altura) usados apenas no backend. Mensagem enviada ao proprio paciente. |
</threat_model>

<verification>
Apos execucao, testar o fluxo completo:
1. npm run typecheck sem erros
2. Responder as 7 perguntas da entrevista
3. Verificar que o bot envia a mensagem com TMB, hidratacao e creatina formatados
4. Verificar no Supabase Dashboard: entrevista_dados contem tmb_kcal, tdee_kcal, hidratacao_ml, creatina_g
5. Teste unitario manual: calcularTMB({sexo:'masculino', peso_kg:80, altura_cm:175, idade:30, atividade_tipo:'musculacao 3x'}) deve retornar tmb_kcal proximo de 1867
</verification>

<success_criteria>
- backend/src/services/calculos.ts com calcularTMB (Mifflin-St Jeor), calcularHidratacao (35ml/kg), calcularCreatina (0.03g/kg), formatarMensagemCalculos
- backend/src/services/agent.ts integra calculos pos-entrevista
- Calculos salvos em entrevista_dados: tmb_kcal, tdee_kcal, hidratacao_ml, creatina_g
- Agente envia 3 mensagens formatadas apos etapa 7
- npm run typecheck sem erros
</success_criteria>

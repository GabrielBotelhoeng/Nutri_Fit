---
phase: 06-landing-page
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - nutrichat-landing/components/Plans.tsx
  - nutrichat-landing/components/Footer.tsx
  - nutrichat-landing/.gitignore
  - nutrichat-landing/README.md
autonomous: false
requirements:
  - LANDING-05
  - LANDING-06
  - LANDING-08

must_haves:
  truths:
    - "Seção de planos exibe 4 cards: 1 mês (R$ 330), 3 meses (R$ 222, destaque), 6 meses (R$ 130), 12 meses (R$ 89,90)"
    - "Card de 3 meses tem badge 'Mais Popular', borda verde floresta (#2D5016) e shadow elevado"
    - "Cada botão CTA abre link wa.me com mensagem pré-preenchida correta para o respectivo plano"
    - "Rodapé tem bg-floresta com texto offwhite, tagline e copyright 2026"
    - "Repositório nutrichat-landing está no GitHub e Vercel faz auto-deploy a partir do branch main"
  artifacts:
    - path: "nutrichat-landing/components/Plans.tsx"
      provides: "4 cards de planos com CTA wa.me e badge Mais Popular"
      contains: "id=\"planos\""
    - path: "nutrichat-landing/components/Footer.tsx"
      provides: "Rodapé com bg-floresta"
      contains: "bg-floresta"
    - path: "nutrichat-landing/.env.local"
      provides: "NEXT_PUBLIC_WHATSAPP_NUMBER para desenvolvimento local"
      contains: "NEXT_PUBLIC_WHATSAPP_NUMBER"
  key_links:
    - from: "components/Plans.tsx"
      to: "lib/whatsapp.ts"
      via: "import { waLink } from '@/lib/whatsapp'"
      pattern: "from '@/lib/whatsapp'"
    - from: "Vercel"
      to: "GitHub repo nutrichat-landing"
      via: "GitHub integration — auto-deploy on push to main"
      pattern: "NEXT_PUBLIC_WHATSAPP_NUMBER no Vercel dashboard"

user_setup:
  - service: vercel
    why: "Deploy automático da landing page (LANDING-08 — per D-01)"
    env_vars:
      - name: NEXT_PUBLIC_WHATSAPP_NUMBER
        source: "Número do WhatsApp do nutricionista (ex: 5562999990000 — DDI+DDD+número sem + ou espaços)"
    dashboard_config:
      - task: "Criar projeto na Vercel importando repositório nutrichat-landing do GitHub"
        location: "vercel.com/new → Import Git Repository"
      - task: "Adicionar env var NEXT_PUBLIC_WHATSAPP_NUMBER com o número real do nutricionista"
        location: "Vercel project → Settings → Environment Variables"
---

<objective>
Implementar a seção de Planos (4 cards com CTA WhatsApp, plano 3 meses em destaque com badge "Mais Popular") e o Rodapé (bg floresta, tagline, copyright). Em seguida, inicializar o repositório Git, fazer push para GitHub e configurar o auto-deploy na Vercel — entregando LANDING-08 (deploy automático).

Purpose: Completar a landing page com o elemento de conversão principal (seção de planos + CTAs WhatsApp) e colocar o site em produção na Vercel com deploy automático a cada push.

Output: landing page completa deployada na Vercel com URL pública funcionando, auto-deploy configurado do GitHub main.
</objective>

<execution_context>
@C:/Users/botel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-CONTEXT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-01-SUMMARY.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-02-SUMMARY.md

<interfaces>
<!-- Contratos do plano 06-01 que este plano consome -->

De lib/whatsapp.ts (criado em 06-01):
```ts
export function waLink(months?: number): string {
  const phone = process.env.NEXT_PUBLIC_WHATSAPP_NUMBER ?? '5500000000000'
  const text = months
    ? encodeURIComponent(`Olá! Tenho interesse no plano de ${months} ${months === 1 ? 'mês' : 'meses'} do Nutri Chat. Pode me passar mais informações?`)
    : encodeURIComponent('Olá! Quero começar com o NutriChat.')
  return `https://wa.me/${phone}?text=${text}`
}
```

Dados exatos dos planos (06-CONTEXT.md D-10 + 06-UI-SPEC.md §Planos):
- 1 mês: R$ 330/mês — sem destaque
- 3 meses: R$ 222/mês — featured: true (badge "Mais Popular", borda floresta, shadow 0 8px 32px rgba(45,80,22,0.16), lg:scale-105)
- 6 meses: R$ 130/mês — sem destaque
- 12 meses: R$ 89,90/mês — sem destaque

Badge "Mais Popular" (06-UI-SPEC.md §Planos):
- position: absolute, top: -12px (-top-3), center (left-1/2 -translate-x-1/2)
- bg: #2D5016 (bg-floresta), text: #F0F7EC (text-offwhite)
- font: 12px bold, padding: 4px 12px (badge-inline token), border-radius: 99px

Rodapé (06-UI-SPEC.md §Rodapé):
- bg: #2D5016 (bg-floresta), text: #F0F7EC (text-offwhite)
- Logo "NutriChat" + tagline: "Seu assistente nutricional inteligente no WhatsApp"
- Links: contato (email), WhatsApp, GitHub
- Copyright: "© 2026 NutriChat"
- Hover nos links: text-claro (#7DB85A)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Plans.tsx (4 cards + badge + CTA) e Footer.tsx (bg floresta)</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-PATTERNS.md — Seção "Plans.tsx" (linhas 591-686), "Footer.tsx" (linhas 691-729)
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-UI-SPEC.md — "Seção Planos" (linhas 181-195), "Rodapé" (linhas 197-201), "Copywriting Contract" (linhas 234-246)
  </read_first>
  <files>
    nutrichat-landing/components/Plans.tsx,
    nutrichat-landing/components/Footer.tsx
  </files>
  <action>
**1. Substituir o stub de Plans.tsx com a implementação completa:**
```tsx
'use client'

import { motion } from 'framer-motion'
import { waLink } from '@/lib/whatsapp'
import { fadeInUp, staggerContainer } from '@/lib/animations'

const PLANS = [
  { months: 1,  price: 'R$ 330',   perMonth: '/mês', featured: false },
  { months: 3,  price: 'R$ 222',   perMonth: '/mês', featured: true  },
  { months: 6,  price: 'R$ 130',   perMonth: '/mês', featured: false },
  { months: 12, price: 'R$ 89,90', perMonth: '/mês', featured: false },
]

export default function Plans() {
  return (
    <section id="planos" className="py-16 bg-offwhite">
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8">
        <h2 className="text-floresta font-bold text-3xl text-center mb-4">
          Escolha o plano ideal
        </h2>
        <p className="text-claro-br text-center mb-16">
          Acesso completo ao NutriChat. Sem apps para instalar.
        </p>
        <motion.div
          className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-6 items-start"
          variants={staggerContainer}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true, amount: 0.2 }}
        >
          {PLANS.map((plan) => (
            <motion.div
              key={plan.months}
              variants={fadeInUp}
              className={`relative bg-branco rounded-xl p-6 flex flex-col gap-4 transition-transform duration-200 hover:-translate-y-1 ${
                plan.featured
                  ? 'lg:scale-105 border-2 border-floresta'
                  : 'border border-menta'
              }`}
              style={
                plan.featured
                  ? { boxShadow: '0 8px 32px rgba(45,80,22,0.16)' }
                  : {}
              }
            >
              {/* Badge "Mais Popular" — apenas no plano featured (per D-09) */}
              {plan.featured && (
                <span
                  className="absolute -top-3 left-1/2 -translate-x-1/2 bg-floresta text-offwhite text-xs font-bold rounded-full whitespace-nowrap"
                  style={{ padding: '4px 12px' }}
                >
                  Mais Popular
                </span>
              )}

              {/* Título do plano */}
              <h3 className="font-bold text-terra text-lg">
                {plan.months} {plan.months === 1 ? 'mês' : 'meses'}
              </h3>

              {/* Preço */}
              <div className="flex items-end gap-1">
                <span
                  className={`font-bold text-floresta ${
                    plan.featured ? 'text-4xl' : 'text-2xl'
                  }`}
                >
                  {plan.price}
                </span>
                <span className="text-claro-br text-sm pb-1">{plan.perMonth}</span>
              </div>

              {/* CTA — link wa.me com mensagem pré-preenchida (per D-11) */}
              <a
                href={waLink(plan.months)}
                className="mt-auto w-full bg-floresta text-offwhite rounded-lg text-center text-sm font-semibold hover:bg-medio transition-colors"
                style={{
                  minHeight: '44px',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                Quero esse plano
              </a>
            </motion.div>
          ))}
        </motion.div>
      </div>
    </section>
  )
}
```

**2. Substituir o stub de Footer.tsx com a implementação completa:**
```tsx
export default function Footer() {
  const phone = process.env.NEXT_PUBLIC_WHATSAPP_NUMBER ?? '5500000000000'

  return (
    <footer className="bg-floresta text-offwhite py-12">
      <div className="max-w-[1152px] mx-auto px-4 md:px-6 lg:px-8">
        <div className="flex flex-col md:flex-row justify-between items-start gap-8">
          {/* Logo + tagline */}
          <div>
            <span className="text-xl font-bold">NutriChat</span>
            <p className="text-sm mt-2 opacity-80">
              Seu assistente nutricional inteligente no WhatsApp
            </p>
          </div>

          {/* Links */}
          <div className="flex gap-6 text-sm">
            <a
              href="mailto:contato@nutrichat.com.br"
              className="hover:text-claro transition-colors"
            >
              Contato
            </a>
            <a
              href={`https://wa.me/${phone}`}
              className="hover:text-claro transition-colors"
            >
              WhatsApp
            </a>
            <a
              href="https://github.com/GabrielBotelhoeng"
              target="_blank"
              rel="noopener noreferrer"
              className="hover:text-claro transition-colors"
            >
              GitHub
            </a>
          </div>
        </div>

        {/* Copyright */}
        <div className="mt-8 pt-6 border-t border-white/20 text-sm opacity-60">
          © 2026 NutriChat
        </div>
      </div>
    </footer>
  )
}
```

**3. Verificar build completo (todos os 8 componentes):**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build
```
  </action>
  <verify>
    <automated>cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing && npm run build 2>&1 | grep -E "(error TS|Error|compiled|Build)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/Plans.tsx` contém `id="planos"` na section
    - `components/Plans.tsx` contém exatamente os 4 planos: `months: 1` e `months: 3` e `months: 6` e `months: 12`
    - `components/Plans.tsx` contém `featured: true` apenas no plano de 3 meses
    - `components/Plans.tsx` contém `"Mais Popular"` no badge com `absolute -top-3`
    - `components/Plans.tsx` contém `border-2 border-floresta` para o card featured e `border border-menta` para os demais
    - `components/Plans.tsx` contém `waLink(plan.months)` no atributo `href`
    - `components/Plans.tsx` contém `from '@/lib/whatsapp'`
    - `components/Footer.tsx` contém `bg-floresta` e `text-offwhite` na tag footer
    - `components/Footer.tsx` contém `"© 2026 NutriChat"`
    - `components/Footer.tsx` contém `"Seu assistente nutricional inteligente no WhatsApp"`
    - `npm run build` termina sem "error TS" ou "Failed to compile"
  </acceptance_criteria>
  <done>Plans com 4 cards (plano 3 meses em destaque com badge e borda floresta), CTAs wa.me com mensagens pré-preenchidas. Footer com bg floresta, tagline, links e copyright. Build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 2: Git init, push GitHub e deploy na Vercel — verificação manual obrigatória</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/06-landing-page/06-RESEARCH.md — "Deploy Vercel: Fluxo Mínimo" (linhas 609-616), "Pitfall 5: NEXT_PUBLIC_ ausente" (linhas 580-583)
  </read_first>
  <what-built>
Claude vai executar os seguintes passos automatizados ANTES da verificação:

**1. Garantir .gitignore correto (adicionar .env.local se necessário):**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing
cat .gitignore | grep ".env.local" || echo ".env.local" >> .gitignore
```

**2. Inicializar repositório Git e fazer commit inicial:**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing
git init
git add .
git commit -m "feat: landing page NutriChat completa — hero 3D + seções + planos"
```

**3. Criar repositório no GitHub (requer gh CLI autenticado):**
```bash
gh repo create nutrichat-landing --public --source=. --remote=origin --push
```
Se o `gh` não estiver autenticado ou o comando falhar, Claude vai informar o erro e solicitar ao usuário que execute manualmente.

**4. Confirmar que o push foi feito:**
```bash
cd C:/Users/botel/OneDrive/Desktop/nutrichat-landing
git log --oneline -3
git remote -v
```

O que o usuário precisa fazer MANUALMENTE (Claude não consegue interagir com a Vercel):
  </what-built>
  <how-to-verify>
**Passos manuais obrigatórios no Vercel (5 minutos):**

1. Acesse https://vercel.com/new
2. Clique em "Add New → Project"
3. Selecione o repositório `nutrichat-landing` do GitHub (conta GabrielBotelhoeng)
4. Na tela de configuração:
   - Framework Preset: Next.js (detectado automaticamente)
   - Build Command: `next build` (padrão — NÃO alterar)
   - Output Directory: `.next` (padrão — NÃO alterar)
   - NÃO adicionar vercel.json — não é necessário
5. Clique em "Environment Variables" e adicione:
   - Name: `NEXT_PUBLIC_WHATSAPP_NUMBER`
   - Value: O número real do WhatsApp do nutricionista (formato: 5562999990000 — sem +, sem espaços, com DDI+DDD)
6. Clique em "Deploy"
7. Aguarde o build (1-3 minutos)

**Verificações após o deploy:**
- [ ] A URL da Vercel abre a landing page (ex: https://nutrichat-landing.vercel.app)
- [ ] Hero aparece com celular 3D animado em desktop (≥ 768px)
- [ ] Em mobile (375px via DevTools), aparece imagem estática em vez do canvas
- [ ] Clicar em "Quero esse plano" (plano 3 meses) abre WhatsApp com mensagem "Olá! Tenho interesse no plano de 3 meses do Nutri Chat..."
- [ ] Número no link wa.me é o número real (não 5500000000000)
- [ ] Badge "Mais Popular" aparece no card de 3 meses
- [ ] Todo push futuro na branch main dispara novo deploy automaticamente (confirmar em Vercel → Deployments)
  </how-to-verify>
  <resume-signal>
Digite "deployed" quando a Vercel mostrar o site funcionando com a URL pública, ou descreva qualquer problema encontrado.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → wa.me | Link externo com parâmetro text encodado; visitante vê o número do nutricionista no HTML |
| GitHub → Vercel | Webhook de deploy — Vercel só executa next build (sem acesso ao banco) |
| Vercel env vars | NEXT_PUBLIC_WHATSAPP_NUMBER — público por design (aparece no HTML compilado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | NEXT_PUBLIC_WHATSAPP_NUMBER no bundle JS | accept | Número é público por design — aparece nos links wa.me visíveis ao usuário; não é segredo |
| T-06-07 | Tampering | wa.me link — parâmetro text | accept | Texto é apenas sugestão; usuário pode editar antes de enviar no WhatsApp; sem consequência de segurança |
| T-06-08 | Denial of Service | Vercel build — npm install em cada deploy | mitigate | package-lock.json commitar para builds determinísticos; não usar `latest` em dependências |
| T-06-09 | Information Disclosure | .env.local no repositório | mitigate | .gitignore inclui .env.local — verificado na Tarefa 1 antes do commit |
</threat_model>

<verification>
Verificação fase completa após deploy:

**Automatizadas (executar antes do checkpoint):**
1. `cd nutrichat-landing && npm run build` — build sem erros
2. `npm run lint` — sem erros de linting
3. `cat components/Plans.tsx | grep "waLink(plan.months)"` — CTA usa waLink com meses
4. `cat .gitignore | grep ".env.local"` — .env.local não vai para o repositório

**Manuais (via checkpoint):**
5. URL Vercel abre a landing — seções Hero, Como funciona, Funcionalidades, Depoimentos, Planos, Rodapé presentes
6. Canvas 3D funciona em desktop; imagem estática aparece em mobile (375px DevTools)
7. CTA "Quero esse plano" (3 meses) abre wa.me com mensagem correta e número real do nutricionista
8. Push de teste na branch main dispara novo deploy na Vercel automaticamente
</verification>

<success_criteria>
- Plans.tsx: 4 cards com preços corretos, plano 3 meses com badge "Mais Popular" e destaque visual, CTAs apontam para wa.me com mensagem pré-preenchida correta
- Footer.tsx: bg-floresta, tagline, copyright 2026, links de contato
- Repositório nutrichat-landing no GitHub (público)
- Vercel deploying automaticamente a partir do branch main
- NEXT_PUBLIC_WHATSAPP_NUMBER configurada no Vercel dashboard com o número real
- URL pública da Vercel funciona e exibe a landing completa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-landing-page/06-03-SUMMARY.md` com:
- URL da Vercel (ex: https://nutrichat-landing.vercel.app)
- URL do repositório GitHub
- Número do WhatsApp configurado (ex: 556299****000 — ocultar os últimos dígitos se preferir)
- Resultado dos testes manuais do checkpoint
- Qualquer problema encontrado no deploy e como foi resolvido
</output>

---
phase: 1
plan: "01-01"
title: "Supabase — tabelas, pgvector e Storage"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260422000001_create_schema.sql
  - supabase/migrations/20260422000002_enable_pgvector.sql
  - supabase/migrations/20260422000003_rls_policies.sql
  - supabase/migrations/20260422000004_storage_bucket.sql
  - supabase/.gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Tabelas pacientes, dietas, refeicoes, registros_diarios e alertas_config existem no Supabase"
    - "Extensao pgvector esta habilitada e a coluna embedding (vector(1536)) existe na tabela dietas"
    - "Bucket 'dietas' existe no Supabase Storage com acesso restrito (authenticated only)"
    - "RLS esta ativo em todas as tabelas — acesso anonimo retorna vazio"
    - "supabase db push roda sem erros e aplica todas as migrations"
  artifacts:
    - path: "supabase/migrations/20260422000001_create_schema.sql"
      provides: "DDL completo das 5 tabelas com constraints e indexes"
      contains: "CREATE TABLE pacientes"
    - path: "supabase/migrations/20260422000002_enable_pgvector.sql"
      provides: "Habilita extensao pgvector e coluna de embedding"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "supabase/migrations/20260422000003_rls_policies.sql"
      provides: "Row Level Security em todas as tabelas"
      contains: "ALTER TABLE pacientes ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260422000004_storage_bucket.sql"
      provides: "Bucket dietas com politicas de acesso restrito"
      contains: "INSERT INTO storage.buckets"
  key_links:
    - from: "tabela dietas"
      to: "Supabase Storage bucket dietas"
      via: "coluna pdf_url (text) armazena caminho do arquivo"
      pattern: "pdf_url.*text"
    - from: "tabela dietas"
      to: "pgvector"
      via: "coluna embedding vector(1536) — preenchida na Fase 2 pelo LangChain"
      pattern: "embedding.*vector"
    - from: "tabela registros_diarios"
      to: "tabela pacientes"
      via: "UNIQUE (paciente_id, data) — um registro por dia por paciente"
      pattern: "UNIQUE.*paciente_id.*data"
---

<objective>
Criar o schema completo do banco de dados NutriChat no Supabase via migrations SQL versionadas, habilitar a extensao pgvector para embeddings RAG, configurar o bucket de Storage para PDFs das dietas e aplicar Row Level Security em todas as tabelas.

Purpose: Estabelecer a fundacao de dados que todas as fases seguintes dependem. O schema implementa as decisoes D-01 a D-04 do CONTEXT.md.
Output: 4 arquivos de migration SQL + schema aplicado via `supabase db push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infraestrutura-ambiente/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migrations SQL — schema das 5 tabelas + pgvector</name>
  <files>
    supabase/migrations/20260422000001_create_schema.sql
    supabase/migrations/20260422000002_enable_pgvector.sql
  </files>
  <read_first>
    - .planning/phases/01-infraestrutura-ambiente/01-CONTEXT.md (decisoes D-01, D-02, D-03, D-04 — estrutura exata das tabelas)
    - .planning/REQUIREMENTS.md (INFRA-01, INFRA-03)
  </read_first>
  <action>
Criar o diretorio supabase/migrations/ e os dois primeiros arquivos de migration.

**Arquivo: supabase/migrations/20260422000001_create_schema.sql**

Implementar DDL completo seguindo D-01, D-02, D-03, D-04:

```sql
-- Migration 001: Schema NutriChat v1

-- Tabela pacientes
CREATE TABLE IF NOT EXISTS pacientes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  nome TEXT NOT NULL,
  whatsapp TEXT NOT NULL UNIQUE,
  plano TEXT NOT NULL CHECK (plano IN ('1mes', '3meses', '6meses', '12meses')),
  data_expiracao DATE NOT NULL,
  ativo BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Tabela dietas (D-03: uma ativa por paciente; historico preservado)
CREATE TABLE IF NOT EXISTS dietas (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE,
  pdf_url TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'ativa' CHECK (status IN ('ativa', 'expirada')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_dietas_paciente_status ON dietas(paciente_id, status);

-- Tabela refeicoes
CREATE TABLE IF NOT EXISTS refeicoes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE,
  descricao TEXT NOT NULL,
  kcal NUMERIC(8,2),
  proteina_g NUMERIC(8,2),
  carbo_g NUMERIC(8,2),
  gordura_g NUMERIC(8,2),
  tipo_registro TEXT CHECK (tipo_registro IN ('texto', 'audio', 'foto', 'codigo_barras', 'rotulo')),
  registrado_em TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_refeicoes_paciente_data ON refeicoes(paciente_id, registrado_em);

-- Tabela registros_diarios (D-02: uma linha por paciente/data, UPDATE acumula)
CREATE TABLE IF NOT EXISTS registros_diarios (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE,
  data DATE NOT NULL,
  kcal_consumido NUMERIC(8,2) NOT NULL DEFAULT 0,
  proteina_g NUMERIC(8,2) NOT NULL DEFAULT 0,
  carbo_g NUMERIC(8,2) NOT NULL DEFAULT 0,
  gordura_g NUMERIC(8,2) NOT NULL DEFAULT 0,
  agua_ml INTEGER NOT NULL DEFAULT 0,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (paciente_id, data)
);
CREATE INDEX IF NOT EXISTS idx_registros_diarios_paciente ON registros_diarios(paciente_id, data DESC);

-- Tabela alertas_config (D-01: uma linha por paciente, colunas por tipo)
CREATE TABLE IF NOT EXISTS alertas_config (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE UNIQUE,
  horario_cafe TEXT,
  horario_almoco TEXT,
  horario_jantar TEXT,
  horarios_agua TEXT[] DEFAULT ARRAY[]::TEXT[],
  horario_suplementos TEXT[] DEFAULT ARRAY[]::TEXT[],
  ativo BOOLEAN NOT NULL DEFAULT true,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Trigger: atualiza updated_at automaticamente
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER pacientes_updated_at BEFORE UPDATE ON pacientes
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

CREATE TRIGGER registros_diarios_updated_at BEFORE UPDATE ON registros_diarios
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

CREATE TRIGGER alertas_config_updated_at BEFORE UPDATE ON alertas_config
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

**Arquivo: supabase/migrations/20260422000002_enable_pgvector.sql**

```sql
-- Migration 002: Habilitar pgvector para embeddings RAG das dietas

-- Habilitar extensao pgvector
CREATE EXTENSION IF NOT EXISTS vector;

-- Adicionar coluna de embedding na tabela dietas
-- vector(1536) = dimensoes do modelo text-embedding-ada-002 / OpenAI (compativel com LangChain + Supabase)
ALTER TABLE dietas ADD COLUMN IF NOT EXISTS embedding vector(1536);

-- Index HNSW para busca de similaridade eficiente
-- Configuracao: m=16, ef_construction=64 (boa performance para < 100k vetores)
CREATE INDEX IF NOT EXISTS idx_dietas_embedding ON dietas
  USING hnsw (embedding vector_cosine_ops)
  WITH (m = 16, ef_construction = 64);

-- Funcao para busca de similaridade (usada pelo LangChain na Fase 2)
CREATE OR REPLACE FUNCTION match_dieta_chunks(
  query_embedding vector(1536),
  paciente_id_param UUID,
  match_threshold FLOAT DEFAULT 0.7,
  match_count INT DEFAULT 5
)
RETURNS TABLE (
  id UUID,
  pdf_url TEXT,
  content TEXT,
  similarity FLOAT
)
LANGUAGE plpgsql
AS $$
BEGIN
  RETURN QUERY
  SELECT
    d.id,
    d.pdf_url,
    '' AS content,
    1 - (d.embedding <=> query_embedding) AS similarity
  FROM dietas d
  WHERE d.paciente_id = paciente_id_param
    AND d.status = 'ativa'
    AND 1 - (d.embedding <=> query_embedding) > match_threshold
  ORDER BY d.embedding <=> query_embedding
  LIMIT match_count;
END;
$$;
```
  </action>
  <verify>
    <automated>
      ls C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/ &&
      grep -l "CREATE TABLE pacientes" C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/*.sql &&
      grep -l "CREATE EXTENSION IF NOT EXISTS vector" C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/*.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260422000001_create_schema.sql existe e contem "CREATE TABLE pacientes", "CREATE TABLE dietas", "CREATE TABLE refeicoes", "CREATE TABLE registros_diarios", "CREATE TABLE alertas_config"
    - 20260422000001_create_schema.sql contem "UNIQUE (paciente_id, data)" na tabela registros_diarios (D-02)
    - 20260422000001_create_schema.sql contem "CHECK (status IN ('ativa', 'expirada'))" na tabela dietas (D-03)
    - 20260422000001_create_schema.sql contem "horarios_agua TEXT[]" e "horario_suplementos TEXT[]" na tabela alertas_config (D-01)
    - supabase/migrations/20260422000002_enable_pgvector.sql existe e contem "CREATE EXTENSION IF NOT EXISTS vector"
    - 20260422000002_enable_pgvector.sql contem "embedding vector(1536)"
    - 20260422000002_enable_pgvector.sql contem "USING hnsw"
    - 20260422000002_enable_pgvector.sql contem a funcao "match_dieta_chunks"
  </acceptance_criteria>
  <done>Dois arquivos de migration criados com DDL completo e consistente com todas as decisoes D-01 a D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migrations — RLS + Storage bucket (arquivos apenas)</name>
  <files>
    supabase/migrations/20260422000003_rls_policies.sql
    supabase/migrations/20260422000004_storage_bucket.sql
    supabase/.gitignore
  </files>
  <read_first>
    - supabase/migrations/20260422000001_create_schema.sql (nomes exatos das tabelas para as politicas RLS)
    - .planning/phases/01-infraestrutura-ambiente/01-CONTEXT.md (decisao sobre acesso restrito no Storage)
    - .planning/REQUIREMENTS.md (INFRA-02: acesso restrito ao Storage)
  </read_first>
  <action>
Criar os dois arquivos de migration restantes e o .gitignore do Supabase. NAO executar `supabase db push` aqui — isso e feito na Task 3 [BLOCKING].

**Arquivo: supabase/migrations/20260422000003_rls_policies.sql**

Habilitar RLS em todas as tabelas e criar politicas. Apenas o service_role (backend) tem acesso total. Papel anon nao acessa nada.

```sql
-- Migration 003: Row Level Security em todas as tabelas NutriChat

-- Habilitar RLS
ALTER TABLE pacientes ENABLE ROW LEVEL SECURITY;
ALTER TABLE dietas ENABLE ROW LEVEL SECURITY;
ALTER TABLE refeicoes ENABLE ROW LEVEL SECURITY;
ALTER TABLE registros_diarios ENABLE ROW LEVEL SECURITY;
ALTER TABLE alertas_config ENABLE ROW LEVEL SECURITY;

-- Politicas para service_role (backend Express usa SUPABASE_SERVICE_KEY)
-- service_role bypassa RLS automaticamente no Supabase — nenhuma policy necessaria para ele

-- Politicas para authenticated (nutricionista logado no painel web — Fase 5)
-- Por enquanto: acesso total para authenticated (refinado na Fase 5 com auth real)
CREATE POLICY "authenticated_full_access_pacientes"
  ON pacientes FOR ALL TO authenticated
  USING (true) WITH CHECK (true);

CREATE POLICY "authenticated_full_access_dietas"
  ON dietas FOR ALL TO authenticated
  USING (true) WITH CHECK (true);

CREATE POLICY "authenticated_full_access_refeicoes"
  ON refeicoes FOR ALL TO authenticated
  USING (true) WITH CHECK (true);

CREATE POLICY "authenticated_full_access_registros_diarios"
  ON registros_diarios FOR ALL TO authenticated
  USING (true) WITH CHECK (true);

CREATE POLICY "authenticated_full_access_alertas_config"
  ON alertas_config FOR ALL TO authenticated
  USING (true) WITH CHECK (true);

-- anon nao tem nenhuma policy — acesso negado por padrao com RLS ativo
```

**Arquivo: supabase/migrations/20260422000004_storage_bucket.sql**

```sql
-- Migration 004: Bucket 'dietas' no Supabase Storage com acesso restrito

-- Criar bucket para PDFs das dietas (acesso restrito — nao publico)
INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
VALUES (
  'dietas',
  'dietas',
  false,
  52428800, -- 50 MB por arquivo
  ARRAY['application/pdf']
)
ON CONFLICT (id) DO NOTHING;

-- Politica: apenas authenticated pode fazer upload (nutricionista no painel)
CREATE POLICY "authenticated_upload_dietas"
  ON storage.objects FOR INSERT TO authenticated
  WITH CHECK (bucket_id = 'dietas');

-- Politica: authenticated pode ler (painel + backend via token)
CREATE POLICY "authenticated_read_dietas"
  ON storage.objects FOR SELECT TO authenticated
  USING (bucket_id = 'dietas');

-- Politica: authenticated pode deletar (ao substituir dieta)
CREATE POLICY "authenticated_delete_dietas"
  ON storage.objects FOR DELETE TO authenticated
  USING (bucket_id = 'dietas');

-- anon nao tem acesso ao bucket
```

**Arquivo: supabase/.gitignore**

```
.branches
.temp
```
  </action>
  <verify>
    <automated>
      grep -l "ALTER TABLE pacientes ENABLE ROW LEVEL SECURITY" C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/*.sql &&
      grep -l "INSERT INTO storage.buckets" C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/*.sql &&
      ls C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/ | wc -l
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260422000003_rls_policies.sql existe e contem "ENABLE ROW LEVEL SECURITY" para as 5 tabelas (pacientes, dietas, refeicoes, registros_diarios, alertas_config)
    - 20260422000003_rls_policies.sql contem 5 policies "authenticated_full_access_*" — uma por tabela
    - supabase/migrations/20260422000004_storage_bucket.sql existe e contem "INSERT INTO storage.buckets" com id = 'dietas' e public = false
    - 20260422000004_storage_bucket.sql contem "allowed_mime_types" com 'application/pdf'
    - Total de 4 arquivos em supabase/migrations/
    - supabase/.gitignore existe
  </acceptance_criteria>
  <done>Arquivos de migration RLS e Storage criados. Prontos para aplicar via supabase db push na Task 3.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Aplicar todas as migrations via supabase db push</name>
  <files></files>
  <action>
Aplicar todas as 4 migrations no Supabase remoto. Esta task depende da conclusao das Tasks 1 e 2 — todos os 4 arquivos de migration devem existir antes de executar.

```bash
cd C:/Users/botel/OneDrive/Desktop/nutri_fit
supabase db push
```

Se o comando pedir confirmacao interativa, aceitar (y).

Se supabase CLI nao estiver instalado:
```bash
npm install -g supabase
```

Se houver erro de conexao (projeto Supabase nao configurado localmente), verificar se supabase/config.toml existe com o project_id correto:
```bash
supabase link --project-ref SEU-PROJECT-REF
supabase db push
```

Verificar que o push completou sem a palavra "ERROR" na saida.
  </action>
  <verify>
    <automated>
      supabase db push 2>&1 | tee /tmp/db_push_out.txt ; grep -v "ERROR" /tmp/db_push_out.txt && echo "PUSH OK" || echo "PUSH FAILED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` executa com codigo de saida 0 (sem erros fatais)
    - Saida do comando nao contem a palavra "ERROR"
    - Tabelas existem no Supabase Dashboard: pacientes, dietas, refeicoes, registros_diarios, alertas_config
    - Extension pgvector confirmada: `SELECT extname FROM pg_extension WHERE extname = 'vector';` retorna uma linha
    - Bucket 'dietas' visivel no Supabase Dashboard > Storage
  </acceptance_criteria>
  <done>Todas as 4 migrations aplicadas com sucesso no Supabase remoto. Schema completo disponivel para os planos seguintes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → Supabase | Qualquer requisicao sem autenticacao chega com papel `anon` |
| service_role → Supabase | Backend Express usa SUPABASE_SERVICE_KEY — contorna RLS |
| Storage público vs privado | PDFs das dietas contem dados de saude sensiveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Todas as tabelas | mitigate | RLS habilitado em todas as 5 tabelas — papel `anon` nao tem nenhuma policy, logo acesso e negado por padrao. Verificar via `SELECT * FROM pacientes` com anon key apos aplicar migrations. |
| T-01-02 | Information Disclosure | Supabase Storage bucket dietas | mitigate | Bucket criado com `public = false`. Politicas permitem apenas `authenticated`. URLs do Storage nao sao acessiveis sem token JWT valido. |
| T-01-03 | Elevation of Privilege | SUPABASE_SERVICE_KEY | mitigate | SERVICE_KEY nunca commitada no repositorio. Arquivo `.env` no `.gitignore`. Repositorio inclui apenas `.env.example` com chaves sem valores (ver Plan 01-02). |
| T-01-04 | Tampering | Coluna embedding vector | accept | Embeddings sao dados derivados (nao PII direta). Corrompidos = RAG ruim, nao violacao de dados. Risco baixo; aceito para v1. |
</threat_model>

<verification>
Apos execucao do plano, verificar manualmente no Supabase Dashboard:
1. Table Editor: confirmar que 5 tabelas existem com colunas corretas
2. Authentication > Policies: confirmar que RLS esta ativo em cada tabela
3. Storage: confirmar que bucket 'dietas' existe e esta marcado como privado
4. SQL Editor: executar `SELECT extname FROM pg_extension WHERE extname = 'vector';` — deve retornar uma linha
5. Tentar SELECT com anon key via curl — deve retornar erro 403 ou array vazio
</verification>

<success_criteria>
- 4 arquivos de migration SQL existem em supabase/migrations/
- `supabase db push` executa sem erros
- 5 tabelas criadas: pacientes, dietas, refeicoes, registros_diarios, alertas_config
- pgvector habilitado com coluna embedding vector(1536) e index HNSW na tabela dietas
- Bucket 'dietas' existe como privado (public=false) aceitando apenas PDFs
- RLS ativo em todas as 5 tabelas sem politicas para anon
</success_criteria>

<output>
Apos completar, criar `.planning/phases/01-infraestrutura-ambiente/01-01-SUMMARY.md` com:
- Tabelas criadas e suas colunas principais
- Decisoes implementadas (D-01 a D-04) com referencia ao arquivo
- Comando para verificar: `supabase db push` e queries de validacao
- Qualquer desvio das especificacoes originais com justificativa
</output>

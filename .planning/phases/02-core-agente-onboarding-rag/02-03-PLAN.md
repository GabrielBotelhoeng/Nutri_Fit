---
phase: 2
plan: "02-03"
title: "Boas-vindas automatica e entrevista inicial"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/src/services/agent.ts
  - backend/src/services/conversation.ts
  - backend/src/routes/webhook.ts
  - backend/src/routes/boas-vindas.ts
  - backend/src/routes/api.ts
autonomous: true
requirements:
  - AGENT-08
  - AGENT-09

must_haves:
  truths:
    - "agentService.processarMensagem() processa texto do paciente com Claude e gerencia estado da conversa"
    - "Primeiro contato com paciente novo dispara mensagem de boas-vindas personalizada com o nome do cadastro"
    - "Agente conduz entrevista sequencial coletando: idade, sexo, peso, altura, atividade_fisica, suplementos"
    - "Estado da entrevista e persistido na tabela pacientes (colunas JSONB entrevista_dados e entrevista_status)"
    - "Apos entrevista completa, agente confirma dados e salva no banco"
    - "POST /api/boas-vindas/:pacienteId pode ser chamado externamente para disparar boas-vindas manualmente"
  artifacts:
    - path: "backend/src/services/conversation.ts"
      provides: "Gerenciamento de estado da conversa por paciente (Redis ou Supabase)"
      contains: "getEstado"
    - path: "backend/src/services/agent.ts"
      provides: "Logica principal do agente Claude: entrevista, RAG, resposta"
      contains: "processarMensagem"
    - path: "backend/src/routes/webhook.ts"
      provides: "handleText atualizado para chamar agentService em vez do stub"
      contains: "agentService.processarMensagem"
  key_links:
    - from: "backend/src/routes/webhook.ts handleText()"
      to: "backend/src/services/agent.ts processarMensagem()"
      via: "substituicao do stub por chamada real ao agente"
      pattern: "agentService.processarMensagem"
    - from: "backend/src/services/agent.ts"
      to: "backend/src/services/rag.ts query()"
      via: "consulta ao contexto da dieta antes de responder"
      pattern: "ragService.query"
---

<objective>
Implementar o nucleo conversacional do agente NutriChat usando Claude API. O agente envia boas-vindas personalizadas ao primeiro contato, conduz entrevista inicial para coletar dados do paciente e persiste o estado da conversa entre mensagens.

Purpose: Habilitar o fluxo principal de onboarding — o paciente recebe o agente assim que e cadastrado e passa pela entrevista para personalizar sua experiencia.
Output: agentService com Claude, conversationService de estado, rota de boas-vindas, handleText real no webhook.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — adicionar colunas de entrevista na tabela pacientes</name>
  <files>
    supabase/migrations/20260423000006_paciente_entrevista.sql
  </files>
  <read_first>
    - supabase/migrations/20260422000001_create_schema.sql (estrutura atual de pacientes)
  </read_first>
  <action>
Adicionar colunas para armazenar dados da entrevista e status diretamente na tabela pacientes.

**Arquivo: supabase/migrations/20260423000006_paciente_entrevista.sql**

```sql
-- Migration 006: Colunas de entrevista na tabela pacientes

ALTER TABLE pacientes
  ADD COLUMN IF NOT EXISTS entrevista_status TEXT NOT NULL DEFAULT 'pendente'
    CHECK (entrevista_status IN ('pendente', 'em_andamento', 'completa')),
  ADD COLUMN IF NOT EXISTS entrevista_dados JSONB DEFAULT '{}'::jsonb,
  ADD COLUMN IF NOT EXISTS entrevista_etapa INTEGER NOT NULL DEFAULT 0;

-- entrevista_dados armazena: idade, sexo, peso_kg, altura_cm, atividade_tipo,
--   atividade_frequencia, atividade_horario, suplementos (array)
-- entrevista_etapa: 0=nao iniciada, 1=idade, 2=sexo, 3=peso, 4=altura, 5=atividade, 6=suplementos, 7=completa
```

Executar:
```bash
cd /c/Users/botel/OneDrive/Desktop/nutri_fit
supabase db push
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260423000006_paciente_entrevista.sql &&
      grep -q "entrevista_status" /c/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260423000006_paciente_entrevista.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - Migration 006 criada com ADD COLUMN entrevista_status, entrevista_dados, entrevista_etapa
    - supabase db push sem erros
  </acceptance_criteria>
  <done>Colunas de entrevista adicionadas a tabela pacientes.</done>
</task>

<task type="auto">
  <name>Task 2: Criar conversationService — estado por paciente no Supabase</name>
  <files>
    backend/src/services/conversation.ts
  </files>
  <read_first>
    - backend/src/config/env.ts (SUPABASE_URL, SUPABASE_SERVICE_KEY)
    - supabase/migrations/20260423000006_paciente_entrevista.sql (colunas entrevista_*)
  </read_first>
  <action>
Criar servico que le e atualiza o estado da entrevista de um paciente no Supabase. Usando Supabase como storage de estado (nao Redis) para simplicidade no v1.

**Arquivo: backend/src/services/conversation.ts**

```typescript
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';

const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);

export interface EstadoEntrevista {
  status: 'pendente' | 'em_andamento' | 'completa';
  etapa: number;
  dados: {
    idade?: number;
    sexo?: 'masculino' | 'feminino';
    peso_kg?: number;
    altura_cm?: number;
    atividade_tipo?: string;
    atividade_frequencia?: string;
    atividade_horario?: string;
    suplementos?: string[];
  };
}

export interface PacienteInfo {
  id: string;
  nome: string;
  whatsapp: string;
  ativo: boolean;
  data_expiracao: string;
  entrevista_status: string;
  entrevista_etapa: number;
  entrevista_dados: Record<string, unknown>;
}

export async function buscarPacientePorWhatsapp(
  whatsapp: string,
): Promise<PacienteInfo | null> {
  const { data, error } = await supabase
    .from('pacientes')
    .select('id, nome, whatsapp, ativo, data_expiracao, entrevista_status, entrevista_etapa, entrevista_dados')
    .eq('whatsapp', whatsapp)
    .eq('ativo', true)
    .single();

  if (error || !data) return null;
  return data as PacienteInfo;
}

export async function getEstado(pacienteId: string): Promise<EstadoEntrevista> {
  const { data, error } = await supabase
    .from('pacientes')
    .select('entrevista_status, entrevista_etapa, entrevista_dados')
    .eq('id', pacienteId)
    .single();

  if (error || !data) {
    return { status: 'pendente', etapa: 0, dados: {} };
  }

  return {
    status: data.entrevista_status as EstadoEntrevista['status'],
    etapa: data.entrevista_etapa,
    dados: data.entrevista_dados as EstadoEntrevista['dados'],
  };
}

export async function atualizarEstado(
  pacienteId: string,
  update: Partial<{
    status: EstadoEntrevista['status'];
    etapa: number;
    dados: Partial<EstadoEntrevista['dados']>;
  }>,
): Promise<void> {
  const campos: Record<string, unknown> = {};
  if (update.status !== undefined) campos['entrevista_status'] = update.status;
  if (update.etapa !== undefined) campos['entrevista_etapa'] = update.etapa;
  if (update.dados !== undefined) {
    // Merge com dados existentes via Supabase JSONB concat
    const { data: current } = await supabase
      .from('pacientes')
      .select('entrevista_dados')
      .eq('id', pacienteId)
      .single();
    campos['entrevista_dados'] = { ...(current?.entrevista_dados ?? {}), ...update.dados };
  }

  await supabase.from('pacientes').update(campos).eq('id', pacienteId);
}
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/conversation.ts &&
      grep -q "buscarPacientePorWhatsapp" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/conversation.ts &&
      grep -q "getEstado" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/conversation.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/conversation.ts existe e exporta buscarPacientePorWhatsapp, getEstado, atualizarEstado
    - buscarPacientePorWhatsapp busca por whatsapp E ativo=true
    - atualizarEstado faz merge de dados JSONB (nao sobrescreve)
    - npm run typecheck sem erros em conversation.ts
  </acceptance_criteria>
  <done>conversationService implementado com persistencia de estado no Supabase.</done>
</task>

<task type="auto">
  <name>Task 3: Instalar SDK Anthropic e criar agentService com Claude</name>
  <files>
    backend/src/services/agent.ts
    backend/package.json
  </files>
  <read_first>
    - backend/src/services/conversation.ts (importar buscarPacientePorWhatsapp, getEstado, atualizarEstado)
    - backend/src/services/evolution.ts (importar sendText)
    - backend/src/services/rag.ts (importar query)
    - backend/src/config/env.ts (CLAUDE_API_KEY ja configurado)
    - .planning/REQUIREMENTS.md (AGENT-08, AGENT-09 — boas-vindas e entrevista)
  </read_first>
  <action>
Instalar o SDK da Anthropic e criar o servico principal do agente.

```bash
cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend
npm install @anthropic-ai/sdk@^0.36.0
```

**Arquivo: backend/src/services/agent.ts**

```typescript
import Anthropic from '@anthropic-ai/sdk';
import { env } from '../config/env';
import { sendText } from './evolution';
import { query as ragQuery } from './rag';
import {
  buscarPacientePorWhatsapp,
  getEstado,
  atualizarEstado,
  EstadoEntrevista,
} from './conversation';

const claude = new Anthropic({ apiKey: env.CLAUDE_API_KEY });

const PERGUNTAS_ENTREVISTA: Record<number, string> = {
  1: '📋 Vamos iniciar sua avaliacao! *Qual e a sua idade?*',
  2: '👤 *Qual e o seu sexo?* (masculino/feminino)',
  3: '⚖️ *Qual e o seu peso atual em kg?* (ex: 75)',
  4: '📏 *Qual e a sua altura em cm?* (ex: 175)',
  5: '🏃 *Que tipo de atividade fisica voce pratica?* (musculacao, corrida, caminhada, sedentario...)',
  6: '📅 *Com que frequencia e em que horario voce pratica?* (ex: 3x por semana, as 18h)',
  7: '💊 *Voce usa algum suplemento?* Se sim, quais. Se nao, responda "nao uso".',
};

async function processarRespostaEntrevista(
  pacienteId: string,
  etapa: number,
  texto: string,
  dados: EstadoEntrevista['dados'],
): Promise<Partial<EstadoEntrevista['dados']>> {
  const novoDado: Partial<EstadoEntrevista['dados']> = {};
  const textoLower = texto.toLowerCase().trim();

  switch (etapa) {
    case 1: {
      const idade = parseInt(textoLower.replace(/\D/g, ''), 10);
      if (isNaN(idade) || idade < 10 || idade > 120) return {};
      novoDado.idade = idade;
      break;
    }
    case 2: {
      if (textoLower.includes('masc') || textoLower === 'm') novoDado.sexo = 'masculino';
      else if (textoLower.includes('fem') || textoLower === 'f') novoDado.sexo = 'feminino';
      else return {};
      break;
    }
    case 3: {
      const peso = parseFloat(textoLower.replace(',', '.').replace(/[^\d.]/g, ''));
      if (isNaN(peso) || peso < 30 || peso > 300) return {};
      novoDado.peso_kg = peso;
      break;
    }
    case 4: {
      const altura = parseInt(textoLower.replace(/\D/g, ''), 10);
      if (isNaN(altura) || altura < 100 || altura > 250) return {};
      novoDado.altura_cm = altura;
      break;
    }
    case 5: {
      novoDado.atividade_tipo = texto.trim();
      break;
    }
    case 6: {
      novoDado.atividade_frequencia = texto.trim();
      break;
    }
    case 7: {
      if (textoLower === 'nao uso' || textoLower === 'nao' || textoLower === 'nenhum') {
        novoDado.suplementos = [];
      } else {
        novoDado.suplementos = texto.split(/[,;]/).map((s) => s.trim()).filter(Boolean);
      }
      break;
    }
  }

  return novoDado;
}

async function responderComClaude(
  perguntaUsuario: string,
  contextoRag: string,
  nomePaciente: string,
): Promise<string> {
  const systemPrompt = `Voce e o NutriChat, assistente nutricional do WhatsApp. 
Seu nutricionista prescreveu uma dieta especifica para voce, ${nomePaciente}.

Contexto da sua dieta:
${contextoRag || 'Dieta ainda nao processada. Responda com base em principios gerais de nutricao.'}

Regras:
- Responda sempre em portugues, de forma amigavel e direta
- Mantenha respostas curtas (max 3 paragrafos) — e WhatsApp, nao email
- Nunca invente informacoes nutricionais especificas sem base no contexto da dieta
- Use emojis com moderacao`;

  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 512,
    system: systemPrompt,
    messages: [{ role: 'user', content: perguntaUsuario }],
  });

  return response.content[0].type === 'text' ? response.content[0].text : '';
}

export async function processarMensagem(phone: string, texto: string): Promise<void> {
  // 1. Buscar paciente pelo numero
  const paciente = await buscarPacientePorWhatsapp(phone);

  if (!paciente) {
    await sendText(phone, '😕 Seu numero nao esta cadastrado. Fale com seu nutricionista.');
    return;
  }

  if (!paciente.ativo) {
    await sendText(phone, '⏰ Seu plano expirou. Entre em contato com seu nutricionista para renovar.');
    return;
  }

  // 2. Verificar estado da entrevista
  const estado = await getEstado(paciente.id);

  // 3. Entrevista pendente — iniciar
  if (estado.status === 'pendente') {
    await atualizarEstado(paciente.id, { status: 'em_andamento', etapa: 1 });
    await sendText(
      phone,
      `Ola, ${paciente.nome}! 👋 Sou o *NutriChat*, seu assistente nutricional.\n\n` +
      `Vou te ajudar a acompanhar sua dieta diariamente pelo WhatsApp. 🥗\n\n` +
      PERGUNTAS_ENTREVISTA[1],
    );
    return;
  }

  // 4. Entrevista em andamento — coletar dados
  if (estado.status === 'em_andamento') {
    const etapa = estado.etapa;
    const novoDado = await processarRespostaEntrevista(paciente.id, etapa, texto, estado.dados);

    if (Object.keys(novoDado).length === 0) {
      // Resposta invalida — repetir pergunta
      await sendText(phone, `❓ Nao entendi. ${PERGUNTAS_ENTREVISTA[etapa]}`);
      return;
    }

    const proximaEtapa = etapa + 1;

    if (proximaEtapa > 7) {
      // Entrevista completa
      await atualizarEstado(paciente.id, {
        status: 'completa',
        etapa: 7,
        dados: novoDado,
      });
      await sendText(
        phone,
        `✅ Perfeito! Seus dados foram registrados.\n\n` +
        `A partir de agora voce pode:\n` +
        `• Registrar suas refeicoes (texto, foto ou audio)\n` +
        `• Perguntar sobre sua dieta\n` +
        `• Ver seu saldo diario de calorias\n\n` +
        `Digite qualquer coisa para comecar! 💪`,
      );
    } else {
      await atualizarEstado(paciente.id, { etapa: proximaEtapa, dados: novoDado });
      await sendText(phone, PERGUNTAS_ENTREVISTA[proximaEtapa]);
    }
    return;
  }

  // 5. Entrevista completa — modo agente normal
  const contextoRag = await ragQuery(paciente.id, texto);
  const resposta = await responderComClaude(texto, contextoRag, paciente.nome);
  await sendText(phone, resposta);
}

export async function enviarBoasVindas(pacienteId: string): Promise<void> {
  const { data, error } = await (
    await import('@supabase/supabase-js')
  ).createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY)
    .from('pacientes')
    .select('nome, whatsapp')
    .eq('id', pacienteId)
    .single();

  if (error || !data) {
    console.error(`[agent] Paciente ${pacienteId} nao encontrado para boas-vindas`);
    return;
  }

  await atualizarEstado(pacienteId, { status: 'em_andamento', etapa: 1 });
  await sendText(
    data.whatsapp,
    `Ola, ${data.nome}! 👋 Sou o *NutriChat*, seu assistente nutricional pelo WhatsApp.\n\n` +
    `Fui cadastrado pelo seu nutricionista para te ajudar com sua dieta. 🥗\n\n` +
    PERGUNTAS_ENTREVISTA[1],
  );
}
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts &&
      grep -q "processarMensagem" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts &&
      grep -q "enviarBoasVindas" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts &&
      cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/agent.ts existe e exporta processarMensagem() e enviarBoasVindas()
    - processarMensagem roteia por estado: pendente→boas-vindas, em_andamento→entrevista, completa→agente
    - Entrevista coleta 7 dados: idade, sexo, peso, altura, atividade_tipo, frequencia, suplementos
    - Resposta invalida na entrevista repete a pergunta sem avancar etapa
    - Modo agente normal usa ragQuery + claude.messages.create
    - @anthropic-ai/sdk instalado em package.json
    - npm run typecheck sem erros
  </acceptance_criteria>
  <done>agentService implementado com Claude, entrevista sequencial e resposta RAG.</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar handleText no webhook e criar rota de boas-vindas</name>
  <files>
    backend/src/routes/webhook.ts
    backend/src/routes/boas-vindas.ts
    backend/src/routes/api.ts
  </files>
  <read_first>
    - backend/src/routes/webhook.ts (substituir stub handleText)
    - backend/src/services/agent.ts (importar processarMensagem e enviarBoasVindas)
    - backend/src/routes/api.ts (adicionar boasVindasRouter)
  </read_first>
  <action>
Substituir o stub handleText no webhook pela chamada real ao agentService.

**Modificar: backend/src/routes/webhook.ts**

Remover a funcao `handleText` stub e importar agentService:

```typescript
import { Router, Request, Response } from 'express';
import { sendText } from '../services/evolution';
import * as agentService from '../services/agent';

export const webhookRouter = Router();

// ... (manter interface EvolutionPayload e extractPhoneNumber)

webhookRouter.post('/', async (req: Request, res: Response) => {
  res.status(200).json({ status: 'received' });

  const payload = req.body as EvolutionPayload;
  if (payload?.data?.key?.fromMe) return;
  if (payload?.event !== 'MESSAGES_UPSERT') return;

  const { data } = payload;
  const phone = extractPhoneNumber(data.key.remoteJid);
  const messageType = data.messageType;

  console.log(`[webhook] Mensagem de ${phone} | tipo: ${messageType}`);

  try {
    switch (messageType) {
      case 'conversation':
      case 'extendedTextMessage': {
        const text =
          (data.message.conversation as string) ||
          (data.message.extendedTextMessage as { text: string })?.text || '';
        await agentService.processarMensagem(phone, text);
        break;
      }
      case 'audioMessage': {
        await sendText(phone, '🎤 Recebi seu audio! Transcricao disponivel em breve...');
        break;
      }
      case 'imageMessage': {
        await sendText(phone, '📸 Recebi sua imagem! Analise disponivel em breve...');
        break;
      }
      default: {
        console.log(`[webhook] Tipo nao suportado: ${messageType}`);
      }
    }
  } catch (err) {
    console.error('[webhook] Erro ao processar mensagem:', err);
  }
});
```

**Criar: backend/src/routes/boas-vindas.ts**

```typescript
import { Router, Request, Response } from 'express';
import { enviarBoasVindas } from '../services/agent';

export const boasVindasRouter = Router();

boasVindasRouter.post('/:pacienteId', async (req: Request, res: Response) => {
  const { pacienteId } = req.params;

  res.status(202).json({ status: 'enviando', paciente_id: pacienteId });

  enviarBoasVindas(pacienteId).catch((err) => {
    console.error(`[boas-vindas] Falha para paciente ${pacienteId}:`, err);
  });
});
```

**Modificar: backend/src/routes/api.ts**

```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
apiRouter.use('/boas-vindas', boasVindasRouter);
```
  </action>
  <verify>
    <automated>
      grep -q "agentService" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/webhook.ts &&
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/boas-vindas.ts &&
      grep -q "boasVindasRouter" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts &&
      cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck 2>&1 | grep -c "error" || true
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/webhook.ts importa agentService e chama processarMensagem (nao mais stub)
    - backend/src/routes/boas-vindas.ts exporta boasVindasRouter com POST /:pacienteId
    - backend/src/routes/api.ts registra /boas-vindas
    - npm run typecheck sem erros
  </acceptance_criteria>
  <done>Webhook conectado ao agente real. Rota de boas-vindas pronta para ser chamada pelo painel (Fase 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Anthropic API | Mensagens do paciente sao enviadas ao Claude — dados de saude em terceiros |
| POST /api/boas-vindas | Sem autenticacao em v1 — proteger na Fase 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Claude API | accept | Contexto da dieta e mensagens do paciente enviados a Anthropic. Aceitavel para v1/portfolio. |
| T-02-03-02 | Spoofing | POST /api/boas-vindas | accept (v1) | Qualquer um na rede Docker pode disparar boas-vindas. Autenticacao adicionada na Fase 5 com JWT. |
| T-02-03-03 | Tampering | Estado entrevista | mitigate | atualizarEstado usa service_role — cliente nao acessa diretamente. |
</threat_model>

<verification>
Apos execucao:
1. npm run typecheck sem erros
2. Enviar mensagem para o WhatsApp conectado — agente deve responder com boas-vindas e primeira pergunta da entrevista
3. Responder as 7 perguntas — agente deve confirmar conclusao da entrevista
4. Verificar no Supabase Dashboard: paciente com entrevista_status='completa' e entrevista_dados preenchido
5. POST /api/boas-vindas/:pacienteId retorna 202
</verification>

<success_criteria>
- Migration 006 (entrevista_*) criada e aplicada
- conversationService com buscarPacientePorWhatsapp, getEstado, atualizarEstado
- agentService com processarMensagem (fluxo completo) e enviarBoasVindas
- webhook.ts atualizado: handleText chama agentService.processarMensagem
- Rota POST /api/boas-vindas/:pacienteId retorna 202
- @anthropic-ai/sdk instalado
- npm run typecheck sem erros
</success_criteria>

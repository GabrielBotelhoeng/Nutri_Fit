---
context: default
phase: bot-refinements-batch-1
status: fatia_3_entregue_migration_aplicada_relatorio_parrudo_proximo
last_updated: 2026-06-22T03:30:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **tsx watch no Docker/Windows NAO recarrega apos edit** — sempre `docker restart nutrichat_backend` apos editar `.ts`. Sem isso, smoke tests mentem.
- [ ] **Evolution API v2 usa evento lowercase `messages.upsert`** (nao `MESSAGES_UPSERT`). Nao regredir em `backend/src/routes/webhook.ts:32`.
- [ ] **Agente nunca pode fabricar resposta de dieta sem chunks RAG** — fallback em `agent.ts:responderComClaude` — nao relaxar.
- [ ] **Restricoes/aversoes coletadas na entrevista devem entrar no system prompt do Claude** — bloco "REGRAS DE SEGURANCA" inegociaveis.
- [ ] **`obterMetas` em `calculos.ts` e ponto unico de verdade para metas diarias.** Nao reintroduzir 30/40/30 hardcoded.
- [ ] **Prompts de LLM que vao pro WhatsApp PRECISAM ter bloco FORMATACAO explicito** — sem ele, Haiku/Claude geram markdown padrao que o WhatsApp renderiza literal.
- [ ] **N8N workflows usam header `x-internal-key`** (nao `x-internal-token`).
- [ ] **Regex pt-BR com cedilha**: `(almo[çc]?o)`, `caf[eé]`, `(manh[ãa])`. NAO usar `almoc?o` ou `cafe` puro.
- [ ] **`registrarRefeicao` tem 5 callsites** (meal.ts texto + vision.ts barcode/rotulo-via-barcode/rotulo-direto + agent.ts foto). Side-effects pos-registro (saldo+overshoot) devem ser disparados em TODOS os 5 callsites — `dispararAlertaOvershoot` ja esta neles. Nao adicionar 6o callsite sem atualizar.
- [ ] **Detector overshoot usa strict `>` (110% exato nao alerta).** Tunning vive em `OVERSHOOT_THRESHOLD` em `meal.ts`.
- [ ] **Mensagem WhatsApp ideal <= 1500 chars.** Acima disso a UX degrada (paciente nao le).

<current_state>
**#4 (lembretes) 100% entregue.** Fatias 1+2+3 commitadas (`8a664bb` + `a182dc2` + `e433917`). Migration de lanches APLICADA no Supabase Cloud nesta sessao via `npx supabase db push --linked` (CLI funcionou hoje — IPv6 timeout que bloqueava antes foi resolvido). `supabase migration repair --status applied` foi necessario antes do push porque o historico tinha 9 remotas sem reflexo no estado local.

Bloqueador humano da Fatia 2 (rodar migration manual) NAO existe mais. Toda a infra de lembretes esta pronta:
- 5 refeicoes + agua personalizada + suplementos via cron */15min
- Overshoot inline em meal.ts apos cada registro (>110% meta_kcal)
- Sync entrevista -> alertas_config em try/catch isolado em agent.ts

Proximo trabalho (sessao nova): **#5 Relatorio semanal parrudo** — usuario quer que o relatorio do final da semana "deixe o usuario com vontade de continuar". Alinhamos em 4 features: streak + comparacao semanal + best day callout + barras ASCII.

Pausa preventiva em 80% — proximo trabalho merece contexto fresco.
</current_state>

## O que foi entregue nesta sessao

### `e433917` feat(bot): #4 Fatia 3 — detector overshoot inline em meal.ts (>110% kcal)

**Decisao arquitetural:** trigger inline em meal.ts apos cada `registrarRefeicao`, NAO criar cron novo. Cada registro ja sabe o saldo do dia — checar overshoot na hora e gratis. Helper centralizado.

- `backend/src/services/meal.ts`:
  - `OVERSHOOT_THRESHOLD = 1.10` (tunning centralizado)
  - `excedeuMetaKcal(saldo, metas)`: funcao pura, defensiva (meta <= 0 retorna false)
  - `dispararAlertaOvershoot(phone, saldo, metas)`: try/catch interno; so dispara se exceder; mensagem amigavel com pct real calculado + saldo/meta absolutos + sugestao concreta (pausar proxima refeicao ou trocar por algo leve)
  - Chamado em `processarTextoRefeicao` apos sendText do saldo
- `backend/src/services/vision.ts`: import + 3 callsites (barcode, rotulo via barcode, rotulo direto)
- `backend/src/services/agent.ts`: 1 callsite (foto via confirmacao 'sim')
- `backend/scripts/smoke-overshoot.ts`: 7 casos cobrindo <100%, =100%, =110% (limite — nao alerta), >110%, muito acima, meta=0, meta negativa. **7/7 OK**.

### Migration de lanches aplicada via CLI

- `supabase migration repair --linked --status applied <9 migrations originais>` para alinhar historico
- `supabase db push --linked` aplicou 2 pendentes:
  - `20260622000001_alertas_config_lanches.sql` (`horario_lanche_manha`, `horario_lanche_tarde`)
  - `20260621000001_dietas_processamento_status.sql` (NOTICE: column already exists, skipping — idempotencia funcionando)

## Pendencias proxima sessao (ordem)

### 1. #5 Relatorio semanal PARRUDO (M) — proxima fatia de codigo

Estado atual em `backend/src/services/relatorio.ts`: cron domingo 08h, mensagem com dias/total/media/macros/hidratacao + 1-2 frases motivacionais do Haiku. Funcional mas chapado.

**Adicionar 4 features:**

a) **Streak de dias consecutivos com registro** — gatilho de habito
   - Query: `registros_diarios` ordenado por data DESC para o paciente
   - Contar dias consecutivos a partir de hoje (ou ontem se hoje ainda sem registro)
   - Mensagem: `🔥 Streak: N dias consecutivos` (ou `Comece um streak essa semana!` se 0)

b) **Comparacao vs semana anterior**
   - Query adicional: registros entre `data_inicio - 7` e `data_inicio` (semana anterior)
   - Comparar: dias com registro, kcal medio, hidratacao media
   - Setas ↑↓→ + delta absoluto. Ex: `↑ 2 dias mais que semana passada (3 → 5)`

c) **"Best day" callout**
   - Da semana atual, escolher dia com `kcal_consumido` mais proximo da meta (sem ultrapassar muito)
   - Destacar: `🏆 Seu melhor dia: quarta — 98% da meta (1960/2000 kcal)`

d) **Barras ASCII de adesao** — visual sem markdown rico
   - Helper: `barraAdesao(atual, total): string` retornando ex: `▓▓▓▓▓░░ 5/7`
   - Aplicar em: dias com registro, dias que bateram meta, dias com hidratacao OK

**Reformular prompt do Haiku** pra incluir vitorias contextualmente (streak, best day, delta positivo) e sugerir 1 micro-meta proxima semana ("essa semana tente fechar o streak de 7 dias!").

**Smoke programatico** em `scripts/smoke-relatorio-parrudo.ts` cobrindo:
- streak=0, streak=3, streak=7
- Sem semana anterior (paciente novo)
- Melhor que semana anterior / pior que / igual
- Best day = 100% meta / best day muito abaixo
- Mensagem total caber em <= 1500 chars

### 2. (Humano) Validacao E2E lembretes Fatia 1+2+3

- Terminar a entrevista de 14 etapas no WhatsApp
- Conferir linha em `alertas_config` populada com os 5 horarios (agora as 2 colunas de lanche existem)
- Aguardar cron `*/15min` e ver lembretes chegando nos horarios cadastrados
- Registrar refeicao alta em kcal (>110% da meta) e ver alerta proativo de overshoot

### 3. (Humano) Carry-overs

- Validar upload PDF pelo painel → `rag_status: indexado` + bot responde duvida
- Testar barcode com EAN real de Cristal

### 4. Fase 6 Plan 02 + 03 — VERIFICAR ANTES

Usuario informou nesta sessao: "a landing ja esta pronta". Antes de planejar Plans 02/03, conferir o estado de `nutrichat-landing/` (pasta separada no Desktop, fora do worktree) e perguntar se ainda quer fazer pelos planos GSD ou se ja foi entregue.

### 5. Fase 7 — Pagamentos (G) — SEM deploy

Decisao pendente: Stripe / Pix / Mercado Pago? Pacote 1 vs assinatura?

### 6. Deploy (POR ULTIMO — pedido do usuario)

<completed_work>
- Commit `e433917`: #4 Fatia 3 entregue. Detector overshoot inline em meal.ts com 5 callsites ligados + smoke 7/7 verde
- Migration de lanches aplicada no Supabase Cloud via CLI (db push aceito apos migration repair)
- Bloqueador humano da Fatia 2 (migration manual) removido
- Decisao tomada para o relatorio parrudo: streak + comparacao semanal + best day + barras ASCII
</completed_work>

<remaining_work>
1. #5 Relatorio semanal parrudo (M — 4 features novas + reformulacao prompt + smoke)
2. (Humano) Validacao E2E entrevista 14 etapas + lembretes chegando + alerta overshoot
3. (Humano) Validar PDF upload painel + barcode Cristal
4. VERIFICAR estado da landing antes de Plan 06-02/03
5. Fase 7 (Pagamentos — sem deploy)
6. Deploy (POR ULTIMO)
</remaining_work>

<decisions_made>
- **Detector overshoot inline (NAO cron)**: registro ja sabe saldo, gatilho natural, gratis
- **Strict `>` threshold (110% exato nao alerta)**: evita falso-alarme no limite
- **Helper centralizado em meal.ts**: 5 callsites mudam 1 linha cada — single source of truth
- **Mensagem mostra pct real + saldo/meta absolutos**: mais informativa que mensagem generica
- **Relatorio parrudo terá 4 features**: streak + comparacao semanal + best day + barras ASCII (mecanicas de habito comprovadas)
- **Migration aplicada via CLI** (nao mais manual no Studio): IPv6 timeout resolvido, repair --status applied foi necessario antes do push
</decisions_made>

<blockers>
Nenhum bloqueador ativo. Migration aplicada, codigo verde, sessao paused limpa.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — versao estruturada (autoritativo)
2. `backend/src/services/relatorio.ts` — estado atual do relatorio que sera refatorado. Ler antes de editar para entender que ja existe `DadosRelatorio`, `gerarMensagemIncentivo` (Haiku), e o loop por paciente
3. `backend/src/services/meal.ts:96-128` — `obterSaldoDia` e `formatarSaldoDia` — modelo de helpers de macros que pode ser reaproveitado
4. `backend/scripts/smoke-overshoot.ts` — modelo de smoke programatico para a Fatia parrudo
5. `n8n/workflows/nutrichat-cron-relatorio-semanal.json` — cron domingo 08h ja ativo, nao mexer (so o backend muda)
6. `CLAUDE.md` (raiz) — contexto rapido

## Critical Anti-Patterns (do NOT repeat these)

- **tsx watch no Docker/Windows nao recarrega** → `docker restart nutrichat_backend` apos editar .ts
- **Criar nova rota quando ja existe equivalente** → SEMPRE pesquisar `services/` e `routes/` antes de criar
- **Heuristica de tempo para status RAG** — removida em `24d8d43`, nunca reintroduzir
- **Agente fabricar resposta sem chunks RAG** — bloqueado em `40e9bca`, nao relaxar
- **Evento WhatsApp lowercase** `messages.upsert` — `backend/src/routes/webhook.ts:32`
- **Cron N8N timezone**: container em `America/Sao_Paulo`, expression e local
- **N8N workflows** usam header `x-internal-key` (nao `x-internal-token`)
- **Upsert em registros_diarios** sobrescreve — usar RPC `acumular_registro_diario`
- **Barcode strip**: exigir palavra 4+ letras alfa pra aceitar strip
- **30/40/30 hardcoded ELIMINADO em `f99507e`**: sempre passar por `obterMetas(dados)`
- **Restricoes alimentares**: coletar e nao injetar no Claude = alergia armazenada e nao aplicada
- **Falha de side-effect pos-registro NAO pode derrubar fluxo** — try/catch isolado (overshoot em `meal.ts:dispararAlertaOvershoot`, sync em `agent.ts:443`)
- **Prompt LLM sem regras de formato gera markdown padrao** — bloco FORMATACAO explicito (modelo em `calculos.ts:279-285`)
- **Regex pt-BR sem classes de char para cedilha/acento** → falha em `almoço`, `café`, `manhã`. Usar `[çc]`, `[eé]`, `[ãa]`
- **Adicionar feature em registrarRefeicao sem espelhar em vision.ts (3 sites) + agent.ts (1 site)** → bug parcial onde foto/barcode/rotulo nao recebem comportamento novo. SEMPRE checar grep de `registrarRefeicao` antes

## Infrastructure State

- Docker: 5 containers up. `nutrichat_backend` restartado em 2026-06-22T03:15Z apos Fatia 3
- Evolution API: instancia `nutrichat`, `state: "open"`, webhook → `http://backend:3001/api/webhook`
- Supabase Cloud: migrations 100% sincronizadas. Schema com colunas `horario_lanche_manha` + `horario_lanche_tarde` em `alertas_config` e `processamento_status` em `dietas`
- N8N: TZ `America/Sao_Paulo`. Workflows ativos: `nutrichat-cron-alertas` (*/15min), `nutrichat-cron-expiracao` (diario), `nutrichat-cron-relatorio-semanal` (domingo 08h)
- Smoke scripts no container: `smoke-alertas-config.ts` + `smoke-overshoot.ts` (copiados manualmente via `docker cp` — volume nao monta `scripts/`)

<context>
Sessao curta de 1 commit (Fatia 3 do #4) + aplicacao de migrations. Descoberta importante: CLI Supabase funcionou hoje (IPv6 antes bloqueava — esperava ser bloqueador eterno e nao era). Vale tentar CLI antes de orientar passo manual.

Usuario informou que landing ja esta pronta — Plans 06-02/03 podem nao ser mais necessarios, verificar antes de planejar.

Usuario quer relatorio semanal "parrudo" para criar vontade de continuar. Alinhamos em 4 features baseadas em mecanicas de habito: streak (vicio), comparacao (progresso visivel), best day (vitoria celebrada), barras ASCII (visual sem markdown). Tamanho M.

Pausa preventiva em 80% — proximo trabalho merece contexto fresco.

Proxima sessao: `/gsd-resume-work` → arrancar #5 relatorio parrudo com contexto novo.
</context>

<next_action>
1. **Proxima sessao Claude**: `/gsd-resume-work` → arrancar #5 Relatorio Semanal Parrudo:
   - Ler `backend/src/services/relatorio.ts` para entender a base (`DadosRelatorio`, loop por paciente, prompt do Haiku)
   - Adicionar 4 features:
     a) Streak: query registros_diarios DESC, contar dias consecutivos
     b) Comparacao vs semana anterior: query adicional + delta
     c) Best day: ordenar por proximidade da meta, destacar
     d) Barras ASCII com unicode blocks (▓░)
   - Reformular prompt do Haiku pra incluir vitorias contextualmente + micro-meta
   - `backend/scripts/smoke-relatorio-parrudo.ts` cobrindo combinacoes
   - Garantir mensagem <= 1500 chars
   - Typecheck + smoke + docker restart + commit

2. **Usuario humano** (em paralelo): validar E2E (entrevista 14 etapas + lembretes + alerta overshoot) + carry-overs (PDF, barcode Cristal)

3. **Antes de Plan 06-02/03**: confirmar com usuario o estado da landing (ele disse esta pronta)
</next_action>

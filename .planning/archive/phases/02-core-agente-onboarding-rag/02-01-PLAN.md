---
phase: 2
plan: "02-01"
title: "Webhook routing por tipo de midia"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/webhook.ts
  - backend/src/services/evolution.ts
  - backend/src/routes/api.ts
  - backend/src/index.ts
autonomous: true
requirements:
  - AGENT-01

must_haves:
  truths:
    - "POST /api/webhook aceita payload do Evolution API e retorna 200 imediatamente"
    - "Rota identifica tipo de midia a partir de messageType do payload (texto, audio, imagem)"
    - "Texto e processado de forma sincrona; audio/imagem retornam stub de resposta"
    - "EvolutionService.sendText() envia mensagem de texto via Evolution API REST"
    - "Backend responde com 200 antes de processar (fire-and-forget para nao dar timeout no N8N)"
  artifacts:
    - path: "backend/src/routes/webhook.ts"
      provides: "POST /api/webhook com routing por messageType"
      contains: "messageType"
    - path: "backend/src/services/evolution.ts"
      provides: "Cliente para enviar mensagens via Evolution API"
      contains: "sendText"
    - path: "backend/src/routes/api.ts"
      provides: "Router central /api que agrega todas as subrotas"
      contains: "webhookRouter"
  key_links:
    - from: "N8N workflow nutrichat-main-webhook.json"
      to: "backend/src/routes/webhook.ts"
      via: "HTTP POST http://backend:3001/api/webhook com payload JSON do Evolution API"
      pattern: "POST.*api/webhook"
    - from: "backend/src/services/evolution.ts"
      to: "Evolution API"
      via: "POST /message/sendText/:instance com apikey header"
      pattern: "sendText"
---

<objective>
Implementar o endpoint POST /api/webhook no backend Express que recebe mensagens do Evolution API (via N8N), identifica o tipo de midia e roteia para o handler correto. Criar o servico EvolutionService para envio de mensagens de volta ao paciente.

Purpose: Estabelecer a fundacao de comunicacao WhatsApp para todas as funcionalidades do agente. O N8N ja encaminha payloads para http://backend:3001/api/webhook — este plano implementa o receptor.
Output: Rota funcional com routing por tipo, servico de envio de mensagens, e estrutura de routes organizada.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infraestrutura-ambiente/01-CONTEXT.md
@.planning/phases/01-infraestrutura-ambiente/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar EvolutionService — cliente para envio de mensagens</name>
  <files>
    backend/src/services/evolution.ts
  </files>
  <read_first>
    - backend/src/config/env.ts (EVOLUTION_API_URL e EVOLUTION_API_KEY ja configurados)
    - backend/src/index.ts (entender estrutura atual)
    - .planning/phases/01-infraestrutura-ambiente/01-03-SUMMARY.md (instancia "nutrichat", API key "nutrichat_local_key")
  </read_first>
  <action>
Criar o servico de integracao com a Evolution API para envio de mensagens.

**Arquivo: backend/src/services/evolution.ts**

```typescript
import { env } from '../config/env';

const INSTANCE = 'nutrichat';

interface SendTextPayload {
  number: string;
  text: string;
}

export async function sendText(to: string, text: string): Promise<void> {
  const url = `${env.EVOLUTION_API_URL}/message/sendText/${INSTANCE}`;
  const payload: SendTextPayload = { number: to, text };

  const response = await fetch(url, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      apikey: env.EVOLUTION_API_KEY,
    },
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    const body = await response.text().catch(() => '');
    console.error(`[evolution] Falha ao enviar mensagem para ${to}: ${response.status} ${body}`);
    // Nao lanca excecao — falha de envio nao deve derrubar o webhook handler
  }
}
```

Nota: `fetch` e nativo no Node.js 20+. Nao e necessario instalar node-fetch.
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/evolution.ts &&
      grep -q "sendText" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/evolution.ts &&
      grep -q "EVOLUTION_API_URL" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/evolution.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/services/evolution.ts existe e exporta a funcao sendText
    - sendText usa env.EVOLUTION_API_URL e env.EVOLUTION_API_KEY (nao hardcoded)
    - sendText faz POST para /message/sendText/nutrichat com header apikey
    - Falha de envio e logada mas nao lanca excecao (nao derruba o webhook)
    - fetch nativo Node.js 20 — sem dependencia de node-fetch
  </acceptance_criteria>
  <done>EvolutionService criado com funcao sendText que encapsula chamada REST para a Evolution API.</done>
</task>

<task type="auto">
  <name>Task 2: Criar webhook router com routing por tipo de midia</name>
  <files>
    backend/src/routes/webhook.ts
  </files>
  <read_first>
    - backend/src/services/evolution.ts (importar sendText)
    - n8n/workflows/nutrichat-main-webhook.json (payload enviado: JSON.stringify($json.body))
    - .planning/REQUIREMENTS.md (AGENT-01: roteia texto, audio, imagem, codigo de barras)
  </read_first>
  <action>
Criar o router do webhook que recebe o payload do Evolution API via N8N e roteia por tipo.

O N8N encaminha o campo `$json.body` do webhook, que e o payload completo do Evolution API com esta estrutura:
```json
{
  "event": "MESSAGES_UPSERT",
  "instance": "nutrichat",
  "data": {
    "key": { "remoteJid": "5511999999999@s.whatsapp.net", "fromMe": false, "id": "..." },
    "message": {
      "conversation": "texto da mensagem",
      "audioMessage": { "url": "..." },
      "imageMessage": { "url": "...", "caption": "..." }
    },
    "messageType": "conversation",
    "pushName": "Nome do Contato"
  }
}
```

**Arquivo: backend/src/routes/webhook.ts**

```typescript
import { Router, Request, Response } from 'express';
import { sendText } from '../services/evolution';

export const webhookRouter = Router();

interface EvolutionPayload {
  event: string;
  instance: string;
  data: {
    key: { remoteJid: string; fromMe: boolean; id: string };
    message: Record<string, unknown>;
    messageType: string;
    pushName?: string;
  };
}

function extractPhoneNumber(remoteJid: string): string {
  // Remove sufixo @s.whatsapp.net ou @g.us
  return remoteJid.replace(/@[^@]+$/, '');
}

webhookRouter.post('/', async (req: Request, res: Response) => {
  // Responder 200 imediatamente — N8N nao pode aguardar processamento
  res.status(200).json({ status: 'received' });

  const payload = req.body as EvolutionPayload;

  // Ignorar mensagens enviadas pelo proprio bot
  if (payload?.data?.key?.fromMe) return;
  // Ignorar eventos que nao sao mensagens
  if (payload?.event !== 'MESSAGES_UPSERT') return;

  const { data } = payload;
  const phone = extractPhoneNumber(data.key.remoteJid);
  const messageType = data.messageType;

  console.log(`[webhook] Mensagem de ${phone} | tipo: ${messageType}`);

  try {
    switch (messageType) {
      case 'conversation':
      case 'extendedTextMessage': {
        const text =
          (data.message.conversation as string) ||
          (data.message.extendedTextMessage as { text: string })?.text ||
          '';
        await handleText(phone, text);
        break;
      }
      case 'audioMessage': {
        // Stub Fase 2 — transcricao via Groq Whisper implementada na Fase 3
        await sendText(phone, '🎤 Recebi seu audio! Transcricao chegando em breve...');
        break;
      }
      case 'imageMessage': {
        // Stub Fase 2 — analise de imagem via Claude Vision implementada na Fase 3
        await sendText(phone, '📸 Recebi sua imagem! Analise chegando em breve...');
        break;
      }
      default: {
        console.log(`[webhook] Tipo de mensagem nao suportado: ${messageType}`);
      }
    }
  } catch (err) {
    console.error('[webhook] Erro ao processar mensagem:', err);
  }
});

async function handleText(phone: string, text: string): Promise<void> {
  // Stub — sera substituido pelo agente Claude na Fase 2 (02-03)
  console.log(`[webhook] Texto de ${phone}: "${text}"`);
  await sendText(phone, `✅ Recebi: "${text}" (agente em configuracao...)`);
}
```

Nota: O switch tem um case `default` que apenas loga — nao envia mensagem para tipos desconhecidos (stickers, reactions, etc.) para evitar spam.
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/webhook.ts &&
      grep -q "webhookRouter" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/webhook.ts &&
      grep -q "messageType" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/webhook.ts &&
      grep -q "fromMe" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/webhook.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/webhook.ts existe e exporta webhookRouter
    - Responde 200 ANTES de processar (fire-and-forget)
    - Ignora mensagens com fromMe: true
    - Switch por messageType cobre: conversation, extendedTextMessage, audioMessage, imageMessage
    - Extrai numero de telefone removendo sufixo @s.whatsapp.net
    - handleText e um stub que confirma recebimento (sera substituido em 02-03)
  </acceptance_criteria>
  <done>Webhook router criado com routing completo por tipo de midia e resposta imediata 200.</done>
</task>

<task type="auto">
  <name>Task 3: Criar router central /api e registrar no Express</name>
  <files>
    backend/src/routes/api.ts
    backend/src/index.ts
  </files>
  <read_first>
    - backend/src/index.ts (adicionar /api router)
    - backend/src/routes/webhook.ts (importar webhookRouter)
  </read_first>
  <action>
Criar o router central que agrega todas as subrotas da API e registrar no Express.

**Arquivo: backend/src/routes/api.ts**

```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
// Proximas rotas adicionadas por planos seguintes:
// apiRouter.use('/rag', ragRouter);        // 02-02
// apiRouter.use('/expiracao', expiracaoRouter); // Fase 4
```

**Modificar: backend/src/index.ts**

Adicionar o apiRouter abaixo dos middlewares existentes:

```typescript
import { apiRouter } from './routes/api';

// Adicionar apos as linhas de middleware existentes (cors, express.json):
app.use('/api', apiRouter);
```

O arquivo final de index.ts deve ficar assim:

```typescript
import express from 'express';
import cors from 'cors';
import { env } from './config/env';
import { healthRouter } from './routes/health';
import { apiRouter } from './routes/api';

const app = express();

app.use(cors());
app.use(express.json({ limit: '10mb' }));

app.use('/health', healthRouter);
app.use('/api', apiRouter);

app.use((_req, res) => {
  res.status(404).json({ error: 'Not found' });
});

const server = app.listen(env.PORT, () => {
  console.log(`[nutrichat-backend] Rodando na porta ${env.PORT} (${env.NODE_ENV})`);
});

process.on('SIGTERM', () => {
  console.log('[nutrichat-backend] SIGTERM recebido, encerrando...');
  server.close(() => process.exit(0));
});

export { app, server };
```
  </action>
  <verify>
    <automated>
      test -f /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts &&
      grep -q "apiRouter" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/index.ts &&
      grep -q "/api" /c/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/index.ts &&
      cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck 2>&1 | grep -v "error" | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/routes/api.ts existe e exporta apiRouter com /webhook registrado
    - backend/src/index.ts importa apiRouter e registra em app.use('/api', apiRouter)
    - npm run typecheck roda sem erros TypeScript
    - POST /api/webhook esta mapeado atraves de: index.ts → api.ts → webhook.ts
  </acceptance_criteria>
  <done>Router central /api criado e registrado. Estrutura de rotas pronta para expansao por planos seguintes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N8N → Backend | POST /api/webhook sem autenticacao — risco mitigado por firewall Docker (apenas containers internos acessam) |
| Backend → Evolution API | Usa EVOLUTION_API_KEY no header — chave protegida por .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | POST /api/webhook | accept (v1) | Endpoint acessivel apenas na rede Docker interna. Validacao de assinatura postergada para v2. |
| T-02-01-02 | DoS | Webhook handler | mitigate | Resposta 200 imediata antes do processamento; processamento async nao bloqueia N8N. |
| T-02-01-03 | Information Disclosure | Logs de mensagens | mitigate | Logs mostram apenas numero e tipo — nunca conteudo completo da mensagem. |
</threat_model>

<verification>
Apos execucao, testar manualmente:
1. `cd backend && npm run typecheck` — sem erros
2. Enviar mensagem de texto para o WhatsApp conectado e verificar log: `[webhook] Mensagem de <numero> | tipo: conversation`
3. Verificar que N8N executa o workflow com status verde (sem timeout)
4. Verificar que bot responde com confirmacao de recebimento
</verification>

<success_criteria>
- backend/src/services/evolution.ts exporta sendText()
- backend/src/routes/webhook.ts exporta webhookRouter com routing por messageType
- backend/src/routes/api.ts exporta apiRouter com /webhook registrado
- backend/src/index.ts registra /api router
- npm run typecheck sem erros
- POST /api/webhook responde 200 imediatamente
</success_criteria>

---
phase: 04-alertas-expiracao-relatorio-semanal
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - supabase/migrations/20260425000003_add_registrar_agua_diaria.sql
  - backend/src/services/relatorio.ts
  - backend/src/routes/relatorio.ts
  - backend/src/routes/api.ts
  - backend/src/services/agent.ts
  - n8n/workflows/nutrichat-cron-relatorio-semanal.json
autonomous: true
requirements:
  - AGENT-20

must_haves:
  truths:
    - "POST /api/relatorio/semanal retorna 202 com header x-internal-key correto"
    - "Relatorio agrega registros_diarios dos ultimos 7 dias (kcal total, medias de macros, media agua_ml)"
    - "Relatorio conta quantos dias o paciente bateu >= 90% da meta de kcal"
    - "Mensagem de incentivo e gerada por Claude Haiku (texto puro, sem JSON)"
    - "Relatorio e enviado via WhatsApp apenas para pacientes com ativo=true"
    - "POST /api/agua/registrar acumula agua_ml via RPC registrar_agua_diaria"
    - "agent.ts detecta mensagens de agua ('bebi', 'ml', 'litro') e chama registrarAgua"
    - "Cron N8N dispara domingo as 08:00 e chama POST /api/relatorio/semanal"
  artifacts:
    - path: "supabase/migrations/20260425000003_add_registrar_agua_diaria.sql"
      provides: "RPC registrar_agua_diaria(p_paciente_id, p_data, p_agua_ml) com ON CONFLICT"
      contains: "registrar_agua_diaria"
    - path: "backend/src/services/relatorio.ts"
      provides: "gerarRelatorioSemanal() — agrega registros_diarios + Claude Haiku + sendText"
      exports: ["gerarRelatorioSemanal"]
    - path: "backend/src/routes/relatorio.ts"
      provides: "POST /semanal e POST /agua/registrar com requireInternalKey"
      exports: ["relatorioRouter", "aguaRouter"]
    - path: "n8n/workflows/nutrichat-cron-relatorio-semanal.json"
      provides: "Cron domingo 08:00 chamando /api/relatorio/semanal"
      contains: "0 8 * * 0"
  key_links:
    - from: "n8n/workflows/nutrichat-cron-relatorio-semanal.json"
      to: "backend POST /api/relatorio/semanal"
      via: "HTTP Request com x-internal-key"
      pattern: "api/relatorio/semanal"
    - from: "backend/src/services/agent.ts"
      to: "backend/src/routes/api.ts via chamada interna ou service"
      via: "detectar 'bebi.*ml' e chamar registrarAgua(pacienteId, ml)"
      pattern: "registrarAgua"
    - from: "backend/src/services/relatorio.ts"
      to: "supabase registros_diarios"
      via: "query .gte/.lte de data + agregacao"
      pattern: "registros_diarios"
---

<objective>
Implementar relatório semanal automatico via Claude Haiku (AGENT-20) e registro de agua via texto (D-03). Inclui RPC Supabase para acumular agua_ml, endpoint de relatorio e cron N8N para domingo.

Purpose: Todo domingo o paciente recebe um resumo semanal motivador com total kcal, medias de macros, hidratacao media, dias que bateram meta e mensagem de incentivo personalizada. Alem disso, "bebi 500ml" ou "tomei 1 litro de agua" passa a ser registrado corretamente.

Output: migration RPC agua, relatorio.ts (service + route), agent.ts modificado para agua, nutrichat-cron-relatorio-semanal.json.
</objective>

<execution_context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/CLAUDE.md

<interfaces>
<!-- Contratos relevantes extraidos do codebase. O executor usa diretamente — sem explorar. -->

De backend/src/services/meal.ts (padrao Claude Haiku para texto puro):
```typescript
const response = await claude.messages.create({
  model: 'claude-haiku-4-5-20251001',
  max_tokens: 256,
  messages: [{ role: 'user', content: prompt }],
});
// Para texto motivacional: NÃO usar JSON — pegar direto:
const texto = response.content[0].type === 'text' ? response.content[0].text : '';
```

De backend/src/services/meal.ts (padrao RPC Supabase):
```typescript
const { error: rpcError } = await supabase.rpc('acumular_registro_diario', {
  p_paciente_id: pacienteId,
  p_data: hoje,
  p_kcal: m.kcal,
  p_proteina_g: m.proteina_g,
  p_carbo_g: m.carbo_g,
  p_gordura_g: m.gordura_g,
});
```

De backend/src/services/agent.ts (onde inserir deteccao de agua — linha ~268):
```typescript
// Trecho atual (linha ~268-280):
const textoLower = texto.toLowerCase();
const ehSubstituicao = /substitu|nao tenho|não tenho|alternativa|trocar/.test(textoLower);
const ehRegistro = /comi|tomei|bebi|almocei|jantei|.../.test(textoLower);

if (ehRegistro || ehSubstituicao) {
  await mealService.processarTextoRefeicao(phone, texto, paciente);
} else {
  // Consulta RAG
}
```

Deteccao de agua DEVE ser extraida DO fluxo ehRegistro e tratada separadamente:
- Padrao de texto de agua: /bebi|tomei.*agua|agua.*tomei|\d+\s*ml|\d+\s*litro|\d+\s*copo/
- Mas: "tomei 200g de frango" tambem tem "tomei" — a deteccao precisa de "ml" ou "litro" ou "agua"
- Padrao correto: /(\d+)\s*(ml|litros?|copos?).*agua|agua.*(\d+)\s*(ml|litros?|copos?)|bebi\s+\d+\s*(ml|litros?|copos?)/

De backend/src/routes/boas-vindas.ts (padrao requireInternalKey — mesmo para relatorio):
```typescript
function requireInternalKey(req: Request, res: Response, next: NextFunction): void {
  if (req.headers['x-internal-key'] !== env.INTERNAL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}
```

De backend/src/routes/api.ts (estado APOS planos 04-01 e 04-02):
```typescript
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';
import { expiracaoRouter } from './expiracao';
import { alertasRouter } from './alertas';
// Adicionar: import { relatorioRouter } from './relatorio';
// Adicionar: import { aguaRouter } from './agua';

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
apiRouter.use('/boas-vindas', boasVindasRouter);
apiRouter.use('/expiracao', expiracaoRouter);
apiRouter.use('/alertas', alertasRouter);
// Adicionar: apiRouter.use('/relatorio', relatorioRouter);
// Adicionar: apiRouter.use('/agua', aguaRouter);
```

Schema registros_diarios (migration 001):
```sql
registros_diarios(
  paciente_id UUID,
  data DATE,
  kcal_consumido NUMERIC(8,2) DEFAULT 0,
  proteina_g NUMERIC(8,2) DEFAULT 0,
  carbo_g NUMERIC(8,2) DEFAULT 0,
  gordura_g NUMERIC(8,2) DEFAULT 0,
  agua_ml INTEGER DEFAULT 0,
  UNIQUE(paciente_id, data)
)
```

entrevista_dados com tdee_kcal — buscar via estado da entrevista:
```typescript
// De conversation.ts (getEstado retorna):
// estado.dados.tdee_kcal: number
// Para o relatorio, buscar de pacientes JOIN entrevista_dados — ou usar valor default 2000
// A query mais simples e buscar diretamente da tabela de estados de conversa
// ou aceitar que relatorio usa tdee_kcal = 2000 quando nao disponivel (safe default)
```

RPC acumular_registro_diario existente (migration 007 — para referencia de padrao SQL):
```sql
CREATE OR REPLACE FUNCTION acumular_registro_diario(
  p_paciente_id UUID, p_data DATE,
  p_kcal NUMERIC, p_proteina_g NUMERIC, p_carbo_g NUMERIC, p_gordura_g NUMERIC
) RETURNS VOID AS $$
BEGIN
  INSERT INTO registros_diarios (paciente_id, data, kcal_consumido, proteina_g, carbo_g, gordura_g)
  VALUES (p_paciente_id, p_data, p_kcal, p_proteina_g, p_carbo_g, p_gordura_g)
  ON CONFLICT (paciente_id, data) DO UPDATE SET
    kcal_consumido = registros_diarios.kcal_consumido + EXCLUDED.kcal_consumido,
    proteina_g     = registros_diarios.proteina_g     + EXCLUDED.proteina_g,
    carbo_g        = registros_diarios.carbo_g        + EXCLUDED.carbo_g,
    gordura_g      = registros_diarios.gordura_g      + EXCLUDED.gordura_g,
    updated_at     = now();
END;
$$ LANGUAGE plpgsql;
```

N8N ScheduleTrigger (cron domingo 08:00):
```json
{
  "field": "cronExpression",
  "expression": "0 8 * * 0"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration RPC registrar_agua_diaria e registro de agua em agent.ts</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/meal.ts
  </read_first>
  <files>
    supabase/migrations/20260425000003_add_registrar_agua_diaria.sql
    backend/src/services/agent.ts
  </files>
  <action>
**supabase/migrations/20260425000003_add_registrar_agua_diaria.sql** — Criar RPC de acumulacao de agua (D-03, Opcao A da pesquisa — RPC separada sem risco de regressao):

```sql
-- Migration 003 (Fase 4): RPC para registrar agua diaria
-- RPC separada de acumular_registro_diario para evitar regressao (D-03)

CREATE OR REPLACE FUNCTION registrar_agua_diaria(
  p_paciente_id UUID,
  p_data        DATE,
  p_agua_ml     INTEGER
) RETURNS VOID AS $$
BEGIN
  INSERT INTO registros_diarios (paciente_id, data, agua_ml)
  VALUES (p_paciente_id, p_data, p_agua_ml)
  ON CONFLICT (paciente_id, data) DO UPDATE SET
    agua_ml    = registros_diarios.agua_ml + EXCLUDED.agua_ml,
    updated_at = now();
END;
$$ LANGUAGE plpgsql;
```

Aplicar no Supabase Cloud via CLI ou Dashboard SQL Editor.

**backend/src/services/agent.ts** — Adicionar funcao `registrarAgua` e detectar mensagens de agua no fluxo principal.

Mudancas no arquivo (duas edicoes):

**Edicao 1:** Adicionar import de supabase e funcao `registrarAgua` (inserir apos os imports existentes, antes de `const claude = ...`):

```typescript
import { createClient as createSupabaseClient } from '@supabase/supabase-js';

// Acumular agua via RPC (D-03)
async function registrarAgua(pacienteId: string, aguaMl: number): Promise<string> {
  const supabase = createSupabaseClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);
  const hoje = new Date().toISOString().slice(0, 10);
  const { error } = await supabase.rpc('registrar_agua_diaria', {
    p_paciente_id: pacienteId,
    p_data: hoje,
    p_agua_ml: aguaMl,
  });
  if (error) {
    console.error('[agent] Erro ao registrar agua:', error.message);
    return `⚠️ Erro ao registrar hidratação. Tente novamente.`;
  }
  return `💧 *${aguaMl}ml* de água registrados! Continue se hidratando. 💚`;
}
```

ATENCAO: `createClient` ja e importado de supabase em agent.ts (linha ~2). Usar alias `createSupabaseClient` para evitar conflito OU verificar se o import ja existe e reutilizar. Se `createClient` ja foi importado, nao importar novamente — apenas usar `createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY)` diretamente dentro da funcao.

**Edicao 2:** No bloco de deteccao de intencao (em torno da linha 268), ANTES do bloco `if (ehRegistro || ehSubstituicao)`, adicionar deteccao especifica de agua:

```typescript
  // D-03: Deteccao de registro de agua — verificar ANTES de ehRegistro para extrair
  const AGUA_RE = /(\d+)\s*(ml|litros?|copos?)|bebi\s+\d+\s*(ml|litros?|copos?)/i;
  const ehAguaMsg = AGUA_RE.test(textoLower) && /agua|bebi|hidrat|bebo/i.test(textoLower);
  
  if (ehAguaMsg) {
    // Extrair quantidade em ml
    const matchMl = texto.match(/(\d+)\s*ml/i);
    const matchLitro = texto.match(/(\d+)\s*litros?/i);
    const matchCopo = texto.match(/(\d+)\s*copos?/i);
    let aguaMl = 0;
    if (matchMl) aguaMl = parseInt(matchMl[1], 10);
    else if (matchLitro) aguaMl = parseInt(matchLitro[1], 10) * 1000;
    else if (matchCopo) aguaMl = parseInt(matchCopo[1], 10) * 250; // 250ml por copo
    
    if (aguaMl > 0 && aguaMl <= 5000) { // sanity check
      const resposta = await registrarAgua(paciente.id, aguaMl);
      await sendText(phone, resposta);
      return;
    }
  }
```

Inserir ANTES da linha `if (ehRegistro || ehSubstituicao) {`.

Apos editar, executar: `docker restart nutrichat_backend`
  </action>
  <verify>
    <automated>
      grep -n "registrar_agua_diaria" "C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260425000003_add_registrar_agua_diaria.sql" &&
      grep -n "registrarAgua" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts" &&
      grep -n "AGUA_RE" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts"
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260425000003_add_registrar_agua_diaria.sql existe e contem "registrar_agua_diaria"
    - grep "ON CONFLICT" supabase/migrations/20260425000003_add_registrar_agua_diaria.sql retorna resultado (acumulacao correta)
    - grep "registrarAgua" backend/src/services/agent.ts retorna resultado
    - grep "AGUA_RE" backend/src/services/agent.ts retorna resultado (regex de deteccao presente)
    - Apos restart do backend, enviar "bebi 500ml de agua" pelo WhatsApp — deve retornar confirmacao de 500ml registrados
    - Enviar "bebi 1 litro de agua" — deve retornar confirmacao de 1000ml registrados
    - Enviar "bebi 2 copos de agua" — deve retornar confirmacao de 500ml registrados
  </acceptance_criteria>
  <done>
    - RPC registrar_agua_diaria criada no Supabase (migration aplicada)
    - "bebi 500ml de agua" recebe confirmacao e acumula agua_ml em registros_diarios
    - "bebi 1 litro de agua" converte para 1000ml e registra
    - "tomei 200g de frango" NAO e tratado como agua (regex nao falsa positivo)
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar relatorio.ts (service + route), registrar em api.ts e criar cron N8N</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/meal.ts
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/n8n/workflows/nutrichat-cron-expiracao.json
  </read_first>
  <files>
    backend/src/services/relatorio.ts
    backend/src/routes/relatorio.ts
    backend/src/routes/api.ts
    n8n/workflows/nutrichat-cron-relatorio-semanal.json
  </files>
  <action>
**backend/src/services/relatorio.ts** — Criar com `gerarRelatorioSemanal()`:

```typescript
import Anthropic from '@anthropic-ai/sdk';
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';
import { sendText } from './evolution';

const claude = new Anthropic({ apiKey: env.CLAUDE_API_KEY });
const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);

interface DadosRelatorio {
  dias_com_registro: number;
  total_kcal: number;
  media_kcal: number;
  media_proteina_g: number;
  media_carbo_g: number;
  media_gordura_g: number;
  media_agua_ml: number;
  dias_bateram_meta: number;
  meta_kcal: number;
}

async function gerarMensagemIncentivo(dados: DadosRelatorio, nomePaciente: string): Promise<string> {
  const prompt = `Você é o NutriChat, assistente nutricional do WhatsApp. Gere uma mensagem motivacional e personalizada para o relatório semanal de ${nomePaciente}.

Dados da semana:
- Dias com registro: ${dados.dias_com_registro}/7
- Total kcal consumido: ${Math.round(dados.total_kcal)} kcal
- Média kcal/dia: ${Math.round(dados.media_kcal)} kcal (meta: ${dados.meta_kcal} kcal)
- Dias que bateram a meta: ${dados.dias_bateram_meta}
- Média proteína: ${Math.round(dados.media_proteina_g)}g/dia
- Média hidratação: ${Math.round(dados.media_agua_ml)}ml/dia

Escreva 1-2 frases de incentivo em português, levando em conta o desempenho. Se foi uma boa semana, parabenize. Se foi fraca, encoraje sem julgamento. Seja caloroso e direto. NÃO use markdown. APENAS texto puro.`;

  const response = await claude.messages.create({
    model: 'claude-haiku-4-5-20251001',
    max_tokens: 150,
    messages: [{ role: 'user', content: prompt }],
  });

  return response.content[0].type === 'text' ? response.content[0].text.trim() : 'Continue assim!';
}

export async function gerarRelatorioSemanal(): Promise<void> {
  const hoje = new Date().toISOString().slice(0, 10);
  const seteDiasAtras = new Date();
  seteDiasAtras.setDate(seteDiasAtras.getDate() - 7);
  const dataInicio = seteDiasAtras.toISOString().slice(0, 10);

  // Buscar todos os pacientes ativos
  const { data: pacientes, error: errPacientes } = await supabase
    .from('pacientes')
    .select('id, nome, whatsapp, entrevista_dados')
    .eq('ativo', true);

  if (errPacientes) {
    console.error('[relatorio] Erro ao buscar pacientes:', errPacientes.message);
    return;
  }

  if (!pacientes || pacientes.length === 0) {
    console.log('[relatorio] Nenhum paciente ativo — skip');
    return;
  }

  for (const p of pacientes) {
    try {
      // Agregar registros_diarios dos ultimos 7 dias
      const { data: registros, error: errRegistros } = await supabase
        .from('registros_diarios')
        .select('kcal_consumido, proteina_g, carbo_g, gordura_g, agua_ml')
        .eq('paciente_id', p.id)
        .gte('data', dataInicio)
        .lt('data', hoje);

      if (errRegistros) {
        console.error(`[relatorio] Erro ao buscar registros de ${p.nome}:`, errRegistros.message);
        continue;
      }

      if (!registros || registros.length === 0) {
        await sendText(
          p.whatsapp as string,
          `📊 *Relatório Semanal — ${p.nome}*\n\nEsta semana não encontrei registros de refeições. Lembre-se de registrar o que você come para acompanharmos juntos! 💚`,
        );
        continue;
      }

      // Calcular agregacoes
      const n = registros.length;
      const totalKcal = registros.reduce((s, r) => s + Number(r.kcal_consumido), 0);
      const mediaKcal = totalKcal / n;
      const mediaProteina = registros.reduce((s, r) => s + Number(r.proteina_g), 0) / n;
      const mediaCarbo = registros.reduce((s, r) => s + Number(r.carbo_g), 0) / n;
      const mediaGordura = registros.reduce((s, r) => s + Number(r.gordura_g), 0) / n;
      const mediaAgua = registros.reduce((s, r) => s + Number(r.agua_ml), 0) / n;

      // Meta kcal: buscar de entrevista_dados ou usar default 2000
      const entrevistaDados = (p.entrevista_dados as Record<string, unknown>) || {};
      const metaKcal = Number(entrevistaDados['tdee_kcal']) || 2000;

      // Dias que bateram >= 90% da meta
      const diasBateramMeta = registros.filter(
        (r) => Number(r.kcal_consumido) >= metaKcal * 0.9,
      ).length;

      const dados: DadosRelatorio = {
        dias_com_registro: n,
        total_kcal: totalKcal,
        media_kcal: mediaKcal,
        media_proteina_g: mediaProteina,
        media_carbo_g: mediaCarbo,
        media_gordura_g: mediaGordura,
        media_agua_ml: mediaAgua,
        dias_bateram_meta: diasBateramMeta,
        meta_kcal: metaKcal,
      };

      const incentivo = await gerarMensagemIncentivo(dados, p.nome as string);

      const mensagem =
        `📊 *Relatório Semanal — ${p.nome}*\n\n` +
        `🗓️ Dias com registro: *${n}/7*\n` +
        `🔥 Total kcal: *${Math.round(totalKcal)} kcal*\n` +
        `📈 Média diária: *${Math.round(mediaKcal)} kcal* (meta: ${metaKcal} kcal)\n` +
        `🎯 Dias que bateram a meta: *${diasBateramMeta}*\n\n` +
        `💪 Macros médios/dia:\n` +
        `• Proteína: ${Math.round(mediaProteina)}g\n` +
        `• Carboidratos: ${Math.round(mediaCarbo)}g\n` +
        `• Gordura: ${Math.round(mediaGordura)}g\n\n` +
        `💧 Hidratação média: *${Math.round(mediaAgua)}ml/dia*\n\n` +
        `💚 ${incentivo}`;

      await sendText(p.whatsapp as string, mensagem);
      console.log(`[relatorio] Relatorio enviado para ${p.nome}`);
    } catch (err) {
      console.error(`[relatorio] Erro ao processar paciente ${p.nome}:`, err);
    }
  }

  console.log('[relatorio] Relatorio semanal concluido');
}
```

NOTA: `entrevista_dados` nao existe como coluna direta em `pacientes`. Os dados da entrevista ficam em `conversation_states` (tabela de estados). Para simplificar, buscar `tdee_kcal` da tabela de estados de conversa OU aceitar o default 2000. A abordagem mais segura para v1 e buscar da tabela `conversation_states` via join:

```typescript
// Alternativa mais precisa para buscar tdee_kcal:
const { data: conv } = await supabase
  .from('conversation_states')
  .select('dados')
  .eq('paciente_id', p.id)
  .maybeSingle();
const metaKcal = Number((conv?.dados as Record<string, unknown>)?.['tdee_kcal']) || 2000;
```

Usar esta alternativa em vez de `p.entrevista_dados` no loop de pacientes. Ajustar o select inicial de `pacientes` para nao incluir `entrevista_dados` (que nao existe na tabela).

**backend/src/routes/relatorio.ts** — Criar com dois endpoints:
- POST /semanal — cron N8N chama este endpoint
- (registro de agua fica em rota separada `/agua`)

```typescript
import { Router, Request, Response, NextFunction } from 'express';
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';
import { gerarRelatorioSemanal } from '../services/relatorio';

export const relatorioRouter = Router();

function requireInternalKey(req: Request, res: Response, next: NextFunction): void {
  if (req.headers['x-internal-key'] !== env.INTERNAL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}

relatorioRouter.post('/semanal', requireInternalKey, async (_req: Request, res: Response) => {
  res.status(202).json({ status: 'processando' });
  gerarRelatorioSemanal().catch((err) => {
    console.error('[relatorio] Erro ao gerar relatorio semanal:', err);
  });
});
```

Criar tambem `backend/src/routes/agua.ts` para o endpoint de registro de agua (D-03 — acumular agua_ml):

```typescript
import { Router, Request, Response, NextFunction } from 'express';
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';

export const aguaRouter = Router();

function requireInternalKey(req: Request, res: Response, next: NextFunction): void {
  if (req.headers['x-internal-key'] !== env.INTERNAL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}

aguaRouter.post('/registrar', requireInternalKey, async (req: Request, res: Response) => {
  const { paciente_id, agua_ml } = req.body as { paciente_id?: string; agua_ml?: number };

  if (!paciente_id || !agua_ml || agua_ml <= 0 || agua_ml > 5000) {
    res.status(400).json({ error: 'paciente_id e agua_ml (1-5000) sao obrigatorios' });
    return;
  }

  const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);
  const hoje = new Date().toISOString().slice(0, 10);

  const { error } = await supabase.rpc('registrar_agua_diaria', {
    p_paciente_id: paciente_id,
    p_data: hoje,
    p_agua_ml: agua_ml,
  });

  if (error) {
    res.status(500).json({ error: error.message });
    return;
  }

  res.status(200).json({ status: 'registrado', agua_ml, data: hoje });
});
```

**backend/src/routes/api.ts** — Adicionar imports e registros das novas rotas:

```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';
import { expiracaoRouter } from './expiracao';
import { alertasRouter } from './alertas';
import { relatorioRouter } from './relatorio';
import { aguaRouter } from './agua';

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
apiRouter.use('/boas-vindas', boasVindasRouter);
apiRouter.use('/expiracao', expiracaoRouter);
apiRouter.use('/alertas', alertasRouter);
apiRouter.use('/relatorio', relatorioRouter);
apiRouter.use('/agua', aguaRouter);
```

**n8n/workflows/nutrichat-cron-relatorio-semanal.json** — Criar cron domingo 08:00:

```json
{
  "name": "NutriChat — Relatorio Semanal (Domingo 08h)",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 8 * * 0"
            }
          ]
        }
      },
      "id": "cron-relatorio",
      "name": "Cron Domingo 08h",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1,
      "position": [200, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=http://backend:3001/api/relatorio/semanal",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-internal-key",
              "value": "nutrichat_internal_dev_key_2026"
            }
          ]
        },
        "options": {}
      },
      "id": "gerar-relatorio",
      "name": "Gerar Relatorio Semanal",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [400, 300]
    }
  ],
  "connections": {
    "Cron Domingo 08h": {
      "main": [[{ "node": "Gerar Relatorio Semanal", "type": "main", "index": 0 }]]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1"
  }
}
```

Apos editar os TypeScript, executar: `docker restart nutrichat_backend`
  </action>
  <verify>
    <automated>
      grep -n "relatorioRouter" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts" &&
      grep -n "gerarRelatorioSemanal" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/relatorio.ts" &&
      grep -n "0 8 \* \* 0" "C:/Users/botel/OneDrive/Desktop/nutri_fit/n8n/workflows/nutrichat-cron-relatorio-semanal.json" &&
      curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3001/api/relatorio/semanal -H "x-internal-key: nutrichat_internal_dev_key_2026"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "relatorioRouter" backend/src/routes/api.ts retorna resultado
    - grep "aguaRouter" backend/src/routes/api.ts retorna resultado
    - backend/src/services/relatorio.ts existe e contem "gerarRelatorioSemanal"
    - backend/src/routes/relatorio.ts existe e contem "POST /semanal"
    - backend/src/routes/agua.ts existe e contem "registrar_agua_diaria"
    - n8n/workflows/nutrichat-cron-relatorio-semanal.json existe e contem "0 8 * * 0"
    - curl -X POST http://localhost:3001/api/relatorio/semanal -H "x-internal-key: nutrichat_internal_dev_key_2026" retorna HTTP 202
    - curl -X POST http://localhost:3001/api/relatorio/semanal sem header retorna HTTP 401
    - curl -X POST http://localhost:3001/api/agua/registrar -H "x-internal-key: nutrichat_internal_dev_key_2026" -H "Content-Type: application/json" -d '{"paciente_id":"VALID_UUID","agua_ml":500}' retorna HTTP 200
  </acceptance_criteria>
  <done>
    - POST /api/relatorio/semanal retorna 202 e envia relatorio para pacientes ativos com dados
    - POST /api/relatorio/semanal para paciente sem registros envia mensagem de "sem dados esta semana"
    - POST /api/agua/registrar acumula agua_ml no Supabase via RPC registrar_agua_diaria
    - Cron N8N nutrichat-cron-relatorio-semanal.json criado com schedule domingo 08:00
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N8N → Backend /api/relatorio/semanal | Chamada HTTP interna na rede Docker |
| Internet → /api/agua/registrar | Endpoint interno; paciente_id nao pode vir de fonte externa |
| Claude Haiku output → WhatsApp | Texto gerado por LLM enviado diretamente; sem JSON para corromper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | POST /api/relatorio/semanal | mitigate | requireInternalKey middleware em toda request |
| T-04-03-02 | Spoofing | POST /api/agua/registrar | mitigate | requireInternalKey middleware — apenas N8N/backend interno chama |
| T-04-03-03 | Tampering | agua_ml no body | mitigate | Validar 1 <= agua_ml <= 5000 na rota antes de chamar RPC |
| T-04-03-04 | Information Disclosure | Relatorio enviado para paciente expirado | mitigate | gerarRelatorioSemanal() filtra WHERE ativo=true antes de enviar |
| T-04-03-05 | Tampering | Claude Haiku gerando markdown no incentivo | accept | Usando response.content[0].text diretamente (texto puro); sem parse JSON |
</threat_model>

<verification>
Testes manuais apos execucao do plano:

1. Verificar rotas registradas:
```bash
grep "relatorioRouter\|aguaRouter" backend/src/routes/api.ts
```

2. Testar endpoint de relatorio sem autenticacao (401):
```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3001/api/relatorio/semanal
# Esperado: 401
```

3. Testar relatorio manual (deve retornar 202 e enviar relatorio se houver dados):
```bash
curl -s -X POST http://localhost:3001/api/relatorio/semanal \
  -H "x-internal-key: nutrichat_internal_dev_key_2026"
# Esperado: {"status":"processando"}
# Log do backend: "[relatorio] Relatorio enviado para Gabriel Botelho..."
# WhatsApp: paciente recebe relatorio formatado
```

4. Testar registro de agua via endpoint:
```bash
# Substituir PACIENTE_ID pelo UUID real do paciente de teste no Supabase
curl -s -X POST http://localhost:3001/api/agua/registrar \
  -H "x-internal-key: nutrichat_internal_dev_key_2026" \
  -H "Content-Type: application/json" \
  -d '{"paciente_id":"SEU-UUID-AQUI","agua_ml":500}'
# Esperado: {"status":"registrado","agua_ml":500,"data":"2026-04-25"}
```

5. Testar registro de agua via WhatsApp (apos restart do backend):
   - Enviar "bebi 500ml de agua" pelo WhatsApp
   - Esperado: "💧 500ml de água registrados! Continue se hidratando. 💚"
   - Verificar no Supabase que agua_ml aumentou em registros_diarios

6. Verificar JSON do cron N8N:
```bash
grep "0 8 \* \* 0" n8n/workflows/nutrichat-cron-relatorio-semanal.json
# Deve retornar a linha com a expressao cron
```
</verification>

<success_criteria>
- POST /api/relatorio/semanal com x-internal-key retorna 202
- POST /api/relatorio/semanal sem header retorna 401
- Relatorio gerado inclui dias_com_registro, total_kcal, medias de macros, agua media, dias_bateram_meta, mensagem de incentivo (Claude Haiku)
- Paciente sem registros recebe mensagem de "sem dados esta semana" (nao crash)
- POST /api/agua/registrar valida agua_ml entre 1-5000
- "bebi 500ml de agua" no WhatsApp registra 500ml e retorna confirmacao
- "bebi 1 litro" registra 1000ml
- Cron n8n/workflows/nutrichat-cron-relatorio-semanal.json criado com expressao "0 8 * * 0"
- Relatorio nao e enviado para pacientes com ativo=false
</success_criteria>

<output>
Apos conclusao, criar C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/04-alertas-expiracao-relatorio-semanal/04-03-SUMMARY.md
</output>

---
phase: 03-registro-refeicoes-multimodal
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260425000001_add_acumular_registro.sql
autonomous: true
requirements:
  - AGENT-13
  - AGENT-15

must_haves:
  truths:
    - "RPC acumular_registro_diario existe no Supabase e acumula valores sem sobrescrever"
    - "Chamar a RPC duas vezes com 500 kcal resulta em 1000 kcal em registros_diarios, não 500"
    - "A migration aplica sem erros no Supabase"
  artifacts:
    - path: "supabase/migrations/20260425000001_add_acumular_registro.sql"
      provides: "RPC acumular_registro_diario com lógica incremental ON CONFLICT"
      contains: "CREATE OR REPLACE FUNCTION acumular_registro_diario"
  key_links:
    - from: "backend/src/services/meal.ts (plan 03-03)"
      to: "supabase rpc acumular_registro_diario"
      via: "supabase.rpc('acumular_registro_diario', {...})"
      pattern: "acumular_registro_diario"
---

<objective>
Criar a migration Supabase que define a RPC `acumular_registro_diario`, a única forma segura de acumular macros do dia sem sobrescrever.

Purpose: O pitfall crítico da Fase 3 é que `supabase.from('registros_diarios').upsert()` sobrescreve em vez de somar — o segundo almoço apagaria o café da manhã. Esta RPC usa `INSERT ... ON CONFLICT DO UPDATE SET kcal = registros_diarios.kcal_consumido + EXCLUDED.kcal_consumido` para acúmulo atômico correto.

Output: `supabase/migrations/20260425000001_add_acumular_registro.sql` com a função RPC pronta para ser chamada via `supabase.rpc('acumular_registro_diario', params)` nos planos 03-01 e 03-03.
</objective>

<execution_context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/PROJECT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/STATE.md

<interfaces>
<!-- Contratos existentes que esta migration suporta -->

Tabela alvo (de supabase/migrations/20260422000001_create_schema.sql):
```sql
CREATE TABLE IF NOT EXISTS registros_diarios (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  paciente_id UUID NOT NULL REFERENCES pacientes(id) ON DELETE CASCADE,
  data DATE NOT NULL,
  kcal_consumido NUMERIC(8,2) NOT NULL DEFAULT 0,
  proteina_g NUMERIC(8,2) NOT NULL DEFAULT 0,
  carbo_g NUMERIC(8,2) NOT NULL DEFAULT 0,
  gordura_g NUMERIC(8,2) NOT NULL DEFAULT 0,
  agua_ml INTEGER NOT NULL DEFAULT 0,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (paciente_id, data)
);
```

Chamada que os serviços farão (meal.ts no plano 03-03):
```typescript
await supabase.rpc('acumular_registro_diario', {
  p_paciente_id: pacienteId,
  p_data: hoje,           // 'YYYY-MM-DD'
  p_kcal: macros.kcal,
  p_proteina_g: macros.proteina_g,
  p_carbo_g: macros.carbo_g,
  p_gordura_g: macros.gordura_g,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration com RPC acumular_registro_diario</name>
  <read_first>
    - supabase/migrations/20260422000001_create_schema.sql — schema completo de registros_diarios e UNIQUE constraint
    - supabase/migrations/20260423000006_paciente_entrevista.sql — padrão de migration existente (estrutura, comentários)
  </read_first>
  <files>supabase/migrations/20260425000001_add_acumular_registro.sql</files>
  <action>
Criar a migration com a função RPC que usa INSERT ... ON CONFLICT DO UPDATE com soma incremental:

```sql
-- Migration 007: RPC acumular_registro_diario
-- Necessária para Fase 3 — evita o pitfall de UPSERT que sobrescreve em vez de acumular.
-- O client-side upsert do Supabase sobrescreve campos; esta RPC garante soma atômica.

CREATE OR REPLACE FUNCTION acumular_registro_diario(
  p_paciente_id UUID,
  p_data        DATE,
  p_kcal        NUMERIC,
  p_proteina_g  NUMERIC,
  p_carbo_g     NUMERIC,
  p_gordura_g   NUMERIC
)
RETURNS void
LANGUAGE sql
SECURITY DEFINER
AS $$
  INSERT INTO registros_diarios (paciente_id, data, kcal_consumido, proteina_g, carbo_g, gordura_g)
  VALUES (p_paciente_id, p_data, p_kcal, p_proteina_g, p_carbo_g, p_gordura_g)
  ON CONFLICT (paciente_id, data)
  DO UPDATE SET
    kcal_consumido = registros_diarios.kcal_consumido + EXCLUDED.kcal_consumido,
    proteina_g     = registros_diarios.proteina_g     + EXCLUDED.proteina_g,
    carbo_g        = registros_diarios.carbo_g        + EXCLUDED.carbo_g,
    gordura_g      = registros_diarios.gordura_g      + EXCLUDED.gordura_g,
    updated_at     = now();
$$;

-- Garantir que apenas usuários autenticados (service_role) possam chamar
REVOKE ALL ON FUNCTION acumular_registro_diario FROM PUBLIC;
GRANT EXECUTE ON FUNCTION acumular_registro_diario TO service_role;
```

Observações:
- `SECURITY DEFINER` é necessário pois o Supabase client usa `anon` ou `authenticated` role, e a tabela pode ter RLS. Com `service_role` (usado no backend), não é estritamente necessário, mas é boa prática.
- Os valores `p_kcal` etc. devem ser sanitizados no código TypeScript antes de chamar (verificar NaN/negativo).
- O campo `agua_ml` não é acumulado aqui — Fase 4 cuida de hidratação.
  </action>
  <verify>
    <automated>grep -n "acumular_registro_diario" "C:/Users/botel/OneDrive/Desktop/nutri_fit/supabase/migrations/20260425000001_add_acumular_registro.sql" | grep "CREATE OR REPLACE FUNCTION"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em supabase/migrations/20260425000001_add_acumular_registro.sql
    - Contém `CREATE OR REPLACE FUNCTION acumular_registro_diario(`
    - Contém os 6 parâmetros: `p_paciente_id UUID, p_data DATE, p_kcal NUMERIC, p_proteina_g NUMERIC, p_carbo_g NUMERIC, p_gordura_g NUMERIC`
    - Contém `ON CONFLICT (paciente_id, data)`
    - Contém `DO UPDATE SET` com `registros_diarios.kcal_consumido + EXCLUDED.kcal_consumido` (soma, não sobrescrita)
    - Contém `GRANT EXECUTE ON FUNCTION acumular_registro_diario TO service_role`
    - `grep "ON CONFLICT" supabase/migrations/20260425000001_add_acumular_registro.sql` retorna pelo menos 1 resultado
  </acceptance_criteria>
  <done>Migration criada com função RPC que acumula incrementalmente. Os planos 03-01 e 03-03 podem usar `supabase.rpc('acumular_registro_diario', {...})` com segurança.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → Supabase RPC | service_role key — já validado na Fase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | acumular_registro_diario | mitigate | Sanitizar p_kcal, p_proteina_g, p_carbo_g, p_gordura_g no TypeScript antes de chamar RPC: `Math.max(0, isNaN(v) ? 0 : v)` |
| T-03-04-02 | Tampering | registros_diarios acúmulo | accept | Overflow de NUMERIC(8,2) requer ~9999999.99 kcal em um dia — impossível em uso real |
</threat_model>

<verification>
Após criar a migration, aplicar no Supabase Cloud:

```bash
# Opção 1: Supabase CLI (se instalado)
cd C:/Users/botel/OneDrive/Desktop/nutri_fit && supabase db push

# Opção 2: Copiar SQL e executar no Supabase SQL Editor (dashboard.supabase.com)
# Projeto: iqpoohthfhmjkvrqxzns
```

Verificar que a função existe:
```sql
SELECT routine_name FROM information_schema.routines
WHERE routine_name = 'acumular_registro_diario';
```
</verification>

<success_criteria>
- Arquivo `supabase/migrations/20260425000001_add_acumular_registro.sql` existe e contém a função RPC com lógica de acúmulo incremental
- Chamadas repetidas à RPC com o mesmo `(paciente_id, data)` somam valores em vez de sobrescrever
- Os planos 03-01 e 03-03 podem referenciar `supabase.rpc('acumular_registro_diario', params)`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-registro-refeicoes-multimodal/03-04-SUMMARY.md`
</output>

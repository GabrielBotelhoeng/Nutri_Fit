---
phase: 03-registro-refeicoes-multimodal
plan: "02"
type: execute
wave: 3
depends_on:
  - "03-03"
  - "03-01"
files_modified:
  - backend/src/services/vision.ts
  - backend/src/services/barcode.ts
  - backend/src/routes/webhook.ts
autonomous: true
requirements:
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06

must_haves:
  truths:
    - "Paciente envia foto de prato e recebe pedido de 2ª foto (ângulo lateral) — D-04"
    - "Após 2ª foto, agente exibe alimentos identificados e pede confirmação antes de registrar — D-06"
    - "Se timeout de 5 min expirou mas foto1_base64 existe, processa a 1ª foto com aviso de limitação em vez de descartar — D-04"
    - "Paciente envia foto de código de barras e recebe saldo do dia após registro automático"
    - "Paciente envia foto de rótulo e recebe saldo do dia com valores lidos do rótulo"
    - "Claude Vision detecta automaticamente o tipo de foto sem o paciente digitar — D-03"
    - "Estado entre 1ª e 2ª foto é salvo em entrevista_dados com timeout de 5 minutos — D-05"
  artifacts:
    - path: "backend/src/services/vision.ts"
      provides: "downloadMedia + detectarTipoImagem + analisarPrato + lerRotulo + processarImagem"
      exports: ["processarImagem"]
      min_lines: 150
    - path: "backend/src/services/barcode.ts"
      provides: "extrairCodigoViaVision + buscarOpenFoodFacts + processarCodigoBarras"
      exports: ["processarCodigoBarras"]
      min_lines: 60
  key_links:
    - from: "backend/src/routes/webhook.ts case imageMessage"
      to: "backend/src/services/vision.ts processarImagem()"
      via: "await visionService.processarImagem(phone, messageId, caption)"
      pattern: "processarImagem"
    - from: "backend/src/services/vision.ts prato flow"
      to: "backend/src/services/meal.ts processarTextoRefeicao()"
      via: "await mealService.processarTextoRefeicao(phone, descricaoAlimentos, paciente)"
      pattern: "processarTextoRefeicao"
    - from: "backend/src/services/barcode.ts"
      to: "https://world.openfoodfacts.org/api/v0/product/{barcode}.json"
      via: "fetch(url, { headers: { 'User-Agent': 'NutriChat/1.0' } })"
      pattern: "openfoodfacts"
---

<objective>
Implementar análise de imagem via Claude Vision com detecção automática de tipo (prato/código de barras/rótulo), fluxo de 2 fotos para prato, lookup Open Food Facts para código de barras, e conectar ao webhook.

Purpose: Substituir o stub `imageMessage` em webhook.ts. O paciente envia qualquer foto e o bot detecta automaticamente o tipo, conduzindo o fluxo correto — confirmação antes de registrar para pratos (D-06), lookup de produto para código de barras, leitura direta para rótulos.

Output: `backend/src/services/vision.ts`, `backend/src/services/barcode.ts`, e `webhook.ts` com o case imageMessage completo.

Nota sobre agent.ts: A Task 2 deste plano adiciona a interceptação de `confirmacao_pendente` em agent.ts. O plano 03-03 (Wave 2) também modifica agent.ts para o fluxo de registro de texto — o ponto de extensão `confirmacao_pendente` deve ser inserido ANTES do bloco de detecção de intenção (`ehRegistro`/`ehSubstituicao`) para não criar conflito entre as edições.
</objective>

<execution_context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/PROJECT.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/STATE.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/03-registro-refeicoes-multimodal/03-03-SUMMARY.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/03-registro-refeicoes-multimodal/03-01-SUMMARY.md

<interfaces>
<!-- Contratos que vision.ts e barcode.ts consomem e expõem -->

De backend/src/routes/webhook.ts (payload imageMessage):
```typescript
// Dentro do case "imageMessage":
// phone = extractPhoneNumber(data.key.remoteJid)
// messageId = data.key.id
// caption = (data.message.imageMessage as { caption?: string })?.caption ?? ''
```

De backend/src/services/audio.ts (downloadMedia implementado em 03-01):
```typescript
// downloadMedia exportado de audio.ts — vision.ts deve importar e reutilizar
export async function downloadMedia(messageId: string): Promise<{ buffer: Buffer; mimetype: string }>
```

De backend/src/services/meal.ts (plano 03-03):
```typescript
export async function processarTextoRefeicao(phone: string, texto: string, paciente: PacienteInfo): Promise<void>
export async function registrarRefeicao(pacienteId: string, descricao: string, macros: MacrosRefeicao, tipoRegistro: 'texto' | 'audio' | 'foto' | 'codigo_barras' | 'rotulo'): Promise<void>
export async function obterSaldoDia(pacienteId: string): Promise<MacrosRefeicao>
export function formatarSaldoDia(descricao: string, kcalRegistrado: number, saldo: MacrosRefeicao, metas: MetasMacros): string
export interface MacrosRefeicao { kcal: number; proteina_g: number; carbo_g: number; gordura_g: number; }
```

De backend/src/services/conversation.ts:
```typescript
export interface PacienteInfo { id: string; nome: string; ... entrevista_dados: Record<string, unknown>; }
export async function buscarPacientePorWhatsapp(whatsapp: string): Promise<PacienteInfo | null>
export async function getEstado(pacienteId: string): Promise<EstadoEntrevista>
export async function atualizarEstado(pacienteId: string, update: Partial<{...}>): Promise<void>
```

Claude Vision content block (de RESEARCH.md Pattern 3 — verificado na documentação):
```typescript
claude.messages.create({
  model: 'claude-sonnet-4-6',
  max_tokens: 1024,
  messages: [{
    role: 'user',
    content: [
      {
        type: 'image',
        source: { type: 'base64', media_type: mimetype as 'image/jpeg' | 'image/png' | 'image/webp' | 'image/gif', data: base64 },
      },
      { type: 'text', text: prompt },
    ],
  }],
});
```

Estado entre 2 fotos (de RESEARCH.md Pattern 6):
```typescript
interface EstadoAguardandoFoto2 {
  aguardando_foto_2: {
    foto1_message_id: string;
    foto1_base64: string;
    timestamp: string;  // ISO 8601
  };
}
function estaNoTimeout(timestamp: string): boolean {
  return Date.now() - new Date(timestamp).getTime() <= 5 * 60 * 1000;
}
```

Open Food Facts (de RESEARCH.md Pattern 5 — verificado na documentação):
```typescript
// GET https://world.openfoodfacts.org/api/v0/product/{barcode}.json
// Headers: { 'User-Agent': 'NutriChat/1.0 (botelhog45@gmail.com)' }  // requerido por OFF
// Response: { status: 0|1, product: { product_name: string, nutriments: { 'energy-kcal_100g', proteins_100g, carbohydrates_100g, fat_100g } } }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar backend/src/services/barcode.ts e backend/src/services/vision.ts</name>
  <read_first>
    - backend/src/services/audio.ts — downloadMedia() exportado (Wave 3 — já existe); importar em vision.ts, não duplicar
    - backend/src/services/meal.ts — MacrosRefeicao interface, registrarRefeicao(), formatarSaldoDia(), obterSaldoDia()
    - backend/src/services/agent.ts — padrão de instanciação do Anthropic claude client
    - backend/src/services/conversation.ts — atualizarEstado(), getEstado(), PacienteInfo
    - .planning/phases/03-registro-refeicoes-multimodal/03-RESEARCH.md — Pattern 3 (Vision content block), Pattern 4 (prompts), Pattern 5 (OFF), Pattern 6 (estado 2 fotos), Pitfall 3 (JSON inválido), Pitfall 6 (API key header)
    - .planning/phases/03-registro-refeicoes-multimodal/03-CONTEXT.md — D-03, D-04, D-05, D-06
  </read_first>
  <files>backend/src/services/barcode.ts, backend/src/services/vision.ts</files>
  <action>
**Passo 1: Criar backend/src/services/barcode.ts**

```typescript
import Anthropic from '@anthropic-ai/sdk';
import { env } from '../config/env';

const claude = new Anthropic({ apiKey: env.CLAUDE_API_KEY });

export interface ProdutoBarcode {
  nome: string;
  macrosPor100g: {
    kcal: number;
    proteina_g: number;
    carbo_g: number;
    gordura_g: number;
  };
}

interface OffNutriments {
  'energy-kcal_100g'?: number;
  proteins_100g?: number;
  carbohydrates_100g?: number;
  fat_100g?: number;
}

interface OffResponse {
  status: 0 | 1;
  product?: {
    product_name?: string;
    nutriments?: OffNutriments;
  };
}

// Extrair número do barcode via Claude Vision (mais robusto que biblioteca para fotos comprimidas)
export async function extrairCodigoViaVision(base64: string, mimetype: string): Promise<string | null> {
  const mimeClean = mimetype.split(';')[0].trim() as 'image/jpeg' | 'image/png' | 'image/webp' | 'image/gif';
  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 128,
    messages: [{
      role: 'user',
      content: [
        { type: 'image', source: { type: 'base64', media_type: mimeClean, data: base64 } },
        { type: 'text', text: 'Leia o código de barras (EAN-13 ou similar) nesta imagem. Responda APENAS com o número do código, sem espaços ou traços. Se não conseguir ler, responda "null".' },
      ],
    }],
  });
  const texto = response.content[0].type === 'text' ? response.content[0].text.trim() : 'null';
  return texto === 'null' || texto === '' ? null : texto.replace(/\D/g, '');
}

// Buscar produto na Open Food Facts
export async function buscarOpenFoodFacts(barcode: string): Promise<ProdutoBarcode | null> {
  const url = `https://world.openfoodfacts.org/api/v0/product/${barcode}.json`;
  try {
    const response = await fetch(url, {
      headers: { 'User-Agent': 'NutriChat/1.0 (botelhog45@gmail.com)' },
    });
    if (!response.ok) return null;

    const data = await response.json() as OffResponse;
    if (data.status !== 1 || !data.product?.nutriments) return null;

    const n = data.product.nutriments;
    return {
      nome: data.product.product_name ?? `Produto ${barcode}`,
      macrosPor100g: {
        kcal:       n['energy-kcal_100g'] ?? 0,
        proteina_g: n.proteins_100g ?? 0,
        carbo_g:    n.carbohydrates_100g ?? 0,
        gordura_g:  n.fat_100g ?? 0,
      },
    };
  } catch (err) {
    console.error('[barcode] Erro ao buscar Open Food Facts:', err);
    return null;
  }
}

// Fluxo completo: extrair barcode da imagem + buscar nutrientes
// Fallback: se OFF não encontrar, retornar null (vision.ts tentará ler como rótulo)
export async function processarCodigoBarras(
  base64: string,
  mimetype: string,
): Promise<ProdutoBarcode | null> {
  const codigo = await extrairCodigoViaVision(base64, mimetype);
  if (!codigo) {
    console.log('[barcode] Não conseguiu extrair código de barras da imagem');
    return null;
  }

  console.log(`[barcode] Código extraído: ${codigo}`);
  const produto = await buscarOpenFoodFacts(codigo);

  if (!produto) {
    console.log(`[barcode] Produto ${codigo} não encontrado no OFF — fallback para leitura de rótulo`);
  }

  return produto;
}
```

**Passo 2: Criar backend/src/services/vision.ts**

Implementar `processarImagem` com o seguinte tratamento para o estado `aguardando_foto_2` (D-04):

- **Se aguardando E dentro do timeout**: é a 2ª foto — usar ambas as fotos para analisar o prato.
- **Se aguardando E timeout EXPIROU**: D-04 requer processar a 1ª foto com aviso de limitação de porção em vez de descartar. Recuperar `foto1_base64` do estado, chamar `analisarPrato([foto1_base64], mimetype)` com aviso, prosseguir para `handleConfirmacaoPrato`, limpar o estado. Depois tratar a nova imagem recebida como nova 1ª foto de um fluxo separado (salvar novo estado `aguardando_foto_2` para ela).
- **Sem estado aguardando**: 1ª foto — detectar tipo e iniciar fluxo correspondente.

```typescript
import Anthropic from '@anthropic-ai/sdk';
import { env } from '../config/env';
import { downloadMedia } from './audio';
import { sendText } from './evolution';
import { buscarPacientePorWhatsapp, getEstado, atualizarEstado } from './conversation';
import type { PacienteInfo } from './conversation';
import { processarCodigoBarras } from './barcode';
import { registrarRefeicao, obterSaldoDia, formatarSaldoDia, MacrosRefeicao } from './meal';

const claude = new Anthropic({ apiKey: env.CLAUDE_API_KEY });

// Helpers
function extrairJSON(texto: string): unknown {
  const limpo = texto.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim();
  return JSON.parse(limpo);
}

function estaNoTimeout(timestamp: string, minutos = 5): boolean {
  return Date.now() - new Date(timestamp).getTime() <= minutos * 60 * 1000;
}

// Calcular metas do paciente a partir de tdee_kcal (D-08)
function calcularMetasDoEstado(dados: Record<string, unknown>) {
  const tdee = (dados['tdee_kcal'] as number) || 2000;
  return {
    kcal: tdee,
    proteina_g: Math.round((tdee * 0.30) / 4),
    carbo_g:    Math.round((tdee * 0.40) / 4),
    gordura_g:  Math.round((tdee * 0.30) / 9),
  };
}

// Step 1: Detectar tipo de imagem (D-03)
async function detectarTipoImagem(
  base64: string,
  mimetype: string,
): Promise<'prato' | 'barcode' | 'rotulo'> {
  const mimeClean = mimetype.split(';')[0].trim() as 'image/jpeg' | 'image/png' | 'image/webp' | 'image/gif';
  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 16,
    messages: [{
      role: 'user',
      content: [
        { type: 'image', source: { type: 'base64', media_type: mimeClean, data: base64 } },
        { type: 'text', text: `Analise esta imagem e responda APENAS com uma das três opções:\n- "prato" — se for uma foto de comida, prato ou refeição\n- "barcode" — se for uma foto de código de barras (código de barras EAN/QR)\n- "rotulo" — se for uma foto de tabela nutricional ou rótulo de embalagem de alimento\n\nResponda com APENAS uma palavra. Não explique.` },
      ],
    }],
  });
  const tipo = response.content[0].type === 'text' ? response.content[0].text.trim().toLowerCase() : 'prato';
  if (tipo === 'barcode' || tipo === 'rotulo') return tipo;
  return 'prato'; // default seguro
}

// Analisar prato: retorna alimentos identificados e macros estimados
interface AnalisePrato {
  alimentos: string[];
  confianca: 'alta' | 'media' | 'baixa';
  macros: MacrosRefeicao;
  aviso: string | null;
}

async function analisarPrato(
  base64s: string[], // 1 ou 2 fotos
  mimetype: string,
): Promise<AnalisePrato> {
  const mimeClean = mimetype.split(';')[0].trim() as 'image/jpeg' | 'image/png' | 'image/webp' | 'image/gif';

  const imageBlocks = base64s.map((b64) => ({
    type: 'image' as const,
    source: { type: 'base64' as const, media_type: mimeClean, data: b64 },
  }));

  const promptAviso = base64s.length === 2
    ? '\n\nO paciente enviou 2 ângulos do prato — use ambas as fotos para melhor estimativa de porção.'
    : '\n\n⚠️ Apenas 1 foto disponível — estime porção conservadoramente e indique limitação no campo aviso.';

  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 512,
    messages: [{
      role: 'user',
      content: [
        ...imageBlocks,
        {
          type: 'text',
          text: `Você é um assistente nutricional. Analise esta foto de refeição.${promptAviso}

TAREFA: Identifique os alimentos visíveis e estime os macronutrientes.

RESPONDA EM JSON com exatamente este formato:
{"alimentos": ["alimento 1 com quantidade estimada", "alimento 2..."], "confianca": "alta", "kcal": 500, "proteina_g": 30, "carbo_g": 60, "gordura_g": 15, "aviso": null}

Valores de confianca: "alta" (alimentos claramente visíveis), "media" (parcialmente visível), "baixa" (difícil identificar).
Se a porção for incerta, estime conservadoramente e coloque aviso descritivo.
Responda APENAS com JSON válido, sem markdown.`,
        },
      ],
    }],
  });

  const texto = response.content[0].type === 'text' ? response.content[0].text : '{}';
  try {
    const raw = extrairJSON(texto) as Record<string, unknown>;
    return {
      alimentos: (raw['alimentos'] as string[]) || [],
      confianca: (raw['confianca'] as 'alta' | 'media' | 'baixa') || 'baixa',
      macros: {
        kcal:       Number(raw['kcal'])       || 0,
        proteina_g: Number(raw['proteina_g']) || 0,
        carbo_g:    Number(raw['carbo_g'])    || 0,
        gordura_g:  Number(raw['gordura_g'])  || 0,
      },
      aviso: (raw['aviso'] as string | null) || null,
    };
  } catch {
    console.error('[vision] Falha ao parsear análise de prato:', texto);
    return { alimentos: [], confianca: 'baixa', macros: { kcal: 0, proteina_g: 0, carbo_g: 0, gordura_g: 0 }, aviso: 'Não consegui analisar o prato' };
  }
}

// Ler rótulo nutricional
interface DadosRotulo {
  produto: string;
  porcao_g: number;
  macros: MacrosRefeicao;
}

async function lerRotulo(base64: string, mimetype: string): Promise<DadosRotulo | null> {
  const mimeClean = mimetype.split(';')[0].trim() as 'image/jpeg' | 'image/png' | 'image/webp' | 'image/gif';
  const response = await claude.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 512,
    messages: [{
      role: 'user',
      content: [
        { type: 'image', source: { type: 'base64', media_type: mimeClean, data: base64 } },
        { type: 'text', text: `Você é um assistente nutricional. Leia esta tabela nutricional de rótulo.

RESPONDA EM JSON com exatamente este formato:
{"produto": "nome do produto se visível", "porcao_g": 100, "kcal_porcao": 200, "proteina_g": 10, "carbo_g": 30, "gordura_g": 5}

Se algum campo não estiver visível, use null. Responda APENAS com JSON válido.` },
      ],
    }],
  });
  const texto = response.content[0].type === 'text' ? response.content[0].text : '{}';
  try {
    const raw = extrairJSON(texto) as Record<string, unknown>;
    return {
      produto: (raw['produto'] as string) || 'Produto desconhecido',
      porcao_g: Number(raw['porcao_g']) || 100,
      macros: {
        kcal:       Number(raw['kcal_porcao']) || 0,
        proteina_g: Number(raw['proteina_g'])  || 0,
        carbo_g:    Number(raw['carbo_g'])     || 0,
        gordura_g:  Number(raw['gordura_g'])   || 0,
      },
    };
  } catch {
    console.error('[vision] Falha ao parsear rótulo:', texto);
    return null;
  }
}

// Ponto de entrada principal — chamado pelo webhook.ts
export async function processarImagem(
  phone: string,
  messageId: string,
  caption?: string,
): Promise<void> {
  const paciente = await buscarPacientePorWhatsapp(phone);
  if (!paciente) return;

  try {
    const { buffer, mimetype } = await downloadMedia(messageId);
    const base64 = buffer.toString('base64');
    const estado = await getEstado(paciente.id);
    const dadosEstado = estado.dados as Record<string, unknown>;
    const metas = calcularMetasDoEstado(dadosEstado);

    // Verificar estado aguardando_foto_2 (D-04, D-05)
    const aguardando = dadosEstado['aguardando_foto_2'] as {
      foto1_message_id: string;
      foto1_base64: string;
      timestamp: string;
    } | undefined;

    if (aguardando) {
      if (estaNoTimeout(aguardando.timestamp)) {
        // É a 2ª foto dentro do prazo — processar com ambas
        await atualizarEstado(paciente.id, { dados: { aguardando_foto_2: null } });
        const analise = await analisarPrato([aguardando.foto1_base64, base64], mimetype);
        await handleConfirmacaoPrato(phone, paciente, analise, metas);
        return;
      } else {
        // Timeout expirou — D-04: processar a 1ª foto com aviso de limitação de porção
        // em vez de descartar silenciosamente. Depois tratar a nova imagem como nova 1ª foto.
        console.log(`[vision] Timeout foto2 expirou para ${phone} — processando foto1 com aviso`);
        await atualizarEstado(paciente.id, { dados: { aguardando_foto_2: null } });
        const analiseFoto1 = await analisarPrato([aguardando.foto1_base64], mimetype);
        await handleConfirmacaoPrato(
          phone,
          paciente,
          analiseFoto1,
          metas,
          '⚠️ Estimativa baseada em apenas 1 foto (tempo para 2ª foto expirou) — pode variar até 30%.',
        );
        // Tratar a nova imagem recebida como nova 1ª foto de um fluxo separado
        await atualizarEstado(paciente.id, {
          dados: {
            aguardando_foto_2: {
              foto1_message_id: messageId,
              foto1_base64: base64,
              timestamp: new Date().toISOString(),
            },
          },
        });
        await sendText(phone, '📸 Recebi outra foto! Para uma estimativa de porção melhor, mande também uma foto lateral do prato.');
        return;
      }
    }

    // 1ª foto — detectar tipo
    const tipo = await detectarTipoImagem(base64, mimetype);
    console.log(`[vision] Tipo detectado: ${tipo} para ${phone}`);

    if (tipo === 'barcode') {
      // Fluxo código de barras (AGENT-05)
      const produto = await processarCodigoBarras(base64, mimetype);
      if (produto) {
        // Usar nutrientes por 100g como estimativa de uma porção típica
        const descricao = `${produto.nome} (~100g)`;
        await registrarRefeicao(paciente.id, descricao, produto.macrosPor100g, 'codigo_barras');
        const saldo = await obterSaldoDia(paciente.id);
        await sendText(phone, formatarSaldoDia(descricao, produto.macrosPor100g.kcal, saldo, metas));
      } else {
        // Fallback: tentar ler como rótulo
        const rotulo = await lerRotulo(base64, mimetype);
        if (rotulo) {
          await registrarRefeicao(paciente.id, rotulo.produto, rotulo.macros, 'rotulo');
          const saldo = await obterSaldoDia(paciente.id);
          await sendText(phone, formatarSaldoDia(rotulo.produto, rotulo.macros.kcal, saldo, metas));
        } else {
          await sendText(phone, '❌ Não consegui ler o código de barras ou rótulo. Tente uma foto mais nítida ou descreva a refeição por texto.');
        }
      }
      return;
    }

    if (tipo === 'rotulo') {
      // Fluxo rótulo nutricional (AGENT-06)
      const rotulo = await lerRotulo(base64, mimetype);
      if (!rotulo) {
        await sendText(phone, '❌ Não consegui ler os valores do rótulo. Tente uma foto mais nítida.');
        return;
      }
      await registrarRefeicao(paciente.id, rotulo.produto, rotulo.macros, 'rotulo');
      const saldo = await obterSaldoDia(paciente.id);
      await sendText(phone, formatarSaldoDia(rotulo.produto, rotulo.macros.kcal, saldo, metas));
      return;
    }

    // tipo === 'prato' (AGENT-03, AGENT-04)
    // Salvar foto1 e solicitar 2ª foto (D-04)
    await atualizarEstado(paciente.id, {
      dados: {
        aguardando_foto_2: {
          foto1_message_id: messageId,
          foto1_base64: base64,
          timestamp: new Date().toISOString(),
        },
      },
    });
    await sendText(phone, '📸 Para uma estimativa de porção melhor, mande também uma foto lateral do prato.');

  } catch (err) {
    console.error('[vision] Erro ao processar imagem:', err);
    await sendText(phone, '❌ Erro ao analisar a imagem. Tente novamente ou descreva a refeição por texto.');
  }
}

// Fluxo de confirmação antes de registrar (D-06)
// Salva a análise em entrevista_dados e envia lista de alimentos para confirmação
// A interceptação de "sim"/"não" é feita em agent.ts (plano 03-02 Task 2)
// — o bloco confirmacao_pendente em agent.ts deve vir ANTES do bloco ehRegistro/ehSubstituicao
async function handleConfirmacaoPrato(
  phone: string,
  paciente: PacienteInfo,
  analise: AnalisePrato,
  metas: ReturnType<typeof calcularMetasDoEstado>,
  avisoExtra?: string,
): Promise<void> {
  if (analise.macros.kcal === 0 || analise.alimentos.length === 0) {
    await sendText(phone, '❌ Não consegui identificar os alimentos. Tente uma foto com mais luz ou descreva por texto.');
    return;
  }

  // Salvar análise pendente de confirmação
  await atualizarEstado(paciente.id, {
    dados: {
      confirmacao_pendente: {
        analise,
        timestamp: new Date().toISOString(),
      },
    },
  });

  const lista = analise.alimentos.map((a, i) => `${i + 1}. ${a}`).join('\n');
  const avisoConfianca = analise.confianca === 'baixa' ? '\n⚠️ Baixa confiança na identificação.' : '';
  const avisoLimitacao = avisoExtra ? `\n${avisoExtra}` : '';

  await sendText(
    phone,
    `📸 Identifiquei na foto:\n${lista}\n\nEst. ${Math.round(analise.macros.kcal)} kcal${avisoConfianca}${avisoLimitacao}\n\nEstá correto? Responda *sim* para registrar ou *não* para cancelar.`,
  );
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend" && npm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/services/vision.ts` existe
    - Contém `export async function processarImagem(`
    - Contém `detectarTipoImagem` com prompt "prato" | "barcode" | "rotulo" (D-03)
    - Contém `aguardando_foto_2` para estado entre 2 fotos (D-04, D-05)
    - No bloco de timeout expirado: recupera `foto1_base64`, chama `analisarPrato([aguardando.foto1_base64], mimetype)` com aviso de limitação, prossegue para `handleConfirmacaoPrato`, depois trata nova imagem como nova 1ª foto (D-04)
    - Contém `handleConfirmacaoPrato` com `confirmacao_pendente` em entrevista_dados (D-06)
    - Contém `estaNoTimeout` com limite de 5 minutos (D-05)
    - `backend/src/services/barcode.ts` existe
    - Contém `export async function processarCodigoBarras(`
    - Contém `buscarOpenFoodFacts` com URL `openfoodfacts.org/api/v0/product/`
    - Contém `User-Agent: 'NutriChat/1.0'` no header da chamada OFF
    - `npm run typecheck` passa sem erros
  </acceptance_criteria>
  <done>vision.ts e barcode.ts criados com todos os fluxos de análise de imagem, detecção automática de tipo, estado entre 2 fotos e fallback correto para timeout expirado (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Substituir stub imageMessage e integrar confirmação D-06 em agent.ts e webhook.ts</name>
  <read_first>
    - backend/src/routes/webhook.ts — stub atual do case imageMessage (linha ~56-59) e padrão dos outros cases
    - backend/src/services/agent.ts — bloco status completa; localizar onde 03-03 adicionou o bloco ehRegistro/ehSubstituicao para inserir confirmacao_pendente ANTES dele
    - backend/src/services/vision.ts — processarImagem() criado na Task 1; campo confirmacao_pendente
    - backend/src/services/meal.ts — registrarRefeicao(), obterSaldoDia(), formatarSaldoDia() (já importados em agent.ts pelo plano 03-03)
  </read_first>
  <files>backend/src/routes/webhook.ts, backend/src/services/agent.ts</files>
  <action>
**Passo 1: Substituir stub imageMessage em webhook.ts**

Adicionar import:
```typescript
import * as visionService from '../services/vision';
```

Substituir o case `imageMessage`:
```typescript
// Antes (stub):
case 'imageMessage': {
  await sendText(phone, '📸 Recebi sua imagem! Analise disponivel em breve...');
  break;
}

// Depois:
case 'imageMessage': {
  const messageId = data.key.id;
  const caption = (data.message.imageMessage as { caption?: string })?.caption ?? '';
  await visionService.processarImagem(phone, messageId, caption);
  break;
}
```

**Passo 2: Interceptar confirmação "sim"/"não" em agent.ts**

IMPORTANTE: este bloco deve ser inserido ANTES do bloco de detecção de intenção (`ehRegistro`/`ehSubstituicao`) adicionado pelo plano 03-03. Isso preserva o ponto de extensão sem conflito entre as duas edições de agent.ts.

No bloco `status === 'completa'`, ANTES das verificações de `ehRegistro`/`ehSubstituicao`, adicionar:

```typescript
// Verificar confirmação pendente de análise de foto (D-06)
// INSERIR ANTES do bloco ehRegistro/ehSubstituicao (adicionado em 03-03)
const confirmacaoPendente = dadosEstado['confirmacao_pendente'] as {
  analise: {
    alimentos: string[];
    macros: { kcal: number; proteina_g: number; carbo_g: number; gordura_g: number };
    confianca: string;
    aviso: string | null;
  };
  timestamp: string;
} | undefined;

if (confirmacaoPendente) {
  // Timeout de confirmação: 10 minutos
  const confirmacaoExpirada = Date.now() - new Date(confirmacaoPendente.timestamp).getTime() > 10 * 60 * 1000;
  if (confirmacaoExpirada) {
    await atualizarEstado(paciente.id, { dados: { confirmacao_pendente: null } });
    // Não retornar — continuar para processar a mensagem normalmente
  } else {
    const textoLower = texto.toLowerCase().trim();
    if (textoLower === 'sim' || textoLower === 's' || textoLower === 'yes') {
      // Registrar
      await atualizarEstado(paciente.id, { dados: { confirmacao_pendente: null } });
      const macros = confirmacaoPendente.analise.macros;
      const descricao = confirmacaoPendente.analise.alimentos.join(', ');
      await mealService.registrarRefeicao(paciente.id, descricao, macros, 'foto');
      const estadoAtual = await getEstado(paciente.id);
      const dadosAtual = estadoAtual.dados as Record<string, unknown>;
      const tdee = (dadosAtual['tdee_kcal'] as number) || 2000;
      const metas = {
        kcal: tdee,
        proteina_g: Math.round((tdee * 0.30) / 4),
        carbo_g:    Math.round((tdee * 0.40) / 4),
        gordura_g:  Math.round((tdee * 0.30) / 9),
      };
      const saldo = await mealService.obterSaldoDia(paciente.id);
      await sendText(phone, mealService.formatarSaldoDia(descricao, macros.kcal, saldo, metas));
      return;
    } else if (textoLower === 'não' || textoLower === 'nao' || textoLower === 'n' || textoLower === 'no') {
      await atualizarEstado(paciente.id, { dados: { confirmacao_pendente: null } });
      await sendText(phone, '❌ Registro cancelado. Você pode tirar outra foto ou descrever a refeição por texto.');
      return;
    }
    // Não é sim/não — ignorar a confirmação pendente e processar a mensagem normalmente
  }
}
// Continua para bloco ehRegistro/ehSubstituicao (adicionado em 03-03)...
```

Adicionar `obterSaldoDia` e `formatarSaldoDia` às importações de mealService se ainda não estiverem presentes (já importado pelo plano 03-03).

Após editar os dois arquivos, reiniciar o container:
```bash
docker restart nutrichat_backend
```
  </action>
  <verify>
    <automated>cd "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend" && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `webhook.ts` contém `import * as visionService from '../services/vision'`
    - `webhook.ts` case `imageMessage` contém `visionService.processarImagem(phone, messageId, caption)`
    - `webhook.ts` NÃO contém `Analise disponivel em breve` (stub removido)
    - `agent.ts` contém `confirmacao_pendente` — lógica de interceptar sim/não inserida ANTES do bloco ehRegistro/ehSubstituicao
    - `agent.ts` contém `mealService.registrarRefeicao(paciente.id, descricao, macros, 'foto')` no handler de confirmação
    - `agent.ts` bloco ehRegistro/ehSubstituicao (de 03-03) permanece intacto após a inserção
    - `npm run typecheck` passa sem erros
    - `docker ps` mostra `nutrichat_backend` Up
  </acceptance_criteria>
  <done>Stub imageMessage substituído. Fluxo completo: detecção automática de tipo → 1ª foto prato → solicita 2ª → confirmação → registro. Timeout expirado processa foto1 com aviso. Barcode e rótulo registram diretamente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp → Evolution API → vision.ts | Dados de imagem de origem desconhecida |
| vision.ts → Claude API | Base64 de imagem enviada ao Claude Vision |
| barcode.ts → Open Food Facts | Código de barras enviado à API pública |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | macros retornados por analisarPrato | mitigate | Mesma sanitizarMacros() de meal.ts: `Math.max(0, isNaN(v) ? 0 : v)` antes de registrarRefeicao |
| T-03-02-02 | Tampering | confirmacao_pendente em JSONB corrompida | mitigate | Timeout de 10 min + limpeza automática; campos tipados antes de usar |
| T-03-02-03 | Information Disclosure | imagem enviada ao Claude Vision | accept | Mesma categoria de risco já aceita para texto; Claude rejeita CSAM automaticamente |
| T-03-02-04 | Tampering | barcode arbitrário → busca OFF | accept | OFF é API pública — nenhum dado sensível exposto; produto não encontrado → fallback para rótulo |
| T-03-02-05 | Denial of Service | foto1_base64 muito grande em JSONB | mitigate | Fotos JPEG WhatsApp típicas: 100-400 KB → base64 ~130-530 KB; PostgreSQL suporta sem problema. Se falhar: armazenar apenas messageId e re-baixar. |
</threat_model>

<verification>
Verificação funcional (requer WhatsApp real):

**Fluxo prato com 2ª foto:**
1. Enviar foto de prato de comida para `5562992058735`
2. Deve receber: `📸 Para uma estimativa de porção melhor, mande também uma foto lateral do prato.`
3. Enviar 2ª foto lateral
4. Deve receber lista de alimentos identificados + pedido de confirmação (D-06)
5. Responder "sim"
6. Deve receber saldo do dia no formato D-07

**Fluxo prato com timeout expirado (D-04):**
1. Enviar foto de prato
2. Aguardar mais de 5 minutos sem enviar a 2ª foto
3. Enviar qualquer outra foto
4. Deve receber confirmação da foto1 com aviso de limitação E solicitação de 2ª foto para a nova imagem

**Fluxo código de barras:**
1. Enviar foto de código de barras de produto alimentício
2. Deve receber saldo do dia com nome do produto

**Fluxo rótulo:**
1. Enviar foto de tabela nutricional
2. Deve receber saldo do dia com valores lidos

Verificação técnica:
```bash
cd C:/Users/botel/OneDrive/Desktop/nutri_fit/backend && npm run typecheck
docker logs nutrichat_backend --tail 30
```
</verification>

<success_criteria>
- vision.ts criado com detecção automática de tipo (D-03), fluxo 2 fotos (D-04/D-05), fallback correto para timeout expirado (D-04), confirmação antes de registrar (D-06)
- barcode.ts criado com extração via Claude Vision + lookup Open Food Facts (AGENT-05) + fallback para rótulo
- webhook.ts stub imageMessage substituído por visionService.processarImagem()
- agent.ts intercepta "sim"/"não" para confirmar/cancelar registro de foto (D-06), bloco inserido antes de ehRegistro/ehSubstituicao
- typecheck passa sem erros
- Container reiniciado após mudanças
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-registro-refeicoes-multimodal/03-02-SUMMARY.md`
</output>

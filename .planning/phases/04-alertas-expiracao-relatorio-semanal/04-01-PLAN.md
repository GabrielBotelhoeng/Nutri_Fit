---
phase: 04-alertas-expiracao-relatorio-semanal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/workflows/nutrichat-cron-expiracao.json
  - docker-compose.local.yml
  - backend/src/services/expiracao.ts
  - backend/src/routes/expiracao.ts
  - backend/src/routes/api.ts
  - backend/src/services/agent.ts
autonomous: true
requirements:
  - AGENT-18
  - AGENT-19

must_haves:
  truths:
    - "POST /api/expiracao/verificar retorna 202 com header x-internal-key correto"
    - "Pacientes com data_expiracao = hoje sao marcados ativo=false no Supabase"
    - "Pacientes com data_expiracao = hoje+1, hoje+2 ou hoje+3 recebem mensagem de aviso no WhatsApp"
    - "Ao processar mensagem de paciente com plano vencendo em <= 3 dias, agent.ts envia aviso inline sem bloquear"
    - "Workflow N8N usa x-internal-key (nao x-internal-token) e valor hardcoded do token"
  artifacts:
    - path: "backend/src/services/expiracao.ts"
      provides: "verificarExpiracoes() — query pacientes + sendText"
      exports: ["verificarExpiracoes"]
    - path: "backend/src/routes/expiracao.ts"
      provides: "POST /verificar com requireInternalKey"
      exports: ["expiracaoRouter"]
    - path: "n8n/workflows/nutrichat-cron-expiracao.json"
      provides: "Cron 09h corrigido com header x-internal-key"
      contains: "x-internal-key"
  key_links:
    - from: "n8n/workflows/nutrichat-cron-expiracao.json"
      to: "backend POST /api/expiracao/verificar"
      via: "HTTP Request node com header x-internal-key"
      pattern: "x-internal-key"
    - from: "backend/src/routes/expiracao.ts"
      to: "backend/src/services/expiracao.ts"
      via: "import verificarExpiracoes"
      pattern: "verificarExpiracoes"
    - from: "backend/src/routes/api.ts"
      to: "backend/src/routes/expiracao.ts"
      via: "apiRouter.use('/expiracao', expiracaoRouter)"
      pattern: "expiracaoRouter"
---

<objective>
Implementar o endpoint de verificacao de expiracao de planos e corrigir o workflow N8N existente que ja dispara diariamente mas falha por apontar para endpoint inexistente com header errado.

Purpose: Bloquear automaticamente pacientes com plano vencido (AGENT-19) e avisar proativamente 3 dias antes do vencimento via cron (AGENT-18 proativo) e inline no agente ao receber mensagem (AGENT-18 reativo).

Output: backend/src/services/expiracao.ts, backend/src/routes/expiracao.ts, correcao de nutrichat-cron-expiracao.json, correcao do docker-compose.local.yml, adicao do aviso inline em agent.ts.
</objective>

<execution_context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/ROADMAP.md
@C:/Users/botel/OneDrive/Desktop/nutri_fit/CLAUDE.md

<interfaces>
<!-- Contratos relevantes extraidos do codebase. O executor usa diretamente — sem explorar. -->

De backend/src/routes/boas-vindas.ts (padrao de endpoint interno a replicar):
```typescript
import { Router, Request, Response, NextFunction } from 'express';
import { env } from '../config/env';

export const boasVindasRouter = Router();

function requireInternalKey(req: Request, res: Response, next: NextFunction): void {
  if (req.headers['x-internal-key'] !== env.INTERNAL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}

// Padrao: responder 202 imediatamente, processar async
boasVindasRouter.post('/:pacienteId', requireInternalKey, async (req: Request, res: Response) => {
  res.status(202).json({ status: 'enviando', paciente_id: pacienteId });
  enviarBoasVindas(pacienteId).catch((err) => {
    console.error(`[boas-vindas] Falha para paciente ${pacienteId}:`, err);
  });
});
```

De backend/src/routes/api.ts (onde registrar a nova rota):
```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
apiRouter.use('/boas-vindas', boasVindasRouter);
// Proximas rotas adicionadas por planos seguintes:
// apiRouter.use('/expiracao', expiracaoRouter); // Fase 4
```

De backend/src/services/agent.ts (onde inserir o aviso inline — linha 124):
```typescript
// Trecho atual (linhas 124-127):
if (!paciente.ativo) {
  await sendText(phone, '⏰ Seu plano expirou. Entre em contato com seu nutricionista para renovar.');
  return;
}
// [INSERIR AQUI o aviso de 3 dias — antes do bloco acima, APOS buscar paciente]
```

De backend/src/config/env.ts — variavel disponivel:
```
env.INTERNAL_API_KEY  // valor: nutrichat_internal_dev_key_2026 (via docker-compose)
env.SUPABASE_URL
env.SUPABASE_SERVICE_KEY
```

De backend/src/services/evolution.ts — funcao disponivel:
```typescript
export async function sendText(phone: string, text: string): Promise<void>
// instance hardcoded: 'nutrichat'
```

De n8n/workflows/nutrichat-cron-expiracao.json (estado ATUAL — com bug):
```json
// header atual (ERRADO): { "name": "x-internal-token", "value": "={{ $env.N8N_INTERNAL_TOKEN }}" }
// header correto:        { "name": "x-internal-key",   "value": "nutrichat_internal_dev_key_2026" }
```

Schema tabela pacientes (migration 001):
```sql
pacientes(
  id UUID,
  nome TEXT,
  whatsapp TEXT,
  data_expiracao DATE,
  ativo BOOLEAN DEFAULT true
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir workflow N8N e docker-compose (header x-internal-key)</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/n8n/workflows/nutrichat-cron-expiracao.json
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/docker-compose.local.yml
  </read_first>
  <files>
    n8n/workflows/nutrichat-cron-expiracao.json
    docker-compose.local.yml
  </files>
  <action>
**nutrichat-cron-expiracao.json** — Corrigir o node "Chamar Verificacao de Expiracao":
- Mudar o `name` do header de `x-internal-token` para `x-internal-key`
- Mudar o `value` de `={{ $env.N8N_INTERNAL_TOKEN }}` para `nutrichat_internal_dev_key_2026` (hardcoded para dev local — D-04)
- Adicionar campo `method: "POST"` explicitamente nos parameters do HTTP Request node

O JSON do node corrigido deve ficar:
```json
{
  "parameters": {
    "method": "POST",
    "url": "=http://backend:3001/api/expiracao/verificar",
    "sendHeaders": true,
    "headerParameters": {
      "parameters": [
        {
          "name": "x-internal-key",
          "value": "nutrichat_internal_dev_key_2026"
        }
      ]
    },
    "options": {}
  },
  "id": "verificar-expiracao",
  "name": "Chamar Verificacao de Expiracao",
  "type": "n8n-nodes-base.httpRequest",
  "typeVersion": 4,
  "position": [400, 300]
}
```

**docker-compose.local.yml** — Adicionar `N8N_INTERNAL_TOKEN` no bloco `environment:` do servico `n8n` (logo apos `GENERIC_TIMEZONE`):
```yaml
N8N_INTERNAL_TOKEN: ${INTERNAL_API_KEY:-nutrichat_internal_dev_key_2026}
```
Isso garante que workflows futuros que usem `$env.N8N_INTERNAL_TOKEN` funcionem sem hardcode.
  </action>
  <verify>
    <automated>grep -n "x-internal-key" "C:/Users/botel/OneDrive/Desktop/nutri_fit/n8n/workflows/nutrichat-cron-expiracao.json" && grep -n "N8N_INTERNAL_TOKEN" "C:/Users/botel/OneDrive/Desktop/nutri_fit/docker-compose.local.yml"</automated>
  </verify>
  <acceptance_criteria>
    - grep "x-internal-key" n8n/workflows/nutrichat-cron-expiracao.json retorna resultado (nao vazio)
    - grep "x-internal-token" n8n/workflows/nutrichat-cron-expiracao.json retorna vazio (string antiga removida)
    - grep "N8N_INTERNAL_TOKEN" docker-compose.local.yml retorna resultado
    - grep "nutrichat_internal_dev_key_2026" n8n/workflows/nutrichat-cron-expiracao.json retorna resultado (valor hardcoded presente)
  </acceptance_criteria>
  <done>Workflow N8N usa header correto x-internal-key com valor hardcoded; docker-compose tem N8N_INTERNAL_TOKEN definido</done>
</task>

<task type="auto">
  <name>Task 2: Criar expiracao.ts (service + route) e registrar em api.ts</name>
  <read_first>
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/boas-vindas.ts
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts
    - C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts
  </read_first>
  <files>
    backend/src/services/expiracao.ts
    backend/src/routes/expiracao.ts
    backend/src/routes/api.ts
    backend/src/services/agent.ts
  </files>
  <action>
**backend/src/services/expiracao.ts** — Criar com a funcao `verificarExpiracoes()`:

```typescript
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';
import { sendText } from './evolution';

const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);

export async function verificarExpiracoes(): Promise<void> {
  const hoje = new Date().toISOString().slice(0, 10); // UTC — consistente com Fases 2/3

  // 1. Bloquear pacientes cujo plano VENCEU hoje ou antes (AGENT-19)
  const { data: vencidos, error: errVencidos } = await supabase
    .from('pacientes')
    .select('id, nome, whatsapp')
    .lte('data_expiracao', hoje)
    .eq('ativo', true);

  if (errVencidos) {
    console.error('[expiracao] Erro ao buscar vencidos:', errVencidos.message);
  } else if (vencidos && vencidos.length > 0) {
    for (const p of vencidos) {
      await supabase.from('pacientes').update({ ativo: false }).eq('id', p.id);
      await sendText(
        p.whatsapp as string,
        `⏰ Seu plano NutriChat venceu hoje. Para continuar contando com o acompanhamento nutricional, entre em contato com seu nutricionista para renovar. 💚`,
      );
      console.log(`[expiracao] Paciente ${p.nome} bloqueado (plano vencido)`);
    }
  }

  // 2. Avisar pacientes que vencem em exatamente 1, 2 ou 3 dias (AGENT-18 proativo via cron)
  const em3Dias = new Date();
  em3Dias.setDate(em3Dias.getDate() + 3);
  const dataLimite = em3Dias.toISOString().slice(0, 10);

  const amanha = new Date();
  amanha.setDate(amanha.getDate() + 1);
  const dataAmanha = amanha.toISOString().slice(0, 10);

  const { data: expirando, error: errExpirando } = await supabase
    .from('pacientes')
    .select('id, nome, whatsapp, data_expiracao')
    .gte('data_expiracao', dataAmanha)   // nao vencido ainda
    .lte('data_expiracao', dataLimite)   // vence em ate 3 dias
    .eq('ativo', true);

  if (errExpirando) {
    console.error('[expiracao] Erro ao buscar expirando:', errExpirando.message);
  } else if (expirando && expirando.length > 0) {
    for (const p of expirando) {
      const dataExp = new Date(p.data_expiracao as string);
      const diffDias = Math.ceil((dataExp.getTime() - Date.now()) / (1000 * 60 * 60 * 24));
      const diasStr = diffDias === 1 ? '1 dia' : `${diffDias} dias`;
      await sendText(
        p.whatsapp as string,
        `⚠️ Atenção, ${p.nome}! Seu plano NutriChat vence em *${diasStr}*.\n\nPara continuar acompanhando sua dieta, renove com seu nutricionista. 💚`,
      );
      console.log(`[expiracao] Aviso enviado para ${p.nome} — vence em ${diasStr}`);
    }
  }

  console.log('[expiracao] Verificacao concluida');
}
```

**backend/src/routes/expiracao.ts** — Criar seguindo o padrao de boas-vindas.ts:

```typescript
import { Router, Request, Response, NextFunction } from 'express';
import { env } from '../config/env';
import { verificarExpiracoes } from '../services/expiracao';

export const expiracaoRouter = Router();

function requireInternalKey(req: Request, res: Response, next: NextFunction): void {
  if (req.headers['x-internal-key'] !== env.INTERNAL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}

expiracaoRouter.post('/verificar', requireInternalKey, async (_req: Request, res: Response) => {
  res.status(202).json({ status: 'processando' });
  verificarExpiracoes().catch((err) => {
    console.error('[expiracao] Erro na verificacao:', err);
  });
});
```

**backend/src/routes/api.ts** — Adicionar import e registro da rota (remover o comentario placeholder e adicionar a linha real):

```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';
import { expiracaoRouter } from './expiracao';

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
apiRouter.use('/boas-vindas', boasVindasRouter);
apiRouter.use('/expiracao', expiracaoRouter);
```

**backend/src/services/agent.ts** — Inserir aviso inline ANTES do bloco `if (!paciente.ativo)` (linha ~124). O bloco a inserir:

```typescript
  // AGENT-18: Aviso reativo de expiracao proxima (3 dias) — nao bloqueia, so avisa
  if (paciente.data_expiracao && paciente.ativo) {
    const dataExp = new Date(paciente.data_expiracao as string);
    const diasParaVencer = Math.ceil((dataExp.getTime() - Date.now()) / (1000 * 60 * 60 * 24));
    if (diasParaVencer > 0 && diasParaVencer <= 3) {
      const diasStr = diasParaVencer === 1 ? '1 dia' : `${diasParaVencer} dias`;
      await sendText(phone, `⚠️ Lembrete: seu plano NutriChat vence em *${diasStr}*. Renove com seu nutricionista para nao perder o acompanhamento. 💚`);
      // Nao retorna — continua processando a mensagem normalmente
    }
  }
```

ATENCAO: Inserir ANTES da linha `if (!paciente.ativo) {`. A ordem correta no arquivo fica:
1. `const paciente = await buscarPacientePorWhatsapp(phone);`
2. `if (!paciente) { ... return; }`
3. [NOVO] aviso de 3 dias
4. `if (!paciente.ativo) { ... return; }`

Apos editar qualquer arquivo TypeScript do backend, executar: `docker restart nutrichat_backend`
  </action>
  <verify>
    <automated>
      grep -n "expiracaoRouter" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/routes/api.ts" &&
      grep -n "verificarExpiracoes" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/expiracao.ts" &&
      grep -n "diasParaVencer" "C:/Users/botel/OneDrive/Desktop/nutri_fit/backend/src/services/agent.ts" &&
      curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3001/api/expiracao/verificar -H "x-internal-key: nutrichat_internal_dev_key_2026"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "expiracaoRouter" backend/src/routes/api.ts retorna resultado (rota registrada)
    - grep "apiRouter.use('/expiracao'" backend/src/routes/api.ts retorna resultado
    - backend/src/services/expiracao.ts existe e tem "verificarExpiracoes"
    - grep "diasParaVencer" backend/src/services/agent.ts retorna resultado (aviso inline presente)
    - curl -X POST http://localhost:3001/api/expiracao/verificar -H "x-internal-key: nutrichat_internal_dev_key_2026" retorna HTTP 202
    - curl -X POST http://localhost:3001/api/expiracao/verificar sem header retorna HTTP 401
  </acceptance_criteria>
  <done>
    - Endpoint POST /api/expiracao/verificar existe e retorna 202 com header correto, 401 sem header
    - Pacientes com data_expiracao <= hoje sao marcados ativo=false e recebem mensagem de bloqueio
    - Pacientes com data_expiracao em 1-3 dias recebem aviso (sem bloqueio)
    - agent.ts envia aviso inline quando dias_para_vencer <= 3 (sem bloquear o fluxo)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N8N → Backend | Chamada HTTP interna na rede Docker; deve ser autenticada |
| Internet → /api/expiracao/verificar | Endpoint interno nao deve ser acessivel sem credencial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | POST /api/expiracao/verificar | mitigate | requireInternalKey middleware — valida x-internal-key == INTERNAL_API_KEY em toda request |
| T-04-01-02 | Tampering | supabase UPDATE ativo=false | mitigate | Update filtrado por id especifico retornado da query; nunca aceita pacienteId do body da request |
| T-04-01-03 | Denial of Service | cron 09h disparando multiplos sendText() | accept | Volume maximo = numero de pacientes ativos (baixo para v1 single-tenant) |
</threat_model>

<verification>
Testes manuais apos execucao do plano:

1. Verificar correcao do workflow N8N:
```bash
grep "x-internal-key" n8n/workflows/nutrichat-cron-expiracao.json
# Deve retornar a linha com x-internal-key (nao x-internal-token)
```

2. Testar endpoint sem autenticacao (deve retornar 401):
```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3001/api/expiracao/verificar
# Esperado: 401
```

3. Testar endpoint com autenticacao correta (deve retornar 202):
```bash
curl -s -X POST http://localhost:3001/api/expiracao/verificar \
  -H "x-internal-key: nutrichat_internal_dev_key_2026"
# Esperado: {"status":"processando"}
```

4. Testar bloqueio (setar data_expiracao = ontem no paciente de teste no Supabase, chamar endpoint, verificar que paciente.ativo virou false e recebeu mensagem no WhatsApp).

5. Verificar aviso inline no agente (setar data_expiracao = hoje+2 no paciente de teste, enviar qualquer mensagem pelo WhatsApp — deve receber aviso e a resposta normal na sequencia).
</verification>

<success_criteria>
- POST /api/expiracao/verificar com x-internal-key correto retorna 202
- POST /api/expiracao/verificar sem header retorna 401
- Pacientes com data_expiracao <= hoje ficam com ativo=false no Supabase apos chamada ao endpoint
- Pacientes com data_expiracao em 1-3 dias recebem mensagem de aviso no WhatsApp (sem ficarem inativos)
- agent.ts envia aviso quando paciente com plano vencendo em <= 3 dias manda mensagem
- Workflow N8N n'ao usa mais x-internal-token
</success_criteria>

<output>
Apos conclusao, criar C:/Users/botel/OneDrive/Desktop/nutri_fit/.planning/phases/04-alertas-expiracao-relatorio-semanal/04-01-SUMMARY.md
</output>

---
phase: 05-painel-do-nutricionista
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/config/env.ts
  - backend/src/routes/pacientes.ts
  - backend/src/routes/api.ts
  - backend/src/services/rag.ts
  - backend/package.json
autonomous: true
requirements:
  - PANEL-01
  - PANEL-02
  - PANEL-03
  - PANEL-04
  - PANEL-05
  - PANEL-06

must_haves:
  truths:
    - "POST /api/pacientes aceita multipart/form-data com nome, whatsapp, plano, data_expiracao e PDF, retorna 201"
    - "PDF é salvo no Supabase Storage bucket 'dietas' via service key"
    - "Boas-vindas são disparadas no WhatsApp após salvar o paciente"
    - "processarDieta é chamado fire-and-forget após retornar 201"
    - "GET /api/pacientes retorna lista com campo 'status' calculado (ativo/expirando/expirado)"
    - "PATCH /api/pacientes/:id atualiza ativo e/ou data_expiracao"
    - "Todas as rotas rejeitam requisições sem header X-API-Key correto com 401"
    - "Bug STORAGE_PREFIX em rag.ts corrigido (sem /public/ no path)"
  artifacts:
    - path: "backend/src/routes/pacientes.ts"
      provides: "Router com requirePanelKey + POST + GET + PATCH"
      contains: "requirePanelKey"
    - path: "backend/src/config/env.ts"
      provides: "PANEL_API_KEY no tipo Env e no array required"
      contains: "PANEL_API_KEY"
    - path: "backend/src/routes/api.ts"
      provides: "Registro do pacientesRouter em /api/pacientes"
      contains: "pacientesRouter"
    - path: "backend/src/services/rag.ts"
      provides: "STORAGE_PREFIX sem /public/"
      contains: "object/dietas/"
  key_links:
    - from: "backend/src/routes/pacientes.ts"
      to: "backend/src/config/env.ts"
      via: "env.PANEL_API_KEY"
      pattern: "env\\.PANEL_API_KEY"
    - from: "backend/src/routes/pacientes.ts"
      to: "backend/src/services/rag.ts"
      via: "processarDieta fire-and-forget"
      pattern: "processarDieta.*catch"
    - from: "backend/src/routes/api.ts"
      to: "backend/src/routes/pacientes.ts"
      via: "apiRouter.use('/pacientes', pacientesRouter)"
      pattern: "pacientesRouter"
---

<objective>
Adicionar `PANEL_API_KEY` ao env.ts do backend, criar o router `pacientes.ts` com as três rotas (POST, GET, PATCH) usando multer memoryStorage e middleware X-API-Key, registrar o router em api.ts, e corrigir o bug de STORAGE_PREFIX em rag.ts.

Purpose: Expõe a API que o painel frontend consumirá. Sem este plano, o painel 05-01 não tem backend para operar. Paralelo ao 05-01 (zero conflito de arquivos).
Output: 3 rotas novas funcionais em /api/pacientes, bug de rag.ts corrigido, multer instalado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-painel-do-nutricionista/05-CONTEXT.md
@.planning/phases/05-painel-do-nutricionista/05-RESEARCH.md

<interfaces>
<!-- Extraído do codebase para uso direto pelo executor -->

<!-- backend/src/config/env.ts — estrutura atual (adicionar PANEL_API_KEY) -->
```typescript
interface Env {
  PORT: number; NODE_ENV: string;
  SUPABASE_URL: string; SUPABASE_ANON_KEY: string; SUPABASE_SERVICE_KEY: string;
  EVOLUTION_API_URL: string; EVOLUTION_API_KEY: string; N8N_WEBHOOK_URL: string;
  CLAUDE_API_KEY: string; GROQ_API_KEY: string; OPENAI_API_KEY: string;
  INTERNAL_API_KEY: string;
  // Adicionar: PANEL_API_KEY: string;
}
const required = ['SUPABASE_URL', ... 'INTERNAL_API_KEY']
// Adicionar 'PANEL_API_KEY' ao array required e ao objeto retornado
```

<!-- backend/src/routes/api.ts — estado atual (adicionar pacientesRouter) -->
```typescript
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';
import { alertasRouter } from './alertas';
import { expiracaoRouter } from './expiracao';
import { relatorioRouter } from './relatorio';
import { aguaRouter } from './agua';
// Adicionar: import { pacientesRouter } from './pacientes';
// Adicionar: apiRouter.use('/pacientes', pacientesRouter);
```

<!-- backend/src/routes/boas-vindas.ts — padrão requireInternalKey a replicar -->
```typescript
function requireInternalKey(req, res, next): void {
  if (req.headers['x-internal-key'] !== env.INTERNAL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' }); return;
  }
  next();
}
```

<!-- backend/src/services/rag.ts linha 27 — BUG a corrigir -->
// ATUAL (errado): const STORAGE_PREFIX = `${env.SUPABASE_URL}/storage/v1/object/public/dietas/`;
// CORRETO:        const STORAGE_PREFIX = `${env.SUPABASE_URL}/storage/v1/object/dietas/`;

<!-- backend/src/services/agent.ts — função disponível -->
// export async function enviarBoasVindas(pacienteId: string): Promise<void>
// (linha 328 de agent.ts — funcional para uso direto)
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/pacientes | Requer X-API-Key header — rota não exposta publicamente |
| Multer → Supabase Storage | Buffer em memória (sem disco); service key usada apenas no backend |
| Backend → Evolution API | Chamada interna após autenticação X-API-Key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Spoofing | requirePanelKey middleware | mitigate | Header `x-api-key` comparado com `env.PANEL_API_KEY`; retorna 401 se ausente ou incorreto em todas as rotas /api/pacientes |
| T-05-02-02 | Tampering | multer fileFilter | mitigate | `file.mimetype !== 'application/pdf'` rejeita uploads não-PDF com erro 400 antes de qualquer processamento |
| T-05-02-03 | Information Disclosure | SUPABASE_SERVICE_KEY | mitigate | Service key usada apenas no backend para upload Storage e queries — nunca exposta em resposta JSON nem em variável VITE_ |
| T-05-02-04 | Denial of Service | multer limits | mitigate | `limits: { fileSize: 50 * 1024 * 1024 }` — rejeita PDFs acima de 50MB antes de alocar memória completa |
| T-05-02-05 | Elevation of Privilege | GET /api/pacientes sem auth | mitigate | `pacientesRouter.use(requirePanelKey)` aplicado antes de TODAS as rotas — enumeração de pacientes bloqueada sem chave |
| T-05-02-06 | Information Disclosure | PANEL_API_KEY em logs | accept | Logs de erro não incluem valores de headers; aceitável para uso interno single-tenant v1 |
</threat_model>

<tasks>

<task type="auto">
  <id>05-02-T1</id>
  <title>Adicionar PANEL_API_KEY ao env.ts + instalar multer + corrigir bug rag.ts</title>
  <read_first>
    - backend/src/config/env.ts (ver estrutura completa atual antes de editar)
    - backend/src/services/rag.ts (ver linha 27 com STORAGE_PREFIX incorreto)
    - backend/package.json (ver versão atual antes de adicionar multer)
  </read_first>
  <action>
**Passo 1 — Instalar multer no backend:**
```bash
cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend
npm install multer@2.1.1
npm install --save-dev @types/multer@2.1.0
```

**Passo 2 — Editar backend/src/config/env.ts:**

Adicionar `PANEL_API_KEY` ao interface `Env`, ao array `required` e ao objeto retornado. O arquivo final deve ser:
```typescript
import 'dotenv/config';

interface Env {
  PORT: number;
  NODE_ENV: string;
  SUPABASE_URL: string;
  SUPABASE_ANON_KEY: string;
  SUPABASE_SERVICE_KEY: string;
  EVOLUTION_API_URL: string;
  EVOLUTION_API_KEY: string;
  N8N_WEBHOOK_URL: string;
  CLAUDE_API_KEY: string;
  GROQ_API_KEY: string;
  OPENAI_API_KEY: string;
  INTERNAL_API_KEY: string;
  PANEL_API_KEY: string;
}

function validateEnv(): Env {
  const required = [
    'SUPABASE_URL',
    'SUPABASE_ANON_KEY',
    'SUPABASE_SERVICE_KEY',
    'EVOLUTION_API_URL',
    'EVOLUTION_API_KEY',
    'N8N_WEBHOOK_URL',
    'CLAUDE_API_KEY',
    'GROQ_API_KEY',
    'OPENAI_API_KEY',
    'INTERNAL_API_KEY',
    'PANEL_API_KEY',
  ] as const;

  const missing = required.filter((key) => !process.env[key]);
  if (missing.length > 0) {
    console.error('[env] Variaveis de ambiente ausentes:', missing.join(', '));
    process.exit(1);
  }

  return {
    PORT: parseInt(process.env.PORT ?? '3001', 10),
    NODE_ENV: process.env.NODE_ENV ?? 'development',
    SUPABASE_URL: process.env.SUPABASE_URL!,
    SUPABASE_ANON_KEY: process.env.SUPABASE_ANON_KEY!,
    SUPABASE_SERVICE_KEY: process.env.SUPABASE_SERVICE_KEY!,
    EVOLUTION_API_URL: process.env.EVOLUTION_API_URL!,
    EVOLUTION_API_KEY: process.env.EVOLUTION_API_KEY!,
    N8N_WEBHOOK_URL: process.env.N8N_WEBHOOK_URL!,
    CLAUDE_API_KEY: process.env.CLAUDE_API_KEY!,
    GROQ_API_KEY: process.env.GROQ_API_KEY!,
    OPENAI_API_KEY: process.env.OPENAI_API_KEY!,
    INTERNAL_API_KEY: process.env.INTERNAL_API_KEY!,
    PANEL_API_KEY: process.env.PANEL_API_KEY!,
  };
}

export const env = validateEnv();
```

Adicionar `PANEL_API_KEY=nutrichat_panel_key_dev` ao arquivo `.env` na raiz do projeto (ao lado do backend/) para desenvolvimento local.

**Passo 3 — Corrigir bug em backend/src/services/rag.ts linha 27:**

Substituir a linha:
```typescript
const STORAGE_PREFIX = `${env.SUPABASE_URL}/storage/v1/object/public/dietas/`;
```
Por:
```typescript
const STORAGE_PREFIX = `${env.SUPABASE_URL}/storage/v1/object/dietas/`;
```
Apenas esta linha muda. O restante de rag.ts permanece inalterado.
  </action>
  <acceptance_criteria>
    - backend/src/config/env.ts contém `PANEL_API_KEY: string` na interface Env
    - backend/src/config/env.ts contém `'PANEL_API_KEY'` no array required
    - backend/src/config/env.ts contém `PANEL_API_KEY: process.env.PANEL_API_KEY!` no objeto retornado
    - backend/src/services/rag.ts linha 27 contém `object/dietas/` (sem `/public/`)
    - backend/src/services/rag.ts NÃO contém `object/public/dietas/`
    - backend/package.json contém `"multer"` nas dependencies
    - `cd backend && npx tsc --noEmit` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>PANEL_API_KEY adicionado ao env.ts; bug STORAGE_PREFIX corrigido em rag.ts; multer instalado; TypeScript sem erros.</done>
</task>

<task type="auto">
  <id>05-02-T2</id>
  <title>Criar pacientes.ts (POST + GET + PATCH) e registrar em api.ts</title>
  <read_first>
    - backend/src/routes/boas-vindas.ts (padrão requireInternalKey a replicar como requirePanelKey)
    - backend/src/routes/api.ts (estado atual para adicionar pacientesRouter)
    - backend/src/config/env.ts (após T1 — confirmar PANEL_API_KEY disponível)
    - .planning/phases/05-painel-do-nutricionista/05-RESEARCH.md (Pattern 4, Pattern 5, Pattern 6, Code Examples completo POST /api/pacientes)
    - .planning/phases/05-painel-do-nutricionista/05-CONTEXT.md (D-07, D-08, D-09, D-12 — regras de negócio das rotas)
  </read_first>
  <action>
**Criar backend/src/routes/pacientes.ts:**

```typescript
import { Router, Request, Response, NextFunction } from 'express';
import multer from 'multer';
import { createClient } from '@supabase/supabase-js';
import { env } from '../config/env';
import { processarDieta } from '../services/rag';
import { enviarBoasVindas } from '../services/agent';

export const pacientesRouter = Router();

const supabase = createClient(env.SUPABASE_URL, env.SUPABASE_SERVICE_KEY);

const upload = multer({
  storage: multer.memoryStorage(),
  limits: { fileSize: 50 * 1024 * 1024 }, // 50 MB — mesmo limite do bucket 'dietas'
  fileFilter: (_req, file, cb) => {
    if (file.mimetype !== 'application/pdf') {
      cb(new Error('Apenas PDF permitido'));
      return;
    }
    cb(null, true);
  },
});

function requirePanelKey(req: Request, res: Response, next: NextFunction): void {
  if (req.headers['x-api-key'] !== env.PANEL_API_KEY) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}

// Middleware de autenticação aplicado a TODAS as rotas deste router
pacientesRouter.use(requirePanelKey);

// POST /api/pacientes — Cadastrar novo paciente
// Body: multipart/form-data com campos nome, whatsapp, plano, data_expiracao + arquivo 'dieta' (PDF)
pacientesRouter.post('/', upload.single('dieta'), async (req: Request, res: Response) => {
  const { nome, whatsapp, plano, data_expiracao } = req.body as {
    nome: string;
    whatsapp: string;
    plano: string;
    data_expiracao: string;
  };

  if (!req.file) {
    res.status(400).json({ error: 'PDF da dieta obrigatorio' });
    return;
  }
  if (!nome || !whatsapp || !plano || !data_expiracao) {
    res.status(400).json({ error: 'Campos obrigatorios ausentes: nome, whatsapp, plano, data_expiracao' });
    return;
  }

  // 1. Inserir paciente na tabela pacientes
  const { data: paciente, error: pacienteErr } = await supabase
    .from('pacientes')
    .insert({ nome, whatsapp, plano, data_expiracao, ativo: true })
    .select('id')
    .single();

  if (pacienteErr || !paciente) {
    console.error('[pacientes] Erro ao inserir paciente:', pacienteErr?.message);
    res.status(500).json({ error: pacienteErr?.message ?? 'Erro ao salvar paciente' });
    return;
  }

  // 2. Upload PDF para Supabase Storage (bucket 'dietas', privado — service key bypassa RLS)
  const storagePath = `${paciente.id}/${Date.now()}.pdf`;
  const { error: uploadErr } = await supabase.storage
    .from('dietas')
    .upload(storagePath, req.file.buffer, {
      contentType: 'application/pdf',
      upsert: false,
    });

  if (uploadErr) {
    console.error('[pacientes] Erro ao fazer upload do PDF:', uploadErr.message);
    res.status(500).json({ error: uploadErr.message });
    return;
  }

  // pdfUrl sem "/public/" — bucket é privado (compatível com STORAGE_PREFIX corrigido em rag.ts)
  const pdfUrl = `${env.SUPABASE_URL}/storage/v1/object/dietas/${storagePath}`;

  // 3. Inserir registro na tabela dietas
  const { data: dieta, error: dietaErr } = await supabase
    .from('dietas')
    .insert({ paciente_id: paciente.id, pdf_url: pdfUrl, status: 'ativa' })
    .select('id')
    .single();

  if (dietaErr || !dieta) {
    console.error('[pacientes] Erro ao inserir dieta:', dietaErr?.message);
    res.status(500).json({ error: dietaErr?.message ?? 'Erro ao salvar dieta' });
    return;
  }

  // 4. Disparar boas-vindas (await — confirmar envio antes de retornar)
  await enviarBoasVindas(paciente.id).catch((err) => {
    console.error('[pacientes] Falha ao enviar boas-vindas (nao critico):', err);
    // Falha de boas-vindas não bloqueia o cadastro — continua
  });

  // 5. Retornar 201 imediatamente
  res.status(201).json({ sucesso: true, paciente_id: paciente.id });

  // 6. RAG em background: fire-and-forget após retornar 201 (D-08)
  processarDieta(paciente.id, dieta.id, pdfUrl).catch((err) => {
    console.error('[pacientes] Falha no processamento RAG (background):', err);
  });
});

// GET /api/pacientes — Listar pacientes com status calculado
pacientesRouter.get('/', async (_req: Request, res: Response) => {
  const { data: pacientes, error } = await supabase
    .from('pacientes')
    .select('id, nome, whatsapp, plano, data_expiracao, ativo, created_at')
    .order('created_at', { ascending: false });

  if (error) {
    console.error('[pacientes] Erro ao listar pacientes:', error.message);
    res.status(500).json({ error: error.message });
    return;
  }

  const hoje = new Date();
  const tresD = new Date(hoje);
  tresD.setDate(tresD.getDate() + 3);

  function calcularStatus(p: {
    ativo: boolean;
    data_expiracao: string;
  }): 'ativo' | 'expirando' | 'expirado' {
    if (!p.ativo) return 'expirado';
    const exp = new Date(p.data_expiracao);
    if (exp < hoje) return 'expirado';
    if (exp <= tresD) return 'expirando';
    return 'ativo';
  }

  const lista = (pacientes ?? []).map((p) => ({
    ...p,
    status: calcularStatus(p),
  }));

  res.json(lista);
});

// PATCH /api/pacientes/:id — Atualizar ativo e/ou data_expiracao
pacientesRouter.patch('/:id', async (req: Request, res: Response) => {
  const { id } = req.params;
  const { ativo, data_expiracao } = req.body as {
    ativo?: boolean;
    data_expiracao?: string;
  };

  if (ativo === undefined && !data_expiracao) {
    res.status(400).json({ error: 'Ao menos um campo obrigatorio: ativo ou data_expiracao' });
    return;
  }

  const updates: Record<string, unknown> = {};
  if (ativo !== undefined) updates.ativo = ativo;
  if (data_expiracao) updates.data_expiracao = data_expiracao;

  const { error } = await supabase
    .from('pacientes')
    .update(updates)
    .eq('id', id);

  if (error) {
    console.error('[pacientes] Erro ao atualizar paciente:', error.message);
    res.status(500).json({ error: error.message });
    return;
  }

  res.json({ sucesso: true });
});
```

**Editar backend/src/routes/api.ts** — adicionar import e registro do pacientesRouter:
```typescript
import { Router } from 'express';
import { webhookRouter } from './webhook';
import { ragRouter } from './rag';
import { boasVindasRouter } from './boas-vindas';
import { alertasRouter } from './alertas';
import { expiracaoRouter } from './expiracao';
import { relatorioRouter } from './relatorio';
import { aguaRouter } from './agua';
import { pacientesRouter } from './pacientes';  // NOVO

export const apiRouter = Router();

apiRouter.use('/webhook', webhookRouter);
apiRouter.use('/rag', ragRouter);
apiRouter.use('/boas-vindas', boasVindasRouter);
apiRouter.use('/alertas', alertasRouter);
apiRouter.use('/expiracao', expiracaoRouter);
apiRouter.use('/relatorio', relatorioRouter);
apiRouter.use('/agua', aguaRouter);
apiRouter.use('/pacientes', pacientesRouter);  // NOVO
```
  </action>
  <acceptance_criteria>
    - backend/src/routes/pacientes.ts contém `requirePanelKey`
    - backend/src/routes/pacientes.ts contém `multer.memoryStorage()`
    - backend/src/routes/pacientes.ts contém `upload.single('dieta')`
    - backend/src/routes/pacientes.ts contém `enviarBoasVindas`
    - backend/src/routes/pacientes.ts contém `processarDieta` seguido de `.catch`
    - backend/src/routes/pacientes.ts contém `calcularStatus`
    - backend/src/routes/pacientes.ts contém a string `object/dietas/` (sem `/public/`) no pdfUrl
    - backend/src/routes/api.ts contém `pacientesRouter`
    - `cd backend && npx tsc --noEmit` sai com código 0
    - `docker restart nutrichat_backend && sleep 5 && curl -s http://localhost:3001/health` retorna JSON com status ok
    - `curl -s -X GET http://localhost:3001/api/pacientes` retorna 401 (sem X-API-Key)
    - `curl -s -X GET http://localhost:3001/api/pacientes -H "X-API-Key: nutrichat_panel_key_dev"` retorna array JSON (pode ser vazio `[]`)
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/botel/OneDrive/Desktop/nutri_fit/backend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>pacientes.ts com 3 rotas funcionais (POST/GET/PATCH); requirePanelKey aplicado globalmente; calcularStatus no GET; fire-and-forget no POST; api.ts registra o router; TypeScript sem erros; backend responde 401 sem chave e retorna lista com chave correta.</done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` — zero erros de tipo
2. `docker restart nutrichat_backend && sleep 8`
3. `curl -s http://localhost:3001/health` — responde com status ok
4. `curl -s http://localhost:3001/api/pacientes` — responde 401 `{"error":"Unauthorized"}`
5. `curl -s http://localhost:3001/api/pacientes -H "X-API-Key: nutrichat_panel_key_dev"` — responde `[]` ou lista JSON
6. `grep "object/public/dietas" backend/src/services/rag.ts` — deve retornar vazio (bug corrigido)
7. `grep "PANEL_API_KEY" backend/src/config/env.ts` — deve retornar 3 linhas (interface, required, retorno)
</verification>

<success_criteria>
- PANEL_API_KEY declarado em env.ts (PANEL-01 ... PANEL-06: pré-requisito do middleware)
- POST /api/pacientes aceita multipart/form-data, salva paciente, faz upload PDF, dispara boas-vindas, inicia RAG fire-and-forget, retorna 201 (PANEL-01, PANEL-02, PANEL-03)
- GET /api/pacientes retorna lista com campo status: 'ativo' | 'expirando' | 'expirado' (PANEL-04)
- PATCH /api/pacientes/:id atualiza ativo e/ou data_expiracao (PANEL-05, PANEL-06)
- requirePanelKey bloqueia todas as rotas sem X-API-Key correto com 401 (D-12)
- Bug STORAGE_PREFIX corrigido em rag.ts — pdfUrl e validação de path compatíveis
- multer 2.1.1 instalado com memoryStorage (sem disco efêmero)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-painel-do-nutricionista/05-02-SUMMARY.md` com:
- Resultado dos curl smoke tests (GET retorna 401 sem chave, 200 com chave)
- Confirmação do bug fix em rag.ts
- Resultado do typecheck
- Qualquer desvio do plano e justificativa
</output>
